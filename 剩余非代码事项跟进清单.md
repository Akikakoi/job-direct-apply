# 简历直达 · 剩余非代码事项跟进清单

> 来源：开发文档 v1.42 §14 / §12.6 / §12.5 / §10（2026-09-25 整理，随第十八轮更新）｜ 范围：**仅列“代码代不了”的事项 + 前端工程尾巴**；代码缺口另见文末「边界说明」。
> 口径：每项都标注了「谁做」+「闭环判据」+「机器兜底」。签署类事项**不阻断合入**，但脚本会一直报状态（exit 1）。
> 说明：本文件是跟进用清单，非仓库正式文档，可随时删除。

---

## A. 法务（签署 / 条款）——最高优先，卡上线

- [ ] A1 协议与隐私政策**法务复核签署**：填 `frontend/app/legal/legal-text.json` 的 `meta.legal_review.signed_version / reviewer / reviewed_at`，且 `signed_version` 必须等于当前 `POLICY_VERSION`（**当前 `1.1`**）。判据：`python scripts/export_legal_text.py` 不再报“未闭环”（exit 0）。注意：**政策改版会让原签署失效**，脚本会报“须重新复核”。
- [ ] A2 **运营主体名称与联系方式**落终稿：协议「六、联系方式」写具体主体与邮箱（现为占位待补）。
- [ ] A3 **适用法律与争议解决**条款拍板。
- [ ] A4 **个人信息保存期限与到期处理**条款拍板。
- [ ] A5 **英文版效力口径**取定：现为"中文为准、英文为参考译文"，如改为中英同等效力需同步改 `disclaimer` 与 `meta.authoritative_lang`（脚本双向校验，互相打脸会被拒收）。
- [ ] A6 **逐公司 robots/terms 审计结论的法务复核签署**（11/11 家，见开发文档 §14 ③ 表；行数据源 `backend/app/services/compliance.py`）。
- [ ] A7 **KMS / 托管密钥**（应用层 AES-GCM 已于 2026-09-25 落地，政策第四节已改为如实描述、版本升 v1.1）：若后续接入 KMS，需再次同步该节文案；法务复核签署亦需针对 v1.1 重签。

送审出口命令：

```bash
python scripts/export_legal_text.py --lang both --out 送审稿.txt
```

---

## B. 运营

- [ ] B1 **投递告知文案与产品 PRD 正式对齐**：要点已固化成常量，对齐时只改 `legal.APPLY_NOTICE` 一处（后端 `app/services/legal.py`）。
- [ ] B2 **8 项密钥在服务商侧实际轮换**：`llm_api_key` / `ocr_api_key` / `smtp_password` / `im_webhook_secret` / `auth_secret` / `uploads_key` / `DATABASE_URL` / `REDIS_URL`（清单在 `prodcheck.ROTATION_KEYS`）。其中 `uploads_key` 轮换前需先用 `scripts/encrypt_uploads.py` 重新加密存量（否则旧文件解不开）。判据：`python scripts/audit_production_config.py` 无 `fail`（exit 0）。生产 `REDIS_URL` 必须设密码且不暴露公网。
- [ ] B3 **C 端订阅的推送渠道与频次**（市场洞察/旺季提醒）：可复用 `app/services/notify.py` 的邮件/IM 通道，渠道与名单属运营配置。
- [ ] B4 **旺季到点订阅提醒**的订阅名单与推送节奏（同上，可复用 `notify.py`）。

---

## C. 设计

- [ ] C1 **位图安装图标 192/512（含 maskable 版本）**：PWA 现仅有 SVG 图标（`frontend/public/`），真机安装体验需位图。

---

## D. 外部工程 / 第三方

- [ ] D1 **微信小程序**：独立工程（小程序账号 / 域名备案 / 独立前端），非本仓库改造项。
- [ ] D2 **日程双向同步**：需日历 OAuth 授权（面试陪伴闭环的后续）。
- [ ] D3 **公司深度资料**（融资轮次 / 新闻）：需外部数据源，当前背景包只用库内公开招聘信息。
- [ ] D4 **B 端数据交付**：合同 / 脱敏协议 / 导出格式（报告生成与 k-匿名口径本身已就绪，见 `GET /api/insights/market`）。
- [ ] D5 **域名备案**（若走小程序或独立前端）。

---

## E. 工程验收（人工实跑，非新功能）

- [ ] E1 **政策正文渲染实跑**：`npm run build` + `next start` 后按 `lang` cookie 取页，确认中英两页正文与效力声明正确。
- [ ] E2 **PWA 真机验证**：Lighthouse / 手动"安装到主屏"，确认可安装与离线可用（隐私模式/旧浏览器会拒绝属预期）。
- [ ] E3 **PG 存量回填链跑完**：Docker/PG 起来后 → 采集补 description → `scripts/extract_job_skills.py` → 重算 match_scores（历史 4547 条）。
- [ ] E4 **存量原件加密**（部署侧）：配好 `UPLOADS_KEY` 后跑 `python scripts/encrypt_uploads.py`（幂等）。

---

## F. 前端工程（本轮新增代码的接入尾巴）

- [ ] F4 **登录页 + 令牌存储**：JWT 鉴权后端已就绪（`/api/auth/*`），前端要接：登录/注册表单、令牌存取（access 过期用 refresh 换）、请求带 `Authorization: Bearer`、401 统一跳登录；上线时同步置 `AUTH_REQUIRED=true`。
- [ ] F5 **/insights 权重历史卡片（可选）**：`GET /api/match-weights` 已就绪，看板加“当前生效权重（来源）+ 采纳历史 + 一键回滚”。
- [ ] F6 **companies 管理页（可选）**：§12.7 #1 备注项，API 早已就绪。

## G. 人工校对与数据积累

- [ ] G1 **第三语言翻译产出需人工校对**（当前仅中英，`SUPPORTED_LANGS = ("zh-CN","en")`）。
- [ ] G2 **量化评估基线**：约面率 / NDCG 达预设基线需反馈数据积累（反馈回灌闭环已上线，等数据转起来）。
- [ ] G3 **行业细分季节**需先有行业标签（招聘季模块的后续）。

---

## 机器兜底对照（不靠人记）

| 事项 | 兜底机制 | 命令 |
|---|---|---|
| 签署未闭环 / 改版后旧签失效 | `export_legal_text.py` exit 1 | `python scripts/export_legal_text.py` |
| 英文漏译 / 结构漂移 / 效力声明矛盾 | 同上 exit 2 + `tests/test_legal.py` | 同上 |
| 政策版本前后端漂移 | `tests/test_legal.py` 三点一线断言 | `pytest tests/test_legal.py` |
| 密钥占位 / 过短 / `.env` 被跟踪 | `audit_production_config.py` exit 1 | `python scripts/audit_production_config.py` |
| 未开启 AUTH_REQUIRED / 默认 JWT 签名密钥 / 原件未加密 | `prodcheck` 的 `auth_not_required`・`uploads_plaintext`（warn）与 `placeholder_auth_secret`（fail） | 同上 |
| 指标基数爆炸 / 5xx 漏计 / 指标泄露密钥 | `tests/test_obs.py`（路由模板、异常路径计数、不泄露断言） | `pytest tests/test_obs.py` |
| 新增公司漏做审计 | `GET /api/compliance/audit` 单列 `unreviewed` | `python scripts/audit_compliance.py --markdown` |
| 运维口径"美化"（限流剔分母等） | `tests/test_ops.py` | `pytest tests/test_ops.py` |

---

## 边界说明（看起来像"非代码"，实际要写代码）

以下一条被开发文档归入“非代码”，但从实现角度看需要新增代码，**未列入上面的清单**，避免误判为纯商务事项：

1. ~~Prometheus 指标暴露与外部告警通道~~ —— **指标暴露已于 2026-09-25 第十六轮落地**（`GET /metrics`、`LOG_JSON=true`、`SENTRY_DSN` 可选）；**仅剩外部告警通道**（Alertmanager / 钉钉告警）与 Prometheus 服务端部署，属运维配置。

**代码缺口已全部关闭（2026-09-25 第十九轮）**：~~PGVector 多语嵌入~~ 已落地（`services/embeddings.py` + `vector_store.py` + `match.build_vec_map` 双路分流 + `scripts/backfill_vectors.py`；默认 TF-IDF 不变，`EMBEDDINGS_PROVIDER=sentence_transformers` + PG 即启用；本机真容器全链路验证，海外语义均分 0.0020 → 0.4318）。
（已完成项：JWT 鉴权（第十四轮）、简历原件加密（第十五轮）、可观测性/指标暴露（第十六轮）、官网兜底适配器规则化解析（第十七轮）、反馈回灌闭环（第十八轮，权重版本化采纳/回滚/自动调参）、PGVector 多语嵌入（第十九轮）。前端登录页与生产密钥轮换、`AUTH_REQUIRED=true`、存量加密属上表 B2/E4/F 与前端工程。）
