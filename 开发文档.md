# 简历直达 · 开发文档

> 版本：v1.50（2026-09-26 同步）｜ 面向读者：开发/后端/前端工程师 ｜ 依赖：《简历直达方案.md》（产品与策略基线，本文不再复述）
> 本文给出可直接落地的技术设计：数据模型、API、采集适配器、简历解析、匹配引擎、投递闭环、调度、合规与开发任务拆解。

---

## 0. 实现进度速览（2026-09-24 同步）

**总体：P1~P4 全部退出；§9 三项后台任务异步化（idle_jobs_cleanup / resume_parse_task / run_match_task）已于 2026-09-24 收尾，§9 表零挂账；§12.7 清单 12 项全部销号**——#6（greenhouse 无 description）已修根因（适配器 `content=true` 一次请求补齐 800 条全文），#7（采集入库抽标签）已接入"词典优先 + LLM 兜底（默认关）"，**#9（海外岗位默认排序缺位）三方案全落地：A 交错展示 + B 补采 description + C 跨语言/跨区匹配口径（2026-09-24，含修正文档此前"exp/degree 缺失致规则分偏低"的错误归因）**；**§12.6 P5 储备轨道已全部落地（⑤ 项除外无遗留）**：① 市场洞察报告（`GET /api/insights/market`，脱敏聚合）② 面试陪伴闭环（准备清单 + 公司背景包 + `interview_at` 定时催进）③ AI 简历优化（对照目标 JD 逐条差距 + 改写建议）④ 移动端 / PWA（manifest + service worker + 响应式）⑤ 招聘季适配 + 全球多语言看板（`GET /api/insights/seasonality` + 看板中英双语与区域切换）；**§14 上线前 checklist 五项全部销号（政策版本与投递告知留痕 / 逐公司 robots·terms 审计表 / 运维监控看板 / 生产配置审计均离线可测，剩余条件已在 §14 逐项标注为"非代码"）**；**§14 ① 的协议/隐私政策已出中英对照版（第十三轮）：正文单一事实源 `frontend/app/legal/legal-text.json`，英文漏译/结构漂移由用例与 `scripts/export_legal_text.py` 双重拒收，仅剩法务复核签署**；**§5.1 官网兜底适配器（第十七轮）、§12.5 反馈回灌闭环（第十八轮）与 §7 二期 PGVector 多语嵌入（第十九轮：双路 vec 分——默认 TF-IDF 零依赖不变，配 EMBEDDINGS_PROVIDER + PG 就绪则 `<=>` 多语嵌入余弦；**第二十四轮实测该向量分在本库上退化为「JD 有多长」的伪信号，已按用户决定关回 TF-IDF，详见本轮明细**）均已落地**；**§4.3 的前端尾巴（登录/注册页 + 令牌存储 + `Authorization` 头接入）已于第二十轮落地——`app/auth.js` 自动附 Bearer，401 用 refresh 换一次令牌再重试，换不到清令牌跳 `/login?next=`**；测试 415/415（另 3 条 PG 集成测试在真容器全过，CI 无 PG 自动跳过）**：
4547 条真实职位 = 网易 2629 + Stripe 647 + Equinox 736（SmartRecruiters）+ Ashby 341 + Robinhood 153（Greenhouse）+ NVIDIA 40（Workday）+ Anyscale 1（Lever）。已推 GitHub（CI success）。

**本轮改动明细（2026-09-26 第二十六轮，打开 L1+L2 权重归一：把海外三个死权重救回来）：**
- **背景（承第二十五轮遗留②）**：L1+L2 权重归一（不可判定项剔除 + 权重按比例摊给其余项）已挂在 `WEIGHTS_AUTO_TUNE` 后面但**默认关**；关着时海外场景 skill（多岗位无技能标签 → 恒 0.5）、city（跨区 → 恒 0.5）、exp（多数 → 恒 1.0）三项仍是死权重。用户批准打开。
- **决策门：先 A/B 实测再动线上口径**。临时探针（`scripts/_l6_ab_autotune.py`，跑完即删）在**同一进程内**用 `compute_match` 分别按 `weights_auto_tune=False/True` 算两份打分，再按 `main._route_rows` 的同一逻辑（公司分组 → 组内截断 → L4 并列键 → 公司级轮转）复刻海外 top20——两臂**共用同一份 vec 分与同一把判据尺子**，只变开关。12 份简历实测：

  | 指标（12 份平均，海外） | 关（原口径） | 开（归一） | 判读 |
  |---|---|---|---|
  | top20 内对口工程岗 eng/20 | 15.25 | **15.92** | ↑ |
  | 明确无关岗（合计） | 43 | **39** | ↓ |
  | 全库海外 rule 标准差 | 0.0868 | **0.1360** | ↑ +57% |
  | rule 不同取值档数（全库） | 27.2 | **32.5** | ↑ |
  | 最大同分块（同一 final 值，全库） | 598.8 | 577.0 | 仅 -3.6% |
  | top10 门槛上精确并列条数 | 3.4 | 9.8 | ↑ 更差 |
  | 覆盖公司数 | 8.00 | 8.00 | 持平（方案 A 既定行为） |

- **A/B 结论（本轮关键判断）**：① 当初担心的"skill/city 双双被剔除 → 只剩 exp:role 各 50% → rule 塌成 6 档"**没有发生**（rule 档数 27→32、σ 反升 57%）——三个死权重确实被救回，`resume=1` 的 top20 由 `Forward Deployed Engineer / Membership Sales Advisors` 换成 `Backend Engineer, Intelligent Commerce / Backend Engineer, Privy` 这类真对口岗；② **并列问题基本没动（-3.6%）**，因为它的根因是 `final_score` / `rule_score` / `vec_score` 落库都是 `Numeric(5,2)`——vec 分被量化到两位小数后，1918 条海外里最大的一个同分块仍有 ~577 条。**这是精度问题，不是权重问题**，归一治不了；
- **落地**：[.env](file:///d:/AIProjects/job-direct-apply/backend/.env) 置 `WEIGHTS_AUTO_TUNE=true`（附 A/B 结论与"打开必须全量重算"的说明）；[.env.example](file:///d:/AIProjects/job-direct-apply/backend/.env.example) 的 §12.5 段补上"第二重语义 = 启用 L1+L2 归一打分"，模板仍保持 `false`（新部署需显式 opt-in）；
- **顺带发现：库存分数已部分过期**。探针发现用当前职位文本重算时 `rule` 与库中一致、`final` 却对不上（最多 1228/1918 条，差异全在 vec 融合项）——说明上一次全量重算之后职位 description 有更新（采集侧刷新过）。本轮全量重算一并刷新；
- **重算与复核**：12 份简历 `match_scores` 全量重算 **54564 行（10.3s）**；重启 uvicorn 8001（无 `--reload`）；按 `explain` + 归一公式复算抽查 **600/600 条吻合**（确认库存分数确为新口径）。`GET /api/recommend?resume_id=20&region=overseas&limit=20` 前 4 位为 `Backend Engineer, Billing/Tax`（greenhouse，0.71/0.93）/ `Enterprise Solutions Architect - Americas` / `Full-Stack Engineer` / `Design Engineer (Web & Brand)`；第 5 位仍是 Equinox `Personal Trainer Internship, Westchester`——**经公司级轮转拿到曝光位**（该公司 best 就是这条），非打分误判；
- **测试 414 → 415**：新增 `test_weight_normalization_overseas_drops_skill_and_city`（skill 无标签 + city 跨区 → 两项**同时**被剔除、权重摊给 exp/role；关 = 旧口径两项各塞 0.5）；**修 `test_beat_auto_tune_entry_gated_by_settings` 的非密闭性**——它原先直接断言"默认关"，本机 `.env` 打开后必失败；改为显式 monkeypatch 两臂 + `finally` 按本机配置还原注册状态（与 [test_vectors.py](file:///d:/AIProjects/job-direct-apply/backend/tests/test_vectors.py) 的 `_hermetic_env` 同一条约定：**单测不依赖开发者本机 `.env`**）。`pytest tests -q` 在开关开/关两种环境下均 **415 passed / 3 skipped**；
- **遗留（并列的真根因，下一轮的主要候选）**：① 分数精度 —— `Numeric(5,2)` 改 `Numeric(6,4)`（需 alembic 迁移 + 全量重算）才是并列的直接解法，且它会把本轮 L3 分档与权归一的区分度真正释放出来；② L4 仍只在 SQL 已取回的窗口内重排（每公司 `.limit(fetch_n)` 按 updated_at 截断）；③ 数据质量：Anyscale 占位岗与 NVIDIA skills 错标（L5 未做）。

**上一轮改动明细（2026-09-26 第二十五轮，海外排序区分度：L4 并列打破器 + L3 role 分档）：**
- **背景（承上一轮"遗留"）**：关掉向量分后海外 top10 并列仍有 68~96 条。三条根因：① `MatchScore.final_score` / `rule_score` 落库是 `Numeric(5,2)`（两位小数），分数**天然**大面积并列；② 四分项里 skill（海外多岗位无技能标签 → 恒 0.5）、city（跨区 → 恒 0.5）、exp（多数 → 恒 1.0）三项在海外场景是**死权重**，只剩 role 一个真信号，而它是**二值 0/1**；③ SQL 侧 `order_by` 只有 final → rule → updated_at，并列之后实际决定出场的退化成"谁先入库"；
- **L6 基线先行（改前实测，12 份简历 × 海外 top20，同口径）**：avg eng/20 = 15.25、avg 覆盖公司 8.0、avg tie@10 = **95.9**、明确无关岗合计 **28**、avg rule 标准差 0.0808；
- **L4 并列打破器**（[main.py](file:///d:/AIProjects/job-direct-apply/backend/app/main.py)）：新增 `_tokens` / `_role_title_overlap` / `_tie_break_key`；`_route_rows` 的路内排序由裸 `order_by` 改为 `_tie_break_key`——final → rule → **role 原始分 → skill 交集命中数 → title 与 target_role 词面重合数** → updated_at → id；公司顺序补 `best_rule` 次键；`recommend()` 取出 `profile.target_role` 传入。**只改排序、不改分数**。词面比对 ASCII 取整词、CJK 取前两字（"后端工程师" vs "后端开发工程师" 整词不同但同职能，取前两字才比得动）；
- **L3 role 分档**（[match.py](file:///d:/AIProjects/job-direct-apply/backend/app/pipelines/match.py)）：`_role_fit` 精确未命中不再一律 0，按**职能族**给六档——同族他职能（后端简历看前端/测试）**0.85**、本族泛称（software / platform / infrastructure）**0.6**、相邻族（工程↔数据、产品↔运营）**0.4**、未识别 title **0.2**（不冤枉）、他族职能词或明确无关岗词表（recruiter / sales / trainer / spa / locker…）**0.0**；`_ROLE_SYNONYMS` 补"软件 / 平台 / 基础设施"三组。三个坑已修：`operations` 同时挂在运维与运营下 → 列入 `_ROLE_FAMILY_AMBIGUOUS` **不参与判族**（精确命中路径不受影响）；ASCII 词必须用词边界正则（否则 `ui` 会命中 `build`）；`_other_family_hit` 必须同时扫**泛称词表**（否则 `Technical Recruiter` 会落 0.2 而非 0）；
- **L1+L2 权重归一挂在 `WEIGHTS_AUTO_TUNE` 后面（默认关）**：`_skill_hit` / `_city_fit` 新增 `drop_unknown`，不可判定时返回 `None` 而非 0.5；`match_parts` 剔除 `None` 项；`weighted_rule(..., normalize=True)` 把缺项权重按比例摊给其余项（全缺 → 0.0）；`compute_match` 用 `settings.weights_auto_tune` **一处开关**同时控制"是否剔除"与"是否归一"；[tuning.py](file:///d:/AIProjects/job-direct-apply/backend/app/services/tuning.py) 的 `collect_samples` / `_final` 同步读该开关，**避免"调参口径 ≠ 线上口径"漂移**。默认关 = 打分口径逐位不变；
- **[config.py](file:///d:/AIProjects/job-direct-apply/backend/app/core/config.py)**：`weights_auto_tune` 注释补第二重语义（开 = beat 按周评估并采纳优于基线的权重 **+ 启用权重归一打分**）；**打开之前必须全量重算 match_scores**（否则库里是新口径、库外是旧口径）；
- **测试 410 → 414**：`test_role_fit_family_tiers`（六档各一断言）、`test_role_fit_generic_word_does_not_promote_off_target`（`Software Sales Representative` 不因 software 被当工程岗）、`test_weight_normalization_gated_behind_flag`（关 = 0.60 旧口径 / 开 = 0.70 归一）、`test_tie_break_key_sub_score_priority`（同 final/rule 下 role 高的排前）；`test_profile_update_triggers_rematch` 断言由 `role == 0.0` 改为 `0.85 + tier=family_peer`（L3 的预期行为变更）；
- **验证**：`pytest tests -q` **414 passed / 3 skipped**（5.5s）；12 份简历 `match_scores` 全量重算（54564 行）；重启 uvicorn 8001（无 `--reload`）；
- **改后同口径复测**：avg eng/20 **15.25 → 16.08**、avg tie@10 **95.9 → 56.2（-41%）**、明确无关岗 **28 → 11（-61%）**、avg rule 标准差 **0.0808 → 0.0869**、覆盖公司数 8.0 持平（方案 A 的既定行为，本轮不动）；
- **API 实跑**（`GET /api/recommend?resume_id=20&region=overseas&limit=20`）：首轮 8 个位子仍来自 8 家公司；`Full-Stack Engineer`、`Backend Engineer, Core Technology`、`Senior Software Engineer, Backend` 进前 6；仍可见 Equinox `Personal Trainer Internship`（0.42）——它是**经公司轮转拿到曝光位**（该公司 best 就是这条），不是打分误判，其 title 在 L3 下 role = 0；
- **遗留（下一轮的真瓶颈）**：① L4 只在 SQL 已取回的窗口内重排——每公司 `.limit(fetch_n)` 仍按 updated_at 截断，公司内并列超过 20 条时，窗口外的强信号岗进不来；② skill / city / exp 三项的"死权重"未动（把归一打开是干净的下一步，但需先全量重算并接受线上口径变更）；③ 数据质量：Anyscale 的 `We have moved our Careers Page to: ...`（1 条占位岗）与 NVIDIA skills 错标仍会占位（L5 未做）。

**上一轮改动明细（2026-09-25 第二十四轮，§7 二期多语嵌入的实测与回退）：**
- **背景**：承第二十三轮"公司级轮转"。用户追问"有没有更好的方法"，选定开启 §7 二期的多语嵌入（让海外/跨语言岗位的语义分先有区分度）；面对 `BAAI/bge-m3` 全库重建实测 3.5 小时（8 核 CPU 上 ~3s/条）的 CPU 成本，进一步选定**换轻量多语模型** `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`（118M/384 维，全库约 7 分钟）；
- **落地动作**：[embeddings.py](file:///d:/AIProjects/job-direct-apply/backend/app/services/embeddings.py) ① `SentenceTransformerProvider` 新增 `max_seq_length` 覆盖（MiniLM 自带截断仅 128，"标题+技能"就吃满、JD 正文全被切掉，vec 分会退化成规则分的重复）；② `embed()` 改为**长度感知分批**（切批前按文本长度排序、出批后按下标还原）——sentence-transformers 把一批 padding 到**批内最长**样本，而本库描述长度长尾严重（4547 条里 344 条超 2000 字符、最长 10975），按输入顺序切批时一条长 JD 能把整批 32 条拉到 8000+ token，实测全库回填慢 8 倍（2.6s/条 vs 0.32s/条）；③ [config.py](file:///d:/AIProjects/job-direct-apply/backend/app/core/config.py) 新增 `EMBEDDINGS_MAX_SEQ_LEN`（默认 512）；全库 4547 条 `--rebuild` 完成（约 7 分钟，21~22 条/s）；
- **实测发现的真问题（本轮关键结论）**：向量分在本库上**退化成"JD 有多长"的伪信号**，而不是语义匹配。多语 MiniLM 的 mean-pooling 空间高度各向异性（全体职位对全局均值 cos = 0.61），长文本全被拉向同一个"通用文档"方向。按描述长度分桶看对简历的平均 vec 分：**空描述（1573 条，Stripe/NVIDIA 等）0.08 → 1~500 字符 0.43 → 501~2000 字符 0.71 → >2000 字符（ElevenLabs/Ashby/Linear）0.80**；12 份简历里 8 份呈同一条单调曲线。同公司内还进一步塌缩：ElevenLabs within-cos 0.95、Ashby 0.90、Linear 0.91，而 Stripe/Equinox 只有 0.38；
- **已穿透到用户可见的排序**：`resume 20` 的海外 `final` 榜首变成 Ashby `Design Engineer ×4 / Engineering Manager ×3` + ElevenLabs `Enterprise Solutions Engineer`，真正对口且描述为空的 Stripe `Backend Engineer`（vec 0.08~0.29）被挤出前 10；前 10 名**同分并列 49 条**——正是用户最初抱怨的"没有区分度"。按 vec 排序更极端：ElevenLabs 的 `GTM Recruiter` / `Payroll Specialist` / `Accounts Payable Specialist` 与 `Data Engineer` 并列 0.83；
- **试过但未采纳的修法（都留了实测数）**：① **放大截断窗口不可行**——MiniLM 的 `max_position_embeddings=512` 是硬上限，改大会直接报张量尺寸错误；② **剥离正文头部公司样板文**（ElevenLabs 的 `ABOUT THE COMPANY` 约 2400 字符）——ElevenLabs within-cos 0.95 → 0.79，但 Ashby/Linear 几乎不变（0.91），因为它们的重复不在字面前缀而在通篇文化用词；③ **减去公司均值**——无效，长度轴不是均值方向；④ **去 top-2 主成分（ABTT）**在同一次测量里效果最好（海外榜首立刻变成 NVIDIA/Stripe 软件工程师，长度偏置消失：>2000 字符 -0.07 vs 空描述 +0.01），但 **k 极敏感**（k=1 仍被 Ashby 占满、k=4 退化成 Equinox 水疗师）——在待评估数据上调 k 属过拟合，未采纳；
- **本轮落地（按用户决定：先关掉向量分）**：`.env` 的 `EMBEDDINGS_PROVIDER` 改回 `none`，语义分回到 TF-IDF + 规则分 + 公司级轮转；12 份简历的 `match_scores` 全量重算（`note` 全部回到 `tfidf_cosine`，海外 vec 均分 0.003~0.004）；重启后端（`uvicorn ... --port 8001`，**无 `--reload`**）；[.env.example](file:///d:/AIProjects/job-direct-apply/backend/.env.example) 的 §7 段补上这段实测结论，避免下一个人重复踩；`jobs.vector` 里 4547 条 MiniLM 向量**保留未删**（重开时不必重嵌）；
- **API 实跑复核**（`GET /api/recommend?resume_id=20&region=overseas`，1918 条海外）：公司级轮转生效，首轮 8 个位子来自 8 家公司（ashby → elevenlabs → linear → **stripe** → equinox → robinhood → nvidia → anyscale），`Backend Engineer, Core Technology`（Stripe）回到第 4 位；vec 回到内容驱动的 TF-IDF（Stripe 后端岗 0.08，靠"backend↔后端"跨语言桥命中；Equinox 无关岗 0.00）；
- **测试 408 → 410**：新增 `test_embed_length_aware_batching_keeps_input_order`（echo 模型验批内等长 + 输出按入参顺序还原）、`test_provider_applies_configured_max_seq_length`（验 256 覆盖生效、0 时保留模型默认 128）；[test_vectors.py](file:///d:/AIProjects/job-direct-apply/backend/tests/test_vectors.py) 的夹具改名 `_hermetic_env` 并**封回 `embeddings_provider=none`**——生产 `.env` 曾打开 provider，单测不该依赖开发者本机配置；
- **验证**：`pytest tests -q` 410 passed / 3 skipped（4.8s）。
- **遗留（下一步的真瓶颈，与嵌入无关）**：关掉向量分后海外 top10 的**并列仍然存在**（`resume 20` 前 10 门槛上有 68 条并列、集中在 3 家公司），且 Equinox `Personal Trainer Internship` 的 rule 分（0.56）与 Stripe `Backend Engineer`（0.56）持平——海外场景下 city 走跨区中性 0.5、skills 多为空走中性 0.5，**规则分本身区分度就弱**。要让海外排序真正有区分度，下一个杠杆在规则分的海外口径（而非嵌入），需要时另起一轮。

**上一轮改动明细（2026-09-25 第二十三轮，海外推荐的"公司级轮转"）：**
- **问题现象**：用户反馈"还是只有给我推送 netease 的职位"。实测排除后端问题——`region=all` 时交错是生效的（6 份简历一律 netease 25 + ashby 25），真正的现象是**筛选停在「国内」**（`region=cn`，组件内 state 不落 URL/cookie，点过一次就一直生效，实测该模式就是 50/50 全 netease）。此为交互问题，非排序问题；
- **顺带暴露的真缺口**：即使切到「全部」，海外那半屏也**只有 ashby**。实测 `region=overseas` 前 50 名来源分布 = ashby 50/50；库内海外 8 家公司中 elevenlabs/linear/ashby（同在 ashby 平台，rule 均分 0.632）把前 50 名吃光，stripe（647 条）、equinox（736 条）、nvidia（40 条）一条都进不来（要到第 200 名附近才见 greenhouse）。根因：方案 A 只做了"国内 vs 海外"的跨区交错，**路内仍是纯 `final_score` 序**，没有任何公司维度约束；
- **改法**：新增 [`main._round_robin`](file:///d:/AIProjects/job-direct-apply/backend/app/main.py)（N 路严格轮转，`_mix_regions` 是它 N=2 的特例）与 `main._route_rows`——每路内部先按公司分桶（公司顺序 = 该公司在本简历下的最高分降序），公司内仍按 final/rule/updated 排序，逐轮各取一条，取空的公司自动退出。`region=all` 变成"两路各自公司级轮转 → 再跨区 1:1 交错"；`cn`/`overseas` 单区走同一套路内轮转。公司数是个位数（当前 11 家），逐家取数开销可忽略；
- **实测效果（真实 PG，1918 条海外）**：海外前 50 名由"ashby 50/50"变为**覆盖 8 家公司**，首轮 8 个位子来自 8 家不同公司（ashby → elevenlabs → linear → stripe → equinox → robinhood → nvidia → anyscale，按各家最高分降序）；anyscale 只有 1 条、拿到 1 个位置后即退出轮转。国内只有 netease 一家，行为逐条不变；
- **前端**：`page.js` 标题文案同步为"国内/国外交错展示，各路内按公司轮流曝光"（单区为"各公司轮流曝光，公司内按匹配度排序"），原"按匹配度排序"已与实现不符；
- **测试 406 → 408**：新增 `test_overseas_company_round_robin_breaks_monopoly`（同来源同城市、仅公司不同时，低分公司从"垫底"升到第 2 位）、`test_overseas_pagination_stable_across_companies`（多公司轮转下分页切片拼接 == 一次性取全）；`test_recommend_region.py` 的 `_job` 补充 `company_id` 参数；
- **需重启后端生效**：`start-all.ps1` 启的是 `uvicorn ... --port 8001`（**无 `--reload`**），改完必须重启后端进程才会生效；
- **验证**：`pytest tests -q` 408 passed / 3 skipped（5.0s）。

**上一轮改动明细（2026-09-25 第二十二轮，dev 浮标开关）：**
- **`next.config.js` 加 `devIndicators: false`**：关掉 Next.js 15 开发态**左下角**的 DevTools 圆标。项目自身固定浮层只在顶部（右上语言开关 `.lang-switch`、左上账号状态条 `.auth-bar`，见 `globals.css`），左下那个圆标没有对应产品语义、容易被误认成入口；仅影响 `next dev`，`-Prod` 的 build + start 本来就不渲染它。

**上一轮改动明细（2026-09-25 第二十一轮，§10 上传端点的阻塞活移出事件循环）：**
- **问题现象**：后端日志在 `POST /api/resumes 200 OK` 之后刷出一串 `ConnectionResetError: [WinError 10054]`，堆栈停在 `_ProactorBasePipeTransport._call_connection_lost()` → `sock.shutdown(SHUT_RDWR)`。这是 **Windows Proactor 事件循环的清理噪音**（对端已用 RST 关连接，服务端再 shutdown 即 10054；异常发生在响应之后，不影响功能），但它是真问题的症状；
- **根因**：`POST /api/resumes` 是 `async def`，却在事件循环里**同步**跑 `extract_text` → `parse_resume_text` → `llm_extract_profile`（httpx 同步调用，`LLM_TIMEOUT_S` 默认 60，实测单次 20~35s）。解析期间整个循环被冻住——**期间连 `/health` 都没人应答**，浏览器/Next 代理的 keep-alive 连接空转超时被 RST，循环恢复后清理这些连接就吐 10054（日志里一次 4 条＝同一批池化连接被一起掐掉）；
- **改法**：[main.py](file:///d:/AIProjects/job-direct-apply/backend/app/main.py) 的 `upload_resume` 只留"读请求体"（`await file.read()`）与入参校验，余下流程整段抽成同步函数 `_ingest_resume` 交给 `run_in_threadpool`（`starlette.concurrency`）——项目其余端点都是 `def`、天然走线程池，这里只是把唯一漏网的 async 端点补齐；抽文本 / 密文落盘 / LLM 解析 / 入库全在线程池，事件循环始终可服务其他请求；
- **行为不变**：`UnsupportedFile → 400`、空输入 `400`、`saved_path` / `file_encrypted` / `parse_status` / active 互斥逐条保持；**测试 405 → 406**，新增回归用例 `test_upload_blocking_work_leaves_event_loop`（断言"工作线程 ≠ 事件循环线程"，不真起服务、不真调 LLM）；
- **验证**：`pytest tests -q` 406 passed / 3 skipped（4.6s）。

**上一轮改动明细（2026-09-25 第二十轮，§4.3 前端登录页与令牌存储落地）：**
- **新模块 [auth.js](file:///d:/AIProjects/job-direct-apply/frontend/app/auth.js)（令牌 + 带鉴权 fetch）**：令牌存 `localStorage` 的 `access_token` / `refresh_token`（后端只认 `Authorization: Bearer`、无 cookie 会话，用 cookie 反而要自管 CSRF）；`authFetch(url, options)` 自动附 Bearer，**遇 401 先用 refresh 令牌换一次再重试一次原请求**，换不到就清令牌跳 `/login?next=<当前路径>`——"客户端自认登录、后端却拒绝"只剩这一个可自愈动作；**三条防坑**：① `refreshing` 单例让并发 401 只换一次令牌（首屏往往同时打多个接口），② `redirectOnFail: false` 给 `/api/auth/me` 这类**登录态自检**用（未登录 401 是正常答案，不是过期，不该被带去登录页），③ `typeof window` 守卫 + `localStorage` 抛错兜底（隐私模式按未登录处理，不炸页面）；
- **新页面 [/login](file:///d:/AIProjects/job-direct-apply/frontend/app/login/page.js)（登录/注册双 Tab）**：一个表单打 `/api/auth/login`、`/api/auth/register`（后端两条路由字段同名 `{email,password}`），成功后 `setTokens(data)` 再整页跳 `next`；`next` **只接受站内相对路径**（`startsWith("/") && !startsWith("//")`——`//evil.com` 是协议相对 URL，不挡就是开放重定向）；错误文案直接回显后端 `detail`（"邮箱或口令不正确"/"口令至少 8 位"），不自己造一套；
- **新组件 [auth-bar.js](file:///d:/AIProjects/job-direct-apply/frontend/app/auth-bar.js)（账号状态条）**：挂进 [layout.js](file:///d:/AIProjects/job-direct-apply/frontend/app/layout.js)，未登录给"登录"入口、已登录显示邮箱 + 退出（清令牌后整页刷新回到匿名视图，与 lang-switch 的取舍一致）；**登录态只在客户端可知**（令牌在 localStorage），故 `mounted` 前渲染 `null`，避免 hydration 前后 DOM 不一致；
- **首页接入**：[page.js](file:///d:/AIProjects/job-direct-apply/frontend/app/page.js) 的 8 处用户态请求（政策/投递列表/催进/简历详情与轮询/推荐/上传/标记投递/帮填）全部由裸 `fetch` 改为 `authFetch`，**文件内已无裸 `fetch(`**；`user_id=1` 按 §4.3 设计保留（后端有令牌时静默覆盖为令牌主体，过渡期不打断老客户端）；`/insights` 是公开只读看板，不动；
- **本轮补的用例**：新增 `tests/test_frontend_auth.py`（7 条前端契约：令牌双键与 `typeof window` 守卫、`Authorization: Bearer` 写法、**401 → refresh 一次 → 重试 → 跳登录**全链路字符串断言（含并发单例与 `redirectOnFail` 逃生门）、登录页打的两条路由与字段名/回跳开放重定向防护/协议入口、状态条登录态两态与 `layout` 挂载、`page.js` 无裸 `fetch` 且 8 个接口字面量齐备、`globals.css` 三处选择器）；测试 **398 → 405**（另 3 条 PG 集成不变）；
- **验证**：`pytest tests -q` 405 passed / 3 skipped（4.7s）；`npm run build` 通过并产出 `/login` 路由（`ƒ /login  1.93 kB / 105 kB`，build 内建 lint 与类型检查全过）；
- **剩余（非代码/部署）**：`AUTH_REQUIRED=true` 与生产 `AUTH_SECRET` 轮换属部署动作（§14 ⑤ 清单已含）；令牌存 localStorage 的 XSS 面在"不引第三方脚本、服务端不渲染用户私有数据"前提下按折中接受，真要收紧再改 httpOnly cookie + 后端读 cookie；**登录态跳转/回跳的浏览器真机行为未做实跑**（构建期类型与静态契约已覆盖，属浏览器环境验证项）。

**上一轮改动明细（2026-09-25 第十九轮，§7 二期 PGVector 多语嵌入落地）：**
- **新模块 `app/services/embeddings.py`（嵌入提供者）**：`get_provider()` 按配置返回——`EMBEDDINGS_PROVIDER=none`（默认）→ None，语义分继续走 TF-IDF 老路（零依赖不变）；`sentence_transformers` → 懒加载本地模型（**不装 torch 不 import、不下载模型**，首次 embed 才加载）；配置了未知名 → 启动即 `ValueError`（fail loud，不做静默降级——退化只能由 match 的显式分流决定）；模型产出维度与 `EMBEDDINGS_DIM` 不符 → `ValueError`（换模型没同步改配置早暴露）；
- **新模块 `app/services/vector_store.py`（PGVector 存取）**：`jobs.vector` 列**不进 ORM**（SQLite 无此类型，列进模型会污染全量单测）——由 `ensure_column()` 在 PG 上幂等建列（`CREATE EXTENSION IF NOT EXISTS vector` + `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`），读写走 raw SQL（`CAST(:v AS vector)` 参数绑定，**不引 pgvector python 包**，向量就是文本协议 `[0.1,0.2]`）；列**不定维**（pgvector 允许无维度列做 `<=>` 检索，仅建索引需定维；现库 4547 行全表距离毫秒级，量大再定维建 HNSW）——换嵌入模型只改配置不动表结构；维度一致性由 `EMBEDDINGS_DIM` 在读写入口强制校验；
- **就绪语义三层**（match 只认最后一层）：`infra_ready`（PG 方言 + vector 扩展 + 列）→ `vector_mode`（infra + provider 配置）；SQLite 单测环境恒 False，自动回退 TF-IDF；
- **`match.build_vec_map()` 统一出口（match 与 tuning 同源）**：向量模式 → 简历查询文本嵌一次 + 全库 active 职位 `<=>` 全量相似度（`search_similar(limit=None)`）；否则 TF-IDF（索引惰性构建，N 份简历共享一次 fit，向量模式完全不 fit——`refresh_matches_all` 性能不回归）；**缺向量的职位现场补嵌**（机器兜底：采集新增的职位不靠人跑脚本）；explain 的 semantic 项标注来源（`tfidf_cosine` / `embedding_cosine`，口径可审计）；`tuning.collect_samples` 同步改走 `build_vec_map`——**自动调参的 NDCG 评估与线上排序同口径**，不会出现“线上向量、调参 TF-IDF”的漂移；
- **回填脚本 `scripts/backfill_vectors.py`**：`--dry-run`（只报缺向量数）/ 默认只补缺（幂等）/ `--rebuild`（换模型后清空重嵌）；refresh 路径的现场补缺是兜底，本脚本价值是把首次全库嵌入（4547 条 × CPU 百毫秒级）挪到运维窗口；
- **本轮补的用例**：新增 `tests/test_vectors.py`（13 条：provider 三态（none / 未知 fail loud / 懒加载零副作用）、依赖缺失报可读安装提示、维度门禁、向量文本协议精度、SQLite 五重防御、默认路径 explain 仍 tfidf_cosine（P4 行为不变）、**向量模式接管**（vec 分来自 `<=>`、explain 标 embedding_cosine、查询文本确被嵌入）、缺向量现场补嵌、**PG 集成 3 条（设 `JDA_TEST_PG_URL` 才跑，CI 无 PG 自动跳过）**：ensure_column 幂等 / upsert + `<=>` 顺序与距离精确 / NULL 向量行不参与）；测试 **388 → 398**（另 3 条 PG 集成本机真容器全过）；
- **真容器验证（2026-09-25）**：Docker Desktop 起 `pgvector/pgvector:pg15` → 0004 迁移在真 PG 补跑成功 → `ensure_column` 幂等 → 4547 条全库回填（`BAAI/bge-large-zh-v1.5`，1024 维，本机已有缓存；`BAAI/bge-m3` 下载被网络卡住，配置默认仍指向它）→ 全量重算；
- **实测（嵌入冒烟）**：中文简历查询 vs 同类中文 JD 嵌入余弦 **0.7306**（TF-IDF 0.2888）、vs 同类英文 JD **0.5874**（TF-IDF 0.2133）、vs 无关英文岗 **0.3691**（TF-IDF 0.0000）——跨语言信号大幅强于 TF-IDF；**已知特性**：神经嵌入的“无关分地板”更高（0.37 vs 0），排序区分度（0.59 − 0.37 = 0.22）够用，但绝对分进 `final = α·rule + β·vec` 时无关岗也会拿到 β×0.37——需要时再做校准（min-max/z-score，留作后续）；
- **依赖与配置**：`sentence-transformers`（含 torch 约 2GB）为**可选依赖**（requirements.txt 注释段，不装则 provider 必须保持 none）；`EMBEDDINGS_*` 五项进 `.env.example`；`psycopg2-binary` 本机装用于 PG 集成测试；
- **剩余（后续）**：`BAAI/bge-m3`（多语最强、默认目标模型）在本机网络下 2.27GB 下载未完成（HF 直连与 hf-mirror 均卡 0 字节）——先用同维度的 bge-large-zh-v1.5（英文效果打折）跑通全链路；bge-m3 到位后 `EMBEDDINGS_MODEL` 一改 + `--rebuild` 即切换；反馈数据积累与 `/insights` 权重历史卡片（第十八轮遗留）不变。

**上一轮改动明细（2026-09-25 第十八轮，§12.5 反馈回灌闭环上线）：**
- **新表 `match_weight_versions` + 迁移 0004**：被采纳的权重**版本化落库**（weights/source/note/样本量/基线与新 NDCG/重算行数），历史即审计轨迹；**落库而不写 `.env`**——权重是随反馈演进的数据，要可审计、可回滚；`.env` 是宿主机部署配置（改它要重启、不可回滚）；
- **新模块 `app/services/weights.py`（单一口径）**：`active_weights()` 是生效权重的唯一解析入口——**有采纳记录取最新一行，表空回退 `MATCH_W_*`**；打分（`match`）、质量报告（`insights.weights_snapshot(session)`）、调参基线（`tuning.search_weights`）三处都从它取，杜绝"看板显示 A、排序用 B"的漂移；`validate` 拒收一切不自洽组合（和 ≠1、越界、`beta ≠ 1-α`、非数字）；
- **打分链路接入**：`match.compute_match/_rebuild/refresh_matches*` 改为接收**生效权重**（缺省仍回退设置，旧调用不变）；采纳后自动全量重算 `match_scores`（采纳行记 `rematched` 行数）；
- **四个路由**：`GET /api/match-weights`（生效 + 历史）、`POST /api/match-weights/apply`（管理动作，权重不自洽 400）、`POST /api/match-weights/rollback`（回滚到上一版，无记录 400）、`POST /api/match-weights/auto-tune`（跑一次"反馈 → 调参 → 可选采纳"，**默认 dry-run**；管理动作）；
- **自动采纳（默认关）**：`WEIGHTS_AUTO_TUNE=true` 时 beat 每周一 06:00 跑 `auto_tune_weights_task`；**样本与增益双门槛**（样本 ≥ `WEIGHTS_AUTO_TUNE_MIN_SAMPLE` 默认 10、NDCG 提升 ≥ `WEIGHTS_AUTO_TUNE_MARGIN` 默认 0.02）——达不到就如实返回 `insufficient_sample / no_gain / gain_below_margin`，不"为了闭环而闭环"地换参数；调参报告（`/api/insights/tuning`）保持只读；
- **本轮补的用例**：新增 `tests/test_weights.py`（15 条：校验 8 组反例、设置回退、**采纳 → 全量重算按新权重生效**（B 职位 rule 0.35 → 1.0、final 按 α 融合）、回滚链路（两级回滚到设置）、**自动采纳全链路（增益 0.1835 ≥ 门槛 → source=auto → 三处同口径）**、增益不足/样本不足/dry-run 三种不换参数、接口生命周期与 step 越界、管理动作三档 401/403/200、beat 条目随开关注册、任务跳过与直调）；`test_insights.py` 权重快照断言同步（新增 `source` 字段）；测试 **373 → 388**；
- **测试逗出来的一个真缺陷（已修）**：`validate()` 丢弃了来源标注（恒返 manual），导致"看板/接口显示的 source"与实际采纳来源不符——已透传（`source` 不参与数学校验）。
- **剩余（非代码/后续）**：反馈数据积累（闭环有数据才能转起来）；`/insights` 看板的权重历史卡片（前端工程，可选）。

**上一轮改动明细（2026-09-25 第十七轮，§5.1 官网兜底适配器：规则化解析落地）：**
- **`OfficialSiteAdapter` 从骨架补为可用实现**（`app/adapters/official_site.py`，NeteaseAdapter 仍为其子类）：三种模式按可信度依次尝试（纯数据驱动、**不执行任何代码**）——① `json_ld`（默认自动识别）：读 `application/ld+json` 里的 `@type: JobPosting`（含 `@graph` 递归），SEO 驱动的官网大多挂这个，比猜 DOM 稳得多；② `json`（SPA 常见）：按 `script_id` 取内嵌 JSON（`__NEXT_DATA__` 一类）+ `json_path` 定位职位数组 + `fields` 声明字段映射；③ `regex`（最后手段，逐字段正则，**脆弱**—命不中就留空，不拿占位串当数据）；
- **规则放 `company.fetch_policy["html_rules"]`（JSON，纯数据）**而不是新加一列：它就是"怎么抓这个站"的采集策略，与 `interval_min` 同源；真要独立成列时再迁移（不为一次适配加迁移）；
- **合规前置**：`discover` 先拉 robots.txt 判断（不允许 → `RobotsDisallowed`，**不重试、不绕过**，连页面都不抓；robots 404/5xx 仍视为允许，与 P1 既有口径一致）；未配规则 → `OfficialSiteNotConfigured`（站点还没评估，不是崩溃）；两者都被 `collect_company` 的异常处理转成 `fetch_log.failed` 并附错误原因；
- **本轮补的用例**：新增 `tests/test_official_site.py`（19 条：robots 允许/禁止/404/网络异常、`html_to_text` 去 script/style 与实体还原、点号路径含列表下标、JSON-LD `@graph` 递归与畸形 JSON 容错、**JSON-LD 全链路（external_id 取自链接、薪资、城市、日期、描述文本化）**、相对链接补全、**`{page}` 翻页与 `max_pages` 封顶**、`next_page` 跟随、**内嵌 JSON 模式字段映射**、**正则模式单职位页**、**robots 不允许时只碰了 robots 没抓页面**、未配规则/页面无职位两种 NotConfigured、既有关联不变（netease 仍为子类））；测试 **354 → 373**；
- **测试逗出来的三个真缺陷（已修）**：① **JSON-LD 的 `description` 是实体转义串**，按真实 HTML 的"先剥后 unescape"会产出带标签的文本——拆成 `html_to_text`（真实 HTML）与 `escaped_to_text`（转义串）两个函数，在 normalize 里按 `raw_type` 选（与 §12.7 #4 的 greenhouse 顺序坑同源，这次是反向）；② `next_page` 模式默认只翻 1 页（默认值判定只看 `{page}` 占位）——改为"带占位或配了 next_page 都默认 5 页"；③ 未配规则时先去抓页面再报错——改为直接报错、不碰网络；
- **新增逐站评估工具 `scripts/probe_official_site.py`**：输入一个官网职位页 URL，输出 robots 判定、页面字节数、JSON-LD 块数与 JobPosting 数、带 id 的 script（SPA 线索）、首个职位的规范化字段样例；没结构化数据时直接给 `html_rules` 骨架供填。**实测（2026-09-25）**：`hr.163.com/job-list.html` → robots 允许但 **0 个 JSON-LD**（SPA + 自有接口，正是它走 NeteaseAdapter 的原因，工具如实给出"需要补规则或休眠"）；`job-boards.greenhouse.io/stripe/jobs/6606581` → robots 允许、**1 个 JobPosting**、title/url/datePosted/city/description 逐个对得上，并识别出 `__NEXT_DATA__` 脚本——证明 JSON-LD 路径在真实标记上可用；
- **剩余（非代码/后续）**：逐站评估后再把具体站点写进 `companies`（评估属人为决策，工具只提供证据）；数据加密/需登录的站点（大疆 Moka 一类）继续休眠，不绕反爬。

**上一轮改动明细（2026-09-25 第十六轮，§10 可观测性：结构化日志 + 指标 + Sentry）：**
- **新模块 `app/services/obs.py`（零依赖）**：Prometheus 文本格式手写、日志用标准库 `logging`，Sentry 为可选（**没装也不报错**——可观测性是增强项，不能变成启动依赖）；
- **结构化 JSON 日志**：`LOG_JSON=true` 时装 JSON 格式化器（默认关，开发期控制台保持可读），每行一个对象 `{ts,level,logger,msg,request_id,...}`，`extra` 字段平铺供 Loki/ES 直接检索；**异常只留类型不留堆栈正文**（堆栈里可能带用户数据）；
- **request_id 贯穿**：中间件回显 `X-Request-ID`（没有就生成 16 位），写入日志上下文并回写响应头——出问题时能按 id 串起一条链路；
- **`GET /metrics`**：HTTP 计数/耗时直方图 + 业务口径仪表。**HTTP 指标的 `path` 标签用路由模板**（`/api/resumes/{resume_id}`，未匹配归 `unmatched`）——用原始路径会让 `/api/resumes/1`、`/2`… 各成一条序列，基数随用户数据无限膨胀，Prometheus 迟早被撑爆；业务口径**复用 `ops.ops_report`**（采集成功率/队列积压/命中率）而不在抓取路径另算一套；5xx 也计入（`finally` 里记，异常路径不能在指标里消失）；
- **口径与安全**：抓取默认不强制凭据（Prometheus 通常无凭据），配了 `METRICS_TOKEN` 才要求 `?token=` 或 `Bearer`；抓取失败（DB/聚合异常）置 `jda_ops_available 0` 而不抬 500；指标文本与日志**不含密钥与个人信息**（有用例反向断言）；
- **Sentry**：`SENTRY_DSN` 非空且装了 `sentry-sdk` 才初始化，否则静默跳过并把状态落到 `jda_sentry_enabled`（可选依赖不写进 `requirements.txt`，文档说明 `pip install sentry-sdk`）；
- **本轮补的用例**：新增 `tests/test_obs.py`（16 条：JSON 格式与字段/异常只留类型/幂等装卸/按配置开关、日志仅在开启时输出、request_id 回显与生成、**按路由模板计数且未匹配归 unmatched**、直方图分桶与 `_count`/`_sum`、文本格式良构（值可解析）、/metrics 200 与 content-type、**令牌三态（无 token 401 / query token 200 / Bearer 200）**、`METRICS_ENABLED=false` 404、**业务口径仪表（分母含限流、Redis 不可用报 0 而非冒充健康）**、指标与日志不泄露密钥、Sentry 无 DSN/无 SDK 都不报错）；测试 **338 → 354**；
- **踩到的实现坑（已修）**：starlette 1.6 起**不再把 `route` 写进 `scope`**（只给 endpoint/path_params），原先读 `scope["route"]` 的做法会让所有请求都归到 `unmatched`——改为按官方 `route.matches(scope)` 自己匹配（FULL 优先、次选 PARTIAL）；这条已写进 `obs.route_template` docstring，免得后人再踩；
- **剩余（非代码/后续）**：外部告警通道（Alertmanager/钉钉告警）与 Prometheus 服务端部署；多进程部署时进程内指标需靠 Prometheus 服务发现聚合（本形态单进程）。

**上一轮改动明细（2026-09-25 第十五轮，§10 简历原件静态加密 + 隐私政策 v1.1）：**
- **新模块 `app/services/crypto.py`**：**AES-256-GCM**（认证加密：改一个字节就解不开）应用层加密，落盘格式 = 魔数 `JDAENC1` + 12 字节随机 nonce + 密文（tag 附尾）；密钥只从 `UPLOADS_KEY`（base64 的 32 字节）读，**不入库、不落日志、错误文案不回显**；**保留魔数是向后兼容的关键**——没有魔数的旧文件按明文透传，存量原件不必先迁移就能继续被删除/追溯；未配置密钥 = 旧行为（明文，prodcheck 会 warn `uploads_plaintext`），配了但非法（非 base64/长度不对）则**报错而不是静默用明文**；
- **接口埋点**：`POST /api/resumes` 在配了密钥时**密文落盘**，响应新增 `file_encrypted`；解析不受影响（用的是内存字节）；删除接口的磁盘联动对密文同样生效；
- **存量与追溯工具 `scripts/encrypt_uploads.py`**：默认把 `uploads_dir` 下存量明文就地加密（幂等，已是密文跳过）+ `--dry-run`；`--decrypt <文件>` 解单份到 `.plain` 供人工追溯（用完自删）；
- **隐私政策同步（§14 ①）**：应用层加密落地后，第四节「未加密」的披露句已改为**如实描述**（加密密钥只由部署环境变量提供、不入库；未配密钥的部署为明文，仅限本人使用与联调），中英同步改；政策版本 **1.0 → 1.1**（后端 `legal.POLICY_VERSION` 与前端 `legal-doc.js` 三点一线，改版即让原签署失效）；送审脚本的 5 条待拍板清单同步把「加密存储完成后再删句」改为「KMS 托管密钥属部署侧后续」；
- **依赖与审计**：新增依赖 `cryptography`（写进 `requirements.txt`，CI 会装）；`UPLOADS_KEY` 进 §14 ⑤ 的 `SECRET_FIELDS` 与轮换清单（泄露则存量原件全部可解，轮换需先重新加密存量）；新增 warn `uploads_plaintext`；`.env.example` 补 §10 段；
- **本轮补的用例**：新增 `tests/test_uploads_crypto.py`（14 条：密钥生成与非法格式报错、加解密往返、**明文字节不出现在密文里**、同明文两次加密不同（换 nonce）、加密幂等、**存量明文透传**、篡改/截断/错密钥拒收、错误文案不泄露密钥与正文、文件级加密与解密、**上传密文落盘（盘上读不到明文邮箱）+ 响应标记 + 删除联动**、未配密钥保持明文）；`test_prodcheck.py` +1（未配密钥持续 warn、配了不 warn）；测试 **323 → 338**；
- **踩到的一个真坑（已修）**：本机 `backend/.env` 配了真实 LLM key，新增测试文件若不做屏蔽，上传用例会**真连 LLM**（单用例 10s+ 且结果不确定）——已按 `test_resume_parse`/`test_legal` 的既有约定补上 autouse 夹具（新文件与 `test_auth.py` 均已补），全套回到 ~4s；
- **剩余（非代码/后续）**：托管密钥（KMS）与生产密钥轮换、存量原件批量加密属部署动作；法务复核签署需针对 v1.1 重签（`meta.legal_review` 尚未填写）。

**上一轮改动明细（2026-09-25 第十四轮，§4.3 账号体系：JWT 鉴权落地）：**
- **新模块 `app/services/auth.py`（零依赖）**：HS256 JWT 用标准库 `hmac`/`hashlib`/`base64`/`json` 自实现（**不引 PyJWT / passlib / bcrypt**，延续"纯 Python、离线可测"口径——HS256 的签名本来就是 HMAC-SHA256，自实现只多一层 base64 与 JSON 编解码）；口令散列 PBKDF2-SHA256 + 每用户随机盐、`pbkdf2_sha256$迭代$盐$散列` 自描述格式（日后换算法可平滑迁移）、比对走 `hmac.compare_digest`；`decode_token` 校验顺序固定为**结构 → 算法 → 签名 → 类型 → 过期**（先验签再读声明——顺序反了等于把校验交给攻击者），**只认 HS256**（`alg=none` 降级攻击直接拒收）；原因码 `malformed/bad_signature/expired/wrong_type/bad_subject` 供接口层回 401 文案，**不回显令牌/密钥原文**；
- **四条路由**：`POST /api/auth/register`（邮箱唯一 409、口令 ≥8 位与邮箱格式 422；**角色不接受客户端传入，自助注册一律 user**——否则等于开放自助提权）、`POST /api/auth/login`（失败一律 401 且文案不区分"未注册/口令错"；用户不存在时也跑一次**等开销散列**，响应时间不泄露邮箱是否注册）、`POST /api/auth/refresh`（只收 refresh 类型，access 当 refresh 用 401；签名有效但账号已删 401）、`GET /api/auth/me`（**恒需令牌**，与开关无关——此接口本就是登录态自检）；
- **鉴权依赖与开关**：`current_user` 依赖 + `settings.auth_required`（**默认 false**——`user_id` 仍可由表单/查询参数传入，旧客户端不被打死；置 true 后用户态接口缺令牌一律 401）；`_scope_user_id` 把**自述型** `user_id`（表单/查询/请求体）与令牌对齐（普通用户**静默以令牌为准**、admin 尊重显式传值可代操作——真参数是冒烟时撞出来的：前端仍带旧的 `user_id=1`，硬判 403 会让过渡期全面不可用）；`_owned` 给用户态资源做归属校验（**按 id 取他人简历/投递 → 403**）；`_require_admin` 守住公司映射增删改（登录则必须 admin）；**带了坏令牌不静默降级为匿名**——直接 401 并写明原因，否则用户会"莫名看不见自己的数据/看到别人的数据"；
- **覆盖范围**：简历（上传/列表/详情/激活/删除/画像修正/优化）、推荐、投递（创建/列表/状态/陪伴包/面试登记/帮填）、催进与面试催进、公司映射写操作——**越权拒绝落在服务端**，不靠前端"不传 user_id"；
- **生产审计接入（§14 ⑤）**：`AUTH_SECRET` 进 `SECRET_FIELDS`（出厂默认值含 `change-me` → **fail**）与密钥轮换清单（泄露即可伪造任意用户令牌，含 admin）；新增 warn `auth_not_required`（生产建议置 true）；`.env.example` 补 §4.3 段；
- **本轮补的用例**：新增 `tests/test_auth.py`（24 条：散列往返/畸形存储拒绝/等开销散列、令牌往返与 claims、**过期·篡改·错类型·结构错误·alg=none·非法 sub 六类拒收**、注册返回令牌对且角色恒 user、重复邮箱 409、口令与邮箱 422、登录成功与失败同文案、`me` 恒需令牌、refresh 只收 refresh 且拒已删账号、**开关开则 401 且坏令牌不静默降级**、**开关关则旧行为不变**、跨用户简历读/改/推荐/删/优化全 403 且列表只出自己、admin 可跨用户并可代操作、自述 `user_id` 被静默覆盖（旧默认值不报错）、投递记录越权读写 403、公司写操作三档 401/403/200）；`test_prodcheck.py` +2（默认签名密钥判 fail、开关未开 warn）；测试 **297 → 323**（§0 此前记的 296 未含 `93f74c6` 的 PG 时区回归用例，本轮一并校正）；
- **剩余（非代码/后续）**：前端登录页与令牌存储（`Authorization` 头接入）**已于第二十轮落地**（`frontend/app/auth.js` + `/login` + `auth-bar.js`，见 §0 第二十轮明细）；生产 `AUTH_SECRET` 轮换与 `AUTH_REQUIRED=true` 属部署动作（§14 ⑤ 清单已含）；`PATCH /applications/{id}` 仍为设计位。

**上一轮改动明细（2026-09-24 第十三轮，§14 ① 协议/隐私政策英文版与法务终稿对齐）：**
- **正文抽成单一事实源**：新增 `frontend/app/legal/legal-text.json`（`terms`/`privacy` × `zh`/`en`，小节形如 `{id, h, body:[{p}|{ul}]}`）——**页面渲染、送审稿、校验读的是同一份字符串**，改正文只改这一个文件，杜绝"页面一份、送审稿一份"两处漂移；`legal-doc.js` 只留渲染与版本号（`POLICY_VERSION` 仍与后端 `legal.py` 三点一线，契约断言不变），页面改为 `<LegalSections>`，不再内联小节标题；
- **英文版落地**（原 §14 ① 剩余项之一）：六节协议 + 六节隐私政策逐条对照翻译（含"原件文件同时删除 = the original uploaded resume file is deleted at the same time"这条与 `DELETE /api/resumes` 实际行为对应的关键承诺）；行内强调用「纯文本 + `**粗体**`」存放、渲染时切分（不引 markdown 解析器），**送审稿与页面共用同一个字符串**；
- **语言码映射（自测抓到的真 bug）**：cookie 语言码是 `zh-CN`/`en`，正文 JSON 是 `zh`/`en`——直接 `doc[lang]` 取不到 key **不报错、只是页面没正文**（中文用户看到空白卡片），故 `textLang()` 显式映射并回退中文，映射本身也进了契约断言；
- **效力声明口径化**：`disclaimer` + `meta.authoritative_lang`（当前 `zh`：**中文版本为解释依据、英文为参考译文**）写在 JSON 里，页面按语言取用，脚本校验"声明与 authoritative_lang 不能互相打脸"；原 i18n 里那句"英文版本与法务终稿属待办（非代码）"已删除（英文版已落地）；
- **强制披露三节机器可读**：后端新增 `legal.REQUIRED_SECTION_IDS = ["authorization","minimal","deletion"]`（与给人看的 `REQUIRED_SECTIONS` 中文节名并存）并随 `GET /api/legal/policies` 透出——中英两套正文都必须含这三节，断言落在正文节名上而非页面注释（**顺带修掉旧用例的一处假绿**：旧断言"个人信息删除 in privacy/page.js"其实是被页面头注释满足的，正文里从未出现该串，现改为断正文节名「个人信息的删除与撤回」）；
- **送审出口 `scripts/export_legal_text.py`**：`--lang zh|en|both`（默认中英对照）+ `--out` 落盘；校验结构（中英小节 id/顺序一致、块类型与列表条数一一对应、每节字数下限、英文**不得残留中文**、无 TODO/待补 占位、效力声明与 `authoritative_lang` 一致、前端版本 = 后端版本）→ 结构问题 **exit 2**；报签署状态（`meta.legal_review.signed_version/reviewer/reviewed_at` 齐备且签署版本 = 当前版本才算闭环）→ 未闭环 **exit 1**（同 `audit_compliance.py` 口径：**签署属非代码事项，不阻断合入，但要让脚本一直说话**）；
- **法务终稿对齐的口径**：`legal_review.signed_version` 与政策版本绑定——**改版会让原签署失效**（脚本直接报"须重新复核"），避免"签过一版、后面偷偷改正文"；送审稿固定打印 5 条待法务/运营拍板事项（运营主体与联系方式、适用法律与争议解决、个人信息保存期限、英文版效力、加密存储落地后删披露句）；
- **本轮补的用例**：`test_legal.py` 前端契约由"页面文本包含"改为"正文 JSON 结构契约"——中英小节/段落/列表一一对应、强制三节中英齐备且落在指定文档（授权在协议、删除与最小必要在隐私政策）、**英文小节不得残留中文且每节 ≥40 字**（漏译最典型的形态就是少一段或黏着中文）、无占位词、效力声明与 `authoritative_lang` 同口径、两页共用正文模块 + 语言码映射存在、载荷透出 `required_section_ids`；测试 291 → **296**；
- **剩余条件（非代码）**：法务复核签署（`meta.legal_review` 三个字段填齐即闭环）、运营主体名称与联系方式落终稿、适用法律/保存期限条款、英文版法律效力（是否改为中英同等效力）。

**上一轮改动明细（2026-09-24 第十二轮，§14 上线前 checklist 五项）：**
- **① 用户协议 + 隐私政策**：后端 `app/services/legal.py` 是政策版本的**单一事实源**（`POLICY_VERSION="1.0"` / `POLICY_EFFECTIVE_DATE="2026-09-24"` + 文档清单 + `REQUIRED_SECTIONS` 必含「授权范围/个人信息删除/最小必要」），前端静态页 `frontend/app/legal/{terms,privacy}/page.js` + 外壳 `legal-doc.js`（读 cookie 出语言、显示版本行「版本 v{v} ｜ 生效日 {d}」、两页交叉链接）；`GET /api/legal/policies` 回版本 + 投递告知文案，供前端与留痕比对；**顺带关掉 §10 挂账的"删库不删盘"缺口**——`DELETE /api/resumes/{id}` 现在连带删磁盘原件（`main._remove_upload_file`，`Path.resolve()` + `relative_to(uploads_dir)` 归属校验，**越界一律不删**），响应加 `file_removed`；
- **② 投递授权钩子 + 告知文案**：新增列 `applications.authorized_at`（同意时间）+ `consent_version`（所同意的政策版本），迁移 `alembic/versions/0003_apply_consent.py`（可空——迁移前的历史行不回填假数据，如实为"未知"）；`POST /api/applications` 接受 `consent_version`，**与当前版本不符 → 400**、未带则服务端补记当前版本（老客户端不被打死）；前端 `app/page.js` 把「标记已投递」改成**两段式**：先弹告知面板（要点列表 + 勾选框 + 协议链接，`FALLBACK_NOTICE` 保证后端取不到告知时也必须有告知），勾选后才 POST（`authorized: agreed` + `consent_version`），**旧文案"点击即视为用户确认授权投递"已删除**（"默认勾选 = 同意"不成立）；
- **③ 采集 robots/terms 合规审计表**：`app/services/compliance.py` 逐公司审计行（`robots` ∈ allow/no_robots_fallback/not_applicable/halted；`terms` ∈ public_api_ok/terms_silent/halted；`decision` ∈ collect/dormant + 证据 + 复核日），`GET /api/compliance/audit` 做"库内公司 × 审计表"覆盖比对——**未有审计行的公司单列 `unreviewed`**（新增公司先补审计行再开采集，§5.2 合规前置靠接口/用例兜住而不是靠人记）；`scripts/audit_compliance.py --markdown` 直接产出下表；
- **④ 监控看板**：`app/services/ops.py` + `GET /api/insights/ops?hours=&k=`，三块口径——采集成功率（**分母含 rate_limited/robots_blocked，剔除会让成功率虚高**）、任务积压（Redis `llen("celery")`；**Redis 不可用回 `available:false` 而不是拿 0 冒充健康**，另给不依赖 Redis 的 pending/超期投递业务口径）、命中率基线（复用 `insights.build_quality_report`）；阈值判定收敛成 `alerts` 列表（全绿时为空，告警不许长鸣）；前端 `/insights` 新增运维卡片（窗口 24h/7d 切换 + 7 项指标 + 告警与提示）；
- **⑤ `.env` 密钥轮换与生产配置审计**：`app/services/prodcheck.py` 纯函数审计（配置以 Settings/dict 传入，**单测不必改本机 `.env`**）——`fail`：密钥命中占位词或短于 16 位、声明了 SMTP/IM 却缺必填项、`.env` 被 git 跟踪；`warn`：仍在 SQLite、未启用 Redis、采集 UA 还留着默认示例邮箱、SMTP 缺收发件人、`.env.example` 漏列密钥项；另给 6 项**密钥轮换清单**（人工在服务商侧执行，代码代不了）。`scripts/audit_production_config.py` 读真实 Settings + `git ls-files --error-unmatch` 出结论，有 `fail` 则 **exit 1**。`.env.example` 补齐密钥段（LLM/OCR/SMTP/IM/UA + 生产库与 broker 提示）；
- **安全底线**：审计**只判密钥长度与占位词，不打印、不返回原文**（有用例断言报告 JSON 里不含密钥值）；未填写的密钥**不算 fail**（留空 = 该功能未启用，否则不配 OCR 就上不了线）；
- **本轮补的用例**：新增 `tests/test_legal.py`（11 条：政策载荷与必含章节、`/api/legal/policies`、**前端两页存在且含必含章节**、**前后端政策版本一致**（三点一线可核验）、首页链接协议页、投递前告知顺序与旧文案已删、`consent_version` 不符 400、同意留痕落库、删简历连带删盘/越界拒删/无文件也 OK）、`tests/test_compliance.py`（8 条：seed 公司必有审计行、审计行结构自检、非法枚举与缺字段被点名、dji 休眠有原因、netease 记 no_robots 回退、库内公司覆盖比对与缺口单列、Markdown 表渲染、审计接口）、`tests/test_ops.py`（20 条：限流计入分母、窗口外不计、无尝试不给结论、队列不可用不冒充 0、`llen` 读取与读失败降级、pending/超期计数、命中率样本不足/低于下限/正常、告警汇总与全绿静默、接口 200 与窗口/k、越界 422、**前端运维卡片与中英词典 key 契约**）、`tests/test_prodcheck.py`（15 条：生产化配置 ok、Settings 入参、SQLite/Redis 仅 warn、空密钥不算 fail、占位与过短 fail、**报告不回显密钥原文**、SMTP/IM 半配置 fail、默认 UA warn、`.env` 被跟踪 fail 且排序在前、示例文件漏列密钥项 warn、**仓库 .env.example 与 .gitignore 契约**、轮换清单覆盖）；测试 237 → **291**；
- **剩余条件（非代码）**：协议/隐私政策的**英文版本与法务终稿**（英文版已于第十三轮落地，见上；仅剩法务复核签署）、投递告知文案与产品 PRD 的正式对齐、逐公司审计结论的**法务复核签署**、简历原件**加密存储（KMS/应用层 AES）**、JWT 鉴权（§4.3）、结构化日志 + Sentry + Prometheus 指标（§10）、生产密钥在服务商侧的**实际轮换操作**、Docker/PG 起来后的存量回填（采集补 description → `extract_job_skills.py` → 重算 match_scores）。

**上一轮改动明细（2026-09-24 第十一轮，§12.6 P5 ⑤ 招聘季适配 + 全球多语言看板）：**
- **新模块 `app/services/seasonality.py`**：`GET /api/insights/seasonality?region=&lang=&min_sample=`——把多年 `jobs.publish_date` 投影到 1~12 月**叠加**，给出月份/季度分布 + 旺季/淡季判定 + **当前所处季节与行动建议**；region（`all|cn|overseas`）复用 `market.region_of`，与 §4.2 同源；
- **口径硬约束（关键取舍）**：季节**只认 `publish_date`（真实发布日），绝不回退 `created_at`**——`created_at` 是采集时间，会聚集到采集当月，跨年叠加后等于把"我们什么时候采的"说成"市场什么时候招的"（`market.py` 的趋势块回退 `created_at` 是**另一回事**：趋势看的是"库内新增节奏"，季节看的是"市场发布节奏"，故这里刻意不回退，并在 `notes` 里写明）；
- **旺淡季判定**：以月均计数为基准，`≥ 月均 × 1.2` 记 `peak`、`≤ 月均 × 0.8` 记 `off`、其余 `shoulder`（阈值挂 `PEAK_RATIO`/`OFF_RATIO` 常量）；**样本下限 `MIN_DATED_JOBS = 12`**（一年 12 个月，月均不足 1 条时给任何月份贴冷热标签都是噪声）——不达标时 `season` 全为 `null`、只回分布 + 显式说明；`years_covered < 2` 时另加"信号仅供参考"提示；
- **新模块 `app/services/i18n.py`（多语言的最小可用形态）**：语言码事实来源 `SUPPORTED_LANGS = ("zh-CN", "en")`，提供 `labels()`/`month_label()`/`quarter_label()`/`resolve_lang()`；**只翻译枚举与固定提示语**（区域名、季节标签、月份/季度名、建议语），报告主体（数值与结构）语言无关——前端在任何语言下消费同一份数据；未知语言在服务层**回退默认而不抛异常**（`resolve_lang`），API 层才用 `SUPPORTED_LANGS` 显式 422（与 `region` 校验同风格）；
- **前端多语言看板**：`frontend/app/i18n.js`（中英词典 + `t(lang, key, vars)` 含 `{name}` 插值 + `readLangCookie`/`langCookieValue`；**不引 i18n 框架、不做 `navigator.language` 自动协商**——语言存 cookie，链接能"钉住"语言）；`app/lang-switch.js`（客户端开关，写 cookie 后**整页刷新**：`<html lang>` 与全部文案都由服务端按 cookie 渲染，只切客户端 state 会出现"文案英文、lang 属性中文"的错配）；`app/layout.js` 改异步 **读 cookie 输出 `<html lang={lang}>`** 并挂载开关；`app/insights/page.js` 全部文案词典化 + 新增**招聘季画像卡片**（区域切换 `all|cn|overseas` + 语言跟随页面，柱状分布图旺季绿/淡季灰、季度汇总、当前季节与建议、后端 `notes` 原样透出）；
- **构建影响**：读 cookie 使 `/` 与 `/insights` 从预渲染转为**按请求渲染**（`ƒ`）——本项目页面数据全部来自 `/api`，没有可复用的静态产物，无实际代价；`/manifest.webmanifest` 仍为静态 `○`；
- **本轮补的用例**：新增 `tests/test_seasonality.py`（9 条：标签与语言回退、旺淡季阈值、**"只认 publish_date 不回退 created_at"**、样本不足不给结论、月份/季度分布与零值补齐、当前季节与中英标签、单年/跨年年份提示、region 过滤与标签、接口 200/422）；新增 `tests/test_web_i18n.py`（6 条前端**内容契约**：中英词典 key 完全一致（漏翻译 `next build` 不报错，只会静默显示 key）、前端语言码与后端 `SUPPORTED_LANGS` 一致、未知语言回退且代码不引用 `navigator.language`、layout 读 cookie 输出 `<html lang>`、开关写 cookie 后刷新、看板消费 seasonality 接口且样式齐备）；测试 222 → **237**；
- **剩余条件（非代码）**：行业细分季节（需行业标签）、旺季到点订阅提醒（推送渠道与订阅名单属运营配置，可复用 `notify.py`）；第三语言的翻译产出需人工校对。

**上一轮改动明细（2026-09-24 第十轮，§12.6 P5 ④ 移动端 / PWA）：**
- **PWA 清单**：`frontend/app/manifest.js`（Next App Router 元数据路由，构建后 Next 自动注入 `<link rel="manifest">`，产物路由 `/manifest.webmanifest`）——`display: standalone` + `theme_color`/`background_color` + `lang`/`orientation` + 图标；
- **Service Worker（`frontend/public/sw.js`）**：安装预缓存应用外壳（`/`、`/insights`、图标、manifest）；导航请求 **network-first**（离线回退缓存外壳）；同源 `/_next/static` 与外壳 **stale-while-revalidate**；**`/api/` 一律不缓存**——职位/推荐/简历是强时效 + 隐私数据，缓存会造成陈旧结果与跨用户泄露；
- **注册与视口**：`frontend/app/sw-register.js`（客户端组件，`"serviceWorker" in navigator` 能力探测后才注册，失败静默——PWA 是增强项，隐私模式/旧浏览器/非 localhost 都会拒绝）；`app/layout.js` 新增 `viewport`（`device-width` + `viewportFit: cover` + `themeColor`）与 `appleWebApp`（iOS 加主屏——Safari 不读 manifest 的 `display`，只认这个 meta）；**不锁 `user-scalable`**（无障碍）；
- **响应式**：`app/globals.css` 加 `@media (max-width: 640px)`——收窄留白、职位头（标题 + 分数）窄屏改纵向排列、**触控目标 ≥44px**（iOS HIG / Material 最小点击区）、底部留 iPhone 安全区（`env(safe-area-inset-bottom)`）；另加 `html { -webkit-text-size-adjust: 100% }`；
- **本轮补的用例**：新增 `tests/test_pwa_assets.py`（6 条**内容契约**校验：manifest 可安装字段与 standalone、图标为 SVG 且带 viewBox、SW 三钩子齐备 + `/api/` 绝不缓存 + 导航回退 + 静态 SWR、layout 的 viewport/appleWebApp/SW 注册、响应式媒体查询与安全区/44px、注册组件为 `"use client"` 且能力探测 + 静默失败）；`next build` 通过（新增 `/manifest.webmanifest` 路由）；测试 216 → **222**；
- **剩余条件（非代码）**：位图 192/512 安装图标（含 maskable 版本）需设计产出；"可安装 / 离线可用"的真机行为需浏览器环境（Lighthouse / 手动装到主屏）验证；**微信小程序为独立工程**（小程序账号 / 域名备案 / 独立前端，非本仓库改造项）。

**上一轮改动明细（2026-09-24 第九轮，§12.6 P5 ③ AI 简历优化）：**
- **新模块 `app/services/optimize.py`**：`POST /api/resumes/{id}/optimize`（body `{job_id, use_llm=false}`）对照目标 JD 出「逐条差距 + 改写建议」——**默认路径完全离线**（规则推导，零 LLM、零新依赖）。差距拆五个可比口径、**只列未被满足的项**：① `skill`（职位技能逐项，两侧小写，与 `match._skill_hit` 同源，缺失项各列一条）；② `experience`（`experience_years` vs `experience_min`，**口径与 `match._exp_fit` 逐档对齐**：无要求不算差距、差 1~2 年 partial、更多 miss）；③ `education`（`edu_degree` 与 `degree_req` 等级序 phd4>master3>bachelor2>associate1，未知 unknown、低于要求 miss、`na`/空不算）；④ `city`（复用 `city.city_match_variants`，与 `match._city_fit` 同源；命中/远程不算，**跨区按 §12.7 #9 C 中性口径跳过不误报**，同区不同城市仍 miss）；⑤ `keyword`（JD 硬关键词覆盖率）。每项带 `kind/status/title/detail/advice`；
- **关键词口径（关键取舍）**：只取 **ASCII 硬词**（以字母开头、长度 ≥3、去英文虚词/招聘话术停用词），**不用 CJK 2-gram**——中文 n-gram 会产出"负责/要求/经验"这类噪音，而技术栈几乎都是 ASCII；`missing` 按 JD 词频降序列前 15 个，**只提示不诱导**（advice 明说"堆砌无经历的关键词会在面试追问中暴露"）；
- **诚实口径（硬约束）**：`notes` 显式声明"差距与建议属规则比对的改进提示，**不替用户编造任何经历**"；缺口技能给"若未接触过：**不要写进简历**，先补基础并如实说明正在学习"；用例**反向断言**建议里不出现"编造/虚构"措辞；
- **LLM 兜底（默认关，`use_llm=false`）**：开启且 `llm_api_key` 已配置时复用 `llm_extract._build_client`（同一套 base_url/model/timeout）额外产 `summary` + `suggestions`；**未配置 / 调用失败都不抛异常**，只在 `llm.status` 如实标注（`not_configured` / `failed` + `error`），规则结果照常返回——"开了开关但没配 key"不能变成 500（`_llm_block` 兜底）；
- **接口**：`POST /api/resumes/{id}/optimize`（简历/职位不存在 404；缺 `job_id` 422）；
- **本轮补的用例**：新增 `tests/test_optimize.py`（14 条：学历序、技能差距与命中/中性口径、经验 partial·miss·unknown 与"满足/无要求不列"、学历 miss·unknown 与 na 不列、城市命中/远程/跨区跳过/同区 miss/未填 unknown、关键词覆盖与 missing、JD 无硬词与无正文/无 skills 的显式提示、诚实口径反向断言、LLM 默认关 + 未配 key + 失败降级 + 成功路径、接口 200/404/422）；测试 202 → **216**；
- **剩余条件（非代码）**：LLM 兜底的模型选择与配额属配置项（`llm_*`）；简历版本管理与 PDF 导出属前端工程——**差距比对 / 改写建议 / 关键词覆盖 / 接口均离线可测**。

**上一轮改动明细（2026-09-24 第八轮，§12.6 P5 ② 面试陪伴闭环）：**
- **新列 `applications.interview_at`（可空）**：面试时间由**用户自己登记**（平台不去 ATS 猜），登记后才有定时催进；增量迁移 `alembic/versions/0002_interview_at.py`（`op.add_column`，down_revision=0001_init）——**不是**改 0001（存量库 stamp 过 head，改历史迁移会漂移）；
- **新模块 `app/services/interview.py`**：三件事全**离线可算**（规则推导，零 LLM、零新依赖）——① 准备清单 `checklist`（对齐 JD 的简历版本 / 量化项目复盘 / 缺口速通 / 面试形式与时间确认 / 3 个反问；英文职位追加英文自介项、有城市追加地点项，每项带 `done` 供前端打勾）；② 技能考察点 `skill_points`（按职位技能逐个给口径：命中 → 准备量化案例串讲；未命中 → 补基础 + 准备「可迁移经验 + 正在学习」的**诚实口径**，**明确不教用户谎报技能**）；③ 公司背景包 `company_pack`（**只用库内公开招聘信息**：在库 active 职位数、ATS 来源、官网、职位城市与发布日期，不拼外部未公开情报）；
- **诚实口径贯穿**：`notes` 显式声明"清单属准备提示而非面试预测""背景包不含未公开情报"；职位**无 skills 标签时显式提示**（避免前端把空清单当成"无需准备"），未登记面试时间也显式提示登记入口；
- **催进窗口设计（`scan_interview_reminders`）**：`status=interview` + 已登记时间 + 落在 `[now-7天, now+within_days(默认2天)]`；已过时间的标 `overdue=True` 提示"补记结果"；**超 7 天宽限不再提醒**——否则用户迟迟不更新终态会变成永久打扰；按 `interview_at` 升序（最紧的排前面）；
- **`notify.py` 通用化**（小幅重构，不改既有行为）：抽出 `send_mail(subject, body)`（SMTP 细节唯一处，`send_reminder_mail` 委托它）、抽出 `_post_im(payload)`（加签 + 错误码判定，两类提醒共用）、新增 `send_im_text(content)`（wecom→markdown / 其他→text）；面试催进直接复用，**不复制第二套 SMTP/webhook 代码**；
- **Celery beat**：新增 `interview_reminder_task` + `"interview-reminders-daily"`（每日 **09:30**，与投递催进 09:00 **分时**，避免两类提醒挤同一封/同一条）；邮件/IM 均未配置时降级逐条 `print("[interview] ...")`，不抛异常；
- **接口**：`GET /api/applications/{id}/interview-kit`（404 if missing）、`PUT /api/applications/{id}/interview-at`（Pydantic 校验非法时间 422；终态申请 400，`_SETTABLE_STATUSES = submitted/under_review/interview`）、`GET /api/interview-reminders?user_id=&within_days=`（`Query(0,30)` 越界 422）；
- **时区坑**：SQLite 存 naive、PG 可能回 aware，两者相减会 `TypeError` —— `_as_naive` 统一成 naive UTC 后再算 `days_left`；
- **本轮补的用例**：新增 `tests/test_interview.py`（9 条：`days_left` 纯函数、陪伴包结构/缺口/背景包只数 active、无缺口时速通项自动 done、英文职位追加语言项、无 job/resume 的降级与显式提示、终态拒登记、催进扫描窗口（含 overdue 与 7 天宽限）/用户过滤、3 个路由端到端（含 404/400/422）、beat 任务 `.run()` 直调降级日志）；`test_celery_config.py` 补 beat 条目与任务注册断言；测试 193 → **202**；
- **剩余条件（非代码）**：公司深度资料（融资轮次/新闻）需外部数据源；日程双向同步需日历 OAuth 授权；**陪伴包生成 / 催进扫描 / 接口 / beat 任务均离线可测**。

**上一轮改动明细（2026-09-24 第七轮，§12.6 P5 ① 市场洞察报告）：**
- **新模块 `app/services/market.py`**：城市 / 技能 / 薪资 / 发布趋势四块纯聚合——数据源只有 `jobs` 的 active 快照，零新表、零外部依赖。城市取 `city_keys` 首段（采集侧已规范化多城市与别名）回退 `city` 首个分隔段；薪资**只统计 min/max 齐全的职位**并按币种分组给中位数与四分位；趋势按 `publish_date` 月份（缺失回退 `created_at`）并在窗口内补齐零值月份（前端可直接画折线）；
- **脱敏（B 端交付的前置条件）**：分桶计数 < `min_sample`（默认 3）一律**不单独输出**，只累加进 `anonymity.suppressed_buckets`——小样本分桶（如"某城市 1 条"）配合已知信息可反推到具体公司/个人，故按 k-匿名下限截断；报告全篇不含公司名/职位标题/申请链接，天然满足"B 端脱敏数据"形态；
- **region 口径复用 §4.2**：`market.region_of` 懒导入 `main.DOMESTIC_SOURCES`（避免 main ↔ services 模块级循环），`source IS NULL` 归海外，与 `/api/recommend?region=` 同源，不新造第二套分区判定；
- **接口**：新增 `GET /api/insights/market`（`region` 非法 422；`top`/`min_sample`/`months` 由 `Query(ge=,le=)` 校验，越界 422）；
- **本轮补的用例**：新增 `tests/test_market.py`（7 条：中位数/分位/首城解析纯函数、`region_of` 与 §4.2 同源含 NULL 归海外、四维聚合与趋势零值补齐、k-匿名截断（单条城市与小样本技能桶都不出现）、薪资组样本 < min_sample 整体丢弃、region 过滤、API 200 与 region/top 越界 422）；测试 186 → **193**；
- **dev.db 实测（4617 条 active）**：城市 top 国内「杭州 1364 / 广州 638 / 上海 327 / 北京 192」、海外「New York 119 / Menlo Park 63 / Singapore 61」；技能 top「python 379 / sql 215 / backend 206 / go 205 / machine learning 176」（908 条有 skills）；`with_salary = 0` —— **现有 ATS 源（含网易）均不提供薪资字段**，薪资块在接入薪资源前为空属预期；趋势近月聚集（2026-09 计 2173）是 `publish_date` 大面积缺失回退 `created_at` 的结果，已在 `notes` 显式声明；k-匿名截断 273 个分桶；
- **剩余条件（非代码）**：C 端订阅的推送渠道与频次（可复用 `notify.py` 邮件/IM 通道）、B 端数据交付（合同/脱敏协议/导出格式）——**报告生成与脱敏口径本身已离线可测**。

**上一轮改动明细（2026-09-24 第六轮，§12.7 #9 遗留 C：跨语言/跨区匹配口径）：**
- **先量化、修正错误归因**：用 dev.db 真实简历（id=3，8 项 skills / role 后端开发专家 / cities 杭州·上海）对全量 active 职位按区汇总四分项原始分，结论与此前文档写法相反——**海外失分几乎全部来自 `city`，不是"exp/degree 字段 100% 缺失"**：`exp_fit` 在 `min_exp is None` 时返回 **1.0（最优分）**（海外均值 1.000、0 分占比 0.00），`degree_req` **根本没参与打分**（仅存在于 models/adapters）。真实差距：`city` 海外 0.073（0 分占比 0.90）vs 国内 0.643 → 0.2 权重 × 0.57 ≈ **0.114**，与规则分总差 0.110 吻合；`role` 两侧都趋近 0（0 分占比 0.99）；
- **跨区城市改中性（`match._city_region` / `_cross_region`）**：城市串含 CJK 判国内、全非中文判海外（与 `main.DOMESTIC_SOURCES` 的事实基础一致，不依赖 PG 正则、SQLite 可用）；简历意向城市与职位城市**分属两区 → 0.5 中性 + note `cross_region_neutral`**（新增 explain 口径）——理由是简历里的城市列表只表达"国内意向"，对海外岗位属"不可比"，与既有"无法判断不惩罚"（skill 两侧为空 0.5、city 未知 0.5）同源；**同区内不同城市（杭州 vs 乌鲁木齐）仍是 0**，不放松明确不匹配；
- **role 中英双向同义词（`_ROLE_SYNONYMS` 扩容到 16 组 + `_role_candidates` 反查）**：中文 target_role 能命中英文 title（后端→backend/server），英文 target_role 也能**反查中文核心词**命中中文 title（backend→后端）；`_ROLE_GENERIC_SUFFIXES` 补 ASCII 后缀（engineer/developer/…），并修掉"剥英文后缀留下尾空格导致反查失败"的坑（`"backend engineer" → "backend "`）；
- **语义层跨语言术语桥（`semantic._CROSS_LINGUAL` + `cross_lingual_tokens`，零依赖）**：CJK 2-gram 命不中 ASCII 词，是"中文简历 vs 英文 JD 语义分恒为 0"的直接原因；在 `tokenize` 里叠一张中英岗位术语表做**双向投影**（中文出现"后端/算法/数据"→ 补 backend/algorithm/data；英文出现 backend → 补"后端"），ASCII 侧按词边界匹配，避免 `web ⊂ websites`、`qa ⊂ qatar` 一类子串误判。**显式取舍：不引入神经多语模型**（BGE-M3 等）——本模块的立身之本是"纯 Python、零依赖、离线可测"，多语向量模型留给 §7 二期 PGVector，长尾技术术语靠已归一的标准技能标签兜底；
- **实测效果（dev.db 4617 条 active，同一份简历）**：海外 `city` 均值 0.073 → **0.525**（0 分占比 0.90 → 0.00）、`rule` 0.4027 → **0.4932**（国内 0.5131，基本齐平）、跨区规则分差距 0.110 → **0.020**；**海外最高名次 25 → 7**，且新晋海外第一是真正相关的 `Backend Engineer, Payments Experiences`（原第一是靠 remote 0.8 + 中性分蹭上来的 `Business Partner Analyst`）——说明改的是排序**质量**而非单纯抬分。语义侧海外非零相似（>0.01）职位 69/1988 → **144/1988**（均值 0.0013 → 0.0020），国内持平（0.0201 → 0.0205）；
- **本轮补的用例**：`test_match_recommend.py` +2（跨区中性含反向对称与"同区不同城市仍 0"、role 中英双向含 ASCII 后缀剥净与英文↔英文）；`test_semantic.py` +2（术语桥双向补词与词边界、中文简历 vs 英文 JD 语义分不再为 0 且相关 > 无关）；测试 182 → **186**。

**上一轮改动明细（2026-09-24 第五轮，§12.7 #7 采集入库抽标签）：**
- **新模块 `app/pipelines/job_tags.py`**：`tag_from_dictionary`（词典扫描，零成本）+ `llm_extract_job_skills`（职位侧 prompt，独立于简历侧）+ `tag_jobs` 编排。**顺序固定为"词典优先、LLM 兜底"**——否则 LLM 会去处理"本来就该免费命中"的职位，成本白白翻倍；
- **职位侧 prompt 与简历侧分开**：JD 全是招聘方话术，必须显式排除公司名/福利（五险一金、弹性工作）/软性形容词（沟通能力强、抗压）/职级与年限，否则这些会当技能写进 `skills` 污染技能匹配；实测（mimo-v2.5-pro，一条真实 greenhouse JD）抽出 `Python/SQL/PySpark/Pandas/Log analysis/Network security/Digital forensics/Incident response/Fraud detection/Threat intelligence`，无福利与软性词；
- **接入点与成本控制**：`collect.collect_company` 在**职位与 fetch_log 提交之后**才抽标签（LLM 慢或挂都不影响本轮入库结果，且单条失败只计 `llm_failed` 留空待补）；只处理"本轮采集到 + `skills` 为空 + description 非空"的职位（增量、不重复花钱）；`job_tag_llm`（默认关，需 `llm_api_key`）+ `job_tag_llm_cap=50` 限每轮 LLM 条数，超出的计 `llm_skipped`；结果里带 `tagged: {dict, llm, llm_failed, llm_skipped}` 便于观测；补出新标签同样触发 match_scores 全量重算；
- **与既有脚本分工**：入库抽标签让**新采集的轮次自己补齐**（dev.db 实测 robinhood 一轮 161 条里词典命中 99 条、`expired: 0`），`scripts/extract_job_skills.py` 退化为**存量补齐**工具（PG 那 4547 条历史数据仍需它回填）；
- **词典口径唯一**：`job_tags` 与采集侧共用 `build_alias_map` + `canonicalize_skills`（局部导入打破 `collect ↔ job_tags` 循环依赖），不存在两套归一；
- **本轮补的用例**：新增 `tests/test_job_tags.py`（11 条：词典命中 title/description、词典命中即不碰 LLM、LLM 兜底过 canonicalize、cap 上限与 `llm_skipped` 计数、无 key 时不发请求、单条失败不影响其它、无 description 跳过、已有标签不重复处理、共用别名表、`collect_company` 端到端接入、默认不调 LLM）；测试 171 → **182**。

**上一轮改动明细（2026-09-24 第四轮，§12.7 #6 无 description 职位修根因）：**
- **greenhouse 补齐全文（根因修复）**：`GreenhouseAdapter.discover` 请求 boards-api 时带上 `content=true`，列表接口直接返回职位全文（**实体转义的 HTML**）→ `normalize_raw` 落 `description`——此前不带该参数，greenhouse 全量 800 条无 description，语义分为 0、排名被系统性压低；
- **`content_to_text` 顺序坑**：`&lt;div&gt;` 型文本必须**先 `html.unescape` 再剥标签**——顺序反了正则找不到任何真实标签，整段 HTML 会原样入库；`smartrecruiters.html_to_text` 是"先剥后 unescape"，只适用于未转义场景，**不能复用**；
- **顺带修掉采集侧 3 个真实缺陷**：① `cleanup_stale` 前缺 `session.flush()`——`autoflush=False` 会话里随后的 SELECT 读不到本轮刚写的 `updated_at`，把"在源里、只是上次采集很久以前"的职位**整批**误判 expired（实测 greenhouse 重采后 `expired: 647 / 153`、800 条全变 expired）；② upsert `else` 分支未复位 status——`NormalizedJob.to_row()` 刻意不含 status，被误杀/过期的职位即使重新出现在源里也**永远**停在 expired（现在语义明确为"出现在本轮结果里即为在架"，主动复活为 active）；③ `\bgo\b` 在英文 JD 散文里大面积误命中——实测 greenhouse 853 条 JD 中 307 条被标 go，仅 ~94 条真有 Go 语言信号（`go-to-market` / `go live` / `go beyond` 是主因）→ `rules._AMBIGUOUS_ASCII` 改为"首字母大写出现"才算命中（负向断言排除 Go-to / Go live）；这条是启用 `content=true` 的连带修复——英文正文**首次**进入词典扫描；
- **词典补标签（零 LLM 成本）**：`scripts/extract_job_skills.py` 在守卫修复后重跑（并定向清理了修复前写入的 288 条伪 go）——dev.db 实测 active 4600 条中 908 条有 skills、**greenhouse 853 条全部有 description（391 条补上技能标签）**；剩余未命中集中在**非技术岗**（Account Executive / 文案策划 / HRSSC），根因是内置别名表仅 25 个 canonical 词——这正是 #7（LLM 抽标签）要解决的覆盖率天花板；
- **本轮补的用例**：`test_collect.py` 新增 2 条回归（"轮次内刷新过的职位不能被 `cleanup_stale` 误杀""重新出现在源里必须复活为 active"，均以"改回旧行为 → 用例失败"验证过有效性）；`test_adapters.py` 新增 `test_greenhouse_discover_requests_content`（断言请求参数带 `content=true`）并补 `normalize_raw` 的 description 断言；`fakes.py` 的 greenhouse 载荷补 `content`（实体转义 HTML）；测试 168 → **171**。

**上一轮改动明细（2026-09-24 第三轮，§12.7 #3 质量看板 + 权重回归调参）：**
- **打分校唯一化**：`match.py` 抽出 `match_parts`（四分项**原始分**，不含权重）与 `weighted_rule`（按权重合成），`compute_match` 改为复用——线上排序与调参网格搜索共用同一打分源，避免口径漂移；
- **调参引擎**：新增 `app/services/tuning.py`——`simplex_grid(step)` 生成四维权重网格（每维为 step 倍数且 sum=1；step=0.1 → 286 组）、`collect_samples` 预计算每份"有非 0 相关度反馈"简历对全量 active 职位的 `(parts, vec)`、`ndcg_at_k` 评估候选、`search_weights` 在网格 × α∈{0.6,0.75,0.9} 上搜 NDCG@k 最优。**性能关键**：四分项原始分与 TF-IDF 语义分都与权重无关，只算一次，此后每个候选只是纯算术加权 + 排序；
- **结论安全性**：样本 <3 份有反馈简历直接返回 `best=None` + 提示（不硬给结论）；最优组合不优于当前基线时也返回 `best=None`（"已是样本内最优"）；样本 <10 份附"仅作趋势参考"。**只读不改配置**——采纳需人工改 `MATCH_W_*` / `MATCH_ALPHA` 后重算 match_scores，接口/脚本都带 `env_snippet` 供直接粘贴；
- **报告同源**：`insights.py` 抽出 `latest_outcomes`（"最新反馈"口径唯一来源，调参与质量报告共用）、新增 `weights_snapshot()`，`GET /api/insights/quality` 返回值新增 `weights` 字段（看板展示 + 调参对照基线）；
- **接口与 CLI**：新增 `GET /api/insights/tuning`（`step` 越界 [0.05,0.5] 返回 422，`Query(ge=,le=)` 自动校验）；新增 `scripts/tune_weights.py`（离线打印样本/基线/top5 候选/建议 + env 片段，同样不写 .env）；
- **前端质量看板**：新增 `frontend/app/insights/page.js`——反馈质量指标卡（投递总数/反馈覆盖/命中率/NDCG）、结果分布、分数对照、当前生效权重 chips，以及「运行调参」区（粒度可选 0.05~0.5，展示样本/基线/建议/候选/提示）；主页面顶部加入口链接；`globals.css` 补 `.metrics/.metric/.table-row/.mono` 等看板样式；`next build` 通过（新增 `/insights` 路由 2.49 kB）；
- **本轮补的用例**：新增 `tests/test_tuning.py`（9 条：网格 sum=1 与组合数、粗粒度收缩、`ndcg_at_k` 对正确权重的偏好与空样本 None、env 片段行数、搜索找到优于基线的最优解、相关度 0 的简历不进样本、无样本给提示、API 200 与 step 越界 422）；`test_insights.py` 补 `weights` 快照断言；测试 159 → **168**。

**上一轮改动明细（2026-09-24 第二轮，§9 后台任务异步化）：**
- **`idle_jobs_cleanup`（每日 03:00 beat）**：`collect.cleanup_idle_jobs(session, now, ttl_days, rematch)` 按自然日全局兜底——`active` 且 `updated_at` 早于 `settings.idle_jobs_ttl_days`（默认 14 天）→ `expired`；补的是采集侧公司级 TTL 的盲区：**停用/删除公司留下的孤儿职位**不再被任何采集轮次访问，否则永久 active 并占推荐位；有下架时附带全量重算 match_scores（与采集侧一致）。幂等（只动 active，二次执行为 0）；用例：超期下架+新鲜保留、幂等、重算只含 active 职位、任务 `.run()` 直调；
- **`resume_parse_task`（上传后异步）**：`POST /api/resumes` 在 `settings.resume_parse_async=true` 时先入库占位（`profile={"parse_status":"pending"}`，`lang` 由 `detect_lang` 预判）并投递 `resume_parse_task.delay(resume_id)`，立即返回 `parse_status=pending`；worker 内解析 → 落 `profile` + `parse_status=done` → 顺带 `refresh_matches`；**投递失败（开发期无 Redis/worker）自动降级为请求内同步解析**（`main._enqueue` 返回 False），不会静默丢任务；任务幂等（`parse_status=done` 直接 skip，规避 acks_late 重投递重复调 LLM），失败不抛异常而是置 `parse_status=failed` + `parse_error` 供前端展示；`/api/recommend` 对 pending 简历**跳过空画像重算**（避免写入中性分垃圾行）；
- **`run_match_task`（画像修改后异步）**：`PUT /api/resumes/{id}/profile` 在 `settings.match_async=true` 时改为投递任务、返回 `{matches_refreshed: 0, async: true}`（同样失败即降级同步重算）；
- **前端适配**：`page.js` 新增 `waitForParse`（上传返回 pending 时轮询 `GET /api/resumes/{id}` 至终态，超时提示手动刷新）+ 解析中/解析失败提示条；`next build` 通过；
- **本轮补的用例**：新增 `tests/test_async_tasks.py`（13 条：TTL 下架/幂等/重算、解析任务的解析+匹配/幂等 skip/缺简历/空文本置 failed、重算任务幂等、API 异步投递、两处投递失败降级）；`test_celery_config.py` 补 beat 条目与 6 个任务注册断言；测试 146 → **159**。

**上一轮改动明细（2026-09-23/24，简历解析增强 + 推荐地区筛选）：**
- **简历解析增强（院校/项目/实习）**：`llm_extract.SYSTEM_PROMPT` 扩 schema——`education[{school,major,degree,start,end,highlights[]}]`、`projects[{name,role,tech[],links[],description}]`、`internships[{company,title,start,end,description}]`，无对应内容整体省略且禁止编造；`rules.py` 新增**章节切分**（教育/项目/实习标题白名单，其余标题仅作边界）+ **正则兜底**（院校「CJK 前缀 + 大学/学院」并排除"大学生/大学时"误伤、`专业 · 学历` 写法、时间区间归一 `2024年9月 → 2024/9`、以「技术栈：」行为锚点切项目条目并把跨行纯 ASCII 续写并回、项目名跳过仓库链接行、实习按时间区间行切条目）；`validate_profile` 对三字段同样规则兜底（缺失时回填并记 `{key}_from_rules`）；
- **项目技术栈并入 skills**：`parse.py` 把 `projects[].tech` 与 LLM 技能、词典扫描命中取并集后统一 canonicalize——此前「项目里用到的技术不算技能」会让匹配得分偏低；
- **前端解析详情面板**：`page.js` 新增「查看解析详情（院校 / 项目 / 实习）」折叠面板分块渲染 + 学历中文标签；刷新页面后经 `GET /api/resumes/{id}` 单独恢复 profile（`/api/recommend` 不返回 profile），取详情失败不阻塞推荐列表；
- **推荐地区筛选（region）**：`GET /api/recommend` 新增 `region=all|cn|overseas`（非法值 422）；`DOMESTIC_SOURCES=("netease",)` 判国内，海外 = 非国内 ∪ `source IS NULL`（规避 `NOT IN` 遇 NULL 全落空）；实测库内 netease 2629 条全为中文城市、其余 ATS 源 1918 条全为非中文城市，source 信号与城市信号一致且不依赖 PG 正则（SQLite 同样可用）；前端「全部/国内/国外」按钮切换；
- **运维改进**：`start-all.ps1` 的 Docker Desktop 路径改为多候选探测（ProgramFiles / ProgramFiles(x86) / `LOCALAPPDATA\Programs\DockerDesktop`）+ 从 docker CLI 位置反推，去掉硬编码（原写法换机器即"未找到 Docker Desktop"）；`next.config.js` 加 `experimental.proxyTimeout=180000`——Next.js dev 代理默认 30s，简历解析调 LLM 常超时被掐断，表现为前端 500 但后端其实已入库；
- **海外岗位交错展示（方案 A，§12.7 #9）**：`main._mix_regions` 让 `region=all` 的默认列表按国内/海外 1:1 严格交错（两路各自有序，首位归分数更高的一路），海外不再被整体挤出默认前 50；两路各取 `offset+window` 后交错再切片，分页序与一次性取全一致；前端「全部」标题同步为"国内/国外交错展示"；
- **本轮补的用例（§12.7 #10）**：新增 `tests/test_recommend_region.py`（分区计数、`source IS NULL` 归海外、非法 region 422、交错模式、单区不受影响、分页窗口拼接一致性）+ `test_resume_parse.py` 三条（教育/项目/实习规则抽取、LLM 缺字段时规则回填、LLM 路径保留三块且项目技术栈并入 skills）；测试 138 → **146**；
- **顺带修掉规则抽取的一个真缺陷**：项目技术栈跨行续写原先按空格拼接后再切分，会把两行粘成 `FastAPI PostgreSQL` 这种假技能名；改为逐行拆分（`rules._extract_projects`）。
- **挂账（非阻塞）**：海外排序的跨语言语义缺口（当时仅交错解决"看不到"）——**这一条已在第六轮以方案 C 收尾**（见本轮明细）。

**上一轮收尾明细（2026-09-19）：**
- **companies admin API**：GET/POST/PUT/DELETE /api/companies（slug 冲突 409、ats_type 未注册 400、有职位时删除需 force=true 连带 match_scores，列表带 job_count）——公司映射不再依赖改 seed 重跑；
- **match_score 失效重算**：match.py 抽出 _rebuild + 新增 refresh_matches_all（TF-IDF 索引一次构建多简历共享）；collect_company 职位有增/改/过期后自动全量重算（rematched 计数，rematch=False 可关）；
- **质量基线**：app/services/insights.py + GET /api/insights/quality——反馈覆盖率 / 命中率（interview+offer ÷ 有反馈）/ 各 outcome 组 final_score 均值 / NDCG@k（推荐序 vs 反馈相关度，offer=3>interview=2>0），为权重回归调参提供依据；
- **Alembic 补齐**：alembic/versions/0001_init.py（以 Base.metadata 建全 9 表，含 city_keys/is_active）；存量库 `alembic stamp head`；alembic.ini 注释改 ASCII（Windows GBK 解析坑）；
- **city 多值**：新列 jobs.city_keys（city.py city_keys()：分号/顿号/竖线/斜杠拆多城市、别名存储侧展开、去"市"后缀）；查询侧 city_keys+city 双列 LIKE；scripts/backfill_city_keys.py 存量回填幂等（dev.db 4547 条已完成）；实测 "New York" 召回 275 条（此前存储侧 "NYC" 召回不到）；
- **Redis 令牌桶 + 别名缓存**：app/services/ratelimit.py（Lua 原子令牌桶，DB 间隔守卫之上的跨进程第二层，Redis 不可用返回 None 自动降级）+ build_alias_map Redis TTL 缓存（settings.alias_cache_ttl_s=600，key cache:alias_map）；conftest autouse 密封 Redis；
- **遗留（Docker Desktop 未运行，启动后执行）**：PG 侧 `python scripts/backfill_city_keys.py` 回填 + `alembic stamp head` 打点。

**已实现代码地图（backend/）：**

| 模块 | 文件 | 状态 |
|---|---|---|
| 入口 | app/main.py | ✅ /health + /api/jobs（city/source 过滤、分页、{code,data,message} 信封） |
| 模型 | app/models/（10 张表） | ✅ companies/jobs/users/resumes/match_scores/applications/feedback_log/fetch_log/skill_tags/**match_weight_versions（§12.5 采纳版本）**；BIGPK/JSONType 可移植变体 |
| 配置 | app/core/config.py、db.py | ✅ .env 已切 PG（postgresql+psycopg2://jda:jda@localhost:5432/jda）+ Redis（redis://localhost:6379/0）；SQLite 仍可开箱（tests 用独立内存库隔离）；开关：§9 三开关 + `sr_fetch_details` + `job_tag_llm`/`job_tag_llm_cap` |
| 适配器 | app/adapters/ | ✅ base（RawJob/NormalizedJob/AtsAdapter ABC）+ workday/greenhouse/lever/netease/ashby/smartrecruiters/official_site（robots 校验）+ registry。Ashby：posting-api 单页全量带 descriptionPlain 零 N+1；SR：公开 v1 分页 limit=100，列表无 description——详情 enrich 已接（sr_fetch_details 开关默认关，cap=200/次限速 0.1s，HTML 剥标签入 descriptionPlain，带 slug applyUrl）；**Greenhouse：boards-api 带 `content=true` 一次请求拿全量职位全文（实体转义 HTML，`content_to_text` 先 unescape 再剥标签）**；**OfficialSite 家族：官网兜底规则化解析（JSON-LD / 内嵌 JSON / 正则三模式，robots 前置拒绝即停手），Netease 为其子类** |
| 采集 | app/services/collect.py | ✅ 字典归一标签、间隔限流、TTL 失效（公司级 cleanup_stale + 全局 cleanup_idle_jobs）、批内去重、保鲜刷新、fetch_log 审计；**TTL 语义 = "源里已消失"：出现在本轮结果里即强制 `status=active`（复活被误杀职位），且 `cleanup_stale` 前先 `session.flush()`（`autoflush=False` 下否则读不到本轮刚写的 `updated_at`，会把刚刷新过的职位整批误判过期）**；**入库抽标签 `_tag_round_jobs`（§12.7 #7，提交后执行 + 异常兜底，结果带 `tagged` 计数）** |
| PG 迁移 | scripts/migrate_sqlite_to_pg.py + docker-compose（pgvector/pgvector:pg15 + redis） | ✅ 已执行：9 张表行数对账全 OK（jobs 4547 / match_scores 11487 / resumes 3），vector 扩展就绪，PG 上全链路冒烟通过 |
| 通知 | app/services/notify.py | ✅ 催进邮件（smtplib SSL 零依赖；SMTP_* 未配置自动降级日志；只发自收件箱）；**已通用化：`send_mail(subject, body)` + `send_im_text(content)` + `_post_im(payload)`（投递催进与面试催进共用，SMTP/webhook 细节各只有一处）** |
| 鉴权 | app/services/auth.py + main.py | ✅ §4.3（2026-09-25）：HS256 JWT **零依赖**（标准库 hmac/hashlib，不引 PyJWT/passlib/bcrypt）+ PBKDF2-SHA256 口令散列（每用户随机盐、常数时间比对）；`/api/auth/register|login|refresh` + `/api/auth/me`；`current_user` 依赖 + `auth_required` 开关（默认 false = 旧客户端行为不变，true = 缺令牌 401）；`_scope_user_id`/`_owned`/`_require_admin` 把越权拦在服务端（自述 user_id 静默以令牌为准、按 id 取他人资源 403、公司写操作需 admin）；`AUTH_SECRET` 已进 §14 ⑤ 审计与轮换清单 |
| 可观测性 | app/services/obs.py + main.py | ✅ §10（2026-09-25）：**结构化 JSON 日志**（`LOG_JSON=true`，一行一个对象含 `request_id`，异常只留类型不留堆栈）+ **`GET /metrics`**（Prometheus 文本格式手写，零依赖：HTTP 计数/耗时直方图 + 采集成功率/队列积压/命中率仪表，**HTTP 的 `path` 标签用路由模板防基数爆炸**，业务口径复用 `ops`；`METRICS_TOKEN` 可选门禁）+ **Sentry 可选**（`SENTRY_DSN` + `pip install sentry-sdk`，未装则静默跳过）；`X-Request-ID` 回显/生成 |
| 权重回灌 | app/services/weights.py + main.py | ✅ §12.5（2026-09-25）：反馈 → 调参 → **采纳/回滚**；权重版本化落库（`match_weight_versions`，迁移 0004），生效 = 最新一行、表空回退设置；`active_weights()` 单一口径（打分/报告/调参同源）；`GET/POST /api/match-weights`（apply/rollback/auto-tune，管理动作）；自动采纳默认关（beat 周一 06:00，样本与增益双门槛） |
| 限流/缓存 | app/services/ratelimit.py + collect.build_alias_map | ✅ Redis Lua 令牌桶（DB 间隔守卫第二层，跨进程防双抓；Redis 不可用自动降级）+ skill_tags 别名映射 TTL 缓存（cache:alias_map，600s） |
| 质量报告 | app/services/insights.py + tuning.py | ✅ GET /api/insights/quality（反馈覆盖 / 命中率 / outcome 组均分 / NDCG@k + `weights` 权重快照）+ GET /api/insights/tuning（权重网格搜 NDCG@k 最优，step 越界 422，只读不改配置） |
| 公司管理 | app/main.py /api/companies | ✅ 增删改查 + slug/ats_type 校验 + force 连带删除（带 job_count） |
| 市场洞察 | app/services/market.py | ✅ GET /api/insights/market（城市/技能/薪资/趋势四维聚合 + k-匿名脱敏：分桶计数 < min_sample 不单独输出；region 口径复用 §4.2） |
| 面试陪伴 | app/services/interview.py | ✅ §12.6 P5 ②：GET /api/applications/{id}/interview-kit（准备清单 + 技能考察点 + 公司背景包，全部规则推导离线可算）+ PUT /{id}/interview-at（`interview_at` 登记，终态 400）+ GET /api/interview-reminders（`[now-7天, now+2天]` 窗口，overdue 标注）；beat 每日 09:30 `interview_reminder_task` |
| 简历优化 | app/services/optimize.py | ✅ §12.6 P5 ③：POST /api/resumes/{id}/optimize（body `{job_id, use_llm=false}`）对照目标 JD 逐条差距 + 改写建议——五口径（skill/experience/education/city/keyword）只列未满足项，口径与 `match`/`city` 同源（跨区按 §12.7 #9 C 中性跳过）；默认纯规则离线可算，LLM 兜底开关默认关（未配置/失败只在 `llm.status` 标注，不抛异常）；诚实口径硬约束（不替用户编造经历） |
| 移动端/PWA | frontend/app/manifest.js + public/sw.js + app/sw-register.js | ✅ §12.6 P5 ④：manifest 元数据路由（产物 `/manifest.webmanifest`，`standalone` + theme/background + 图标）+ service worker（外壳预缓存 / 导航 network-first / 静态 stale-while-revalidate / **`/api/` 绝不缓存**）+ `layout.js` 的 `viewport`/`appleWebApp` + `globals.css` 响应式（≤640px、触控 ≥44px、iPhone 安全区）；`next build` 通过 |
| 招聘季 / 多语言 | app/services/seasonality.py + i18n.py + frontend/app/i18n.js | ✅ §12.6 P5 ⑤：GET /api/insights/seasonality（月份/季度分布 + 旺淡季判定 + 当前季节与建议；**只认 `publish_date`，不回退 `created_at`**；样本 < 12 不给结论）+ i18n 标签（`SUPPORTED_LANGS=zh-CN\|en`，服务层回退 / API 层 422）+ 前端词典 + cookie 语言开关（`<html lang>` 服务端按 cookie 渲染）+ 看板招聘季卡片（区域切换 + 柱状分布） |
| OCR | app/pipelines/ocr.py | ✅ 扫描件 PDF：pymupdf 渲染 150DPI 页图 → OpenAI 兼容视觉 API（DashScope qwen-vl）逐页抽取；平均每页 <20 字符判定扫描件自动触发；OCR_API_KEY 空=明确报错不静默；max_pages 成本控制 |
| 解析管道 | app/pipelines/ | ✅ §6 落地：text_extract（txt/md 直读 + pypdf 抽取，5MB 限）/ rules（正则/词典兜底 + 防幻觉校验 + **教育/项目/实习章节切分与正则兜底**）/ llm_extract（DeepSeek 兼容，key 空走规则；schema 含三类经历）/ parse（编排 + skills 归一 union，**projects[].tech 并入 skills**）/ **job_tags（§12.7 #7 职位侧抽标签：词典优先 + LLM 兜底，与简历侧 prompt 分离）** |
| 匹配引擎 | app/pipelines/match.py + semantic.py | ✅ §7 一期 + 二期融合：四分项规则分 + explain；role_fit 剥修饰词/通用后缀（含 ASCII 后缀）+ **中英双向同义词表（后端↔backend/server，§12.7 #9 C）**；city_fit 含**跨区中性**（国内简历 vs 海外职位 → 0.5 `cross_region_neutral`，同区不同城市仍 0）；P4 融合 final=α*rule+β*vec（α=0.75 可调），vec=TF-IDF（CJK 2-gram + **中英术语桥双向投影**，纯 Python 零依赖）；match_scores 落 rule_score/vec_score/final_score；greenhouse 无 skills 中性分问题**已从数据侧解决**（`content=true` 补全文 + 词典补标签） |
| 投递闭环 | app/services/applications.py + autofill.py + main.py | ✅ §8：状态机（非法迁移 400）；POST /api/applications（authorized=false 403，重复投递 409）；GET /api/applications；GET /api/reminders 催进扫描（T=3 天可调）；变更回灌 feedback_log；POST /api/applications/{id}/autofill 半自动帮填（playwright 有头预填姓名/邮箱/电话，绝不自动提交，用户人工核对） |
| 简历 API | app/main.py | ✅ POST /api/resumes（multipart/raw_text，新简历自动置 active，`resume_parse_async` 开时返回 `parse_status=pending` 走异步）；GET /api/resumes（列表含 is_active）；GET /api/resumes/{id}（前端解析详情面板与 pending 轮询靠它）；POST /{id}/activate（active 标记互斥）；DELETE /{id}（连带 match_scores）；PUT /{id}/profile（白名单修正，改后触发重匹配，`match_async` 开时走异步）；GET /api/recommend（懒刷新 + score/rule/vec + explain 排序 + region 国内/海外分区，pending 简历跳过空画像重算） |
| 种子 | app/services/seed.py | ✅ 5 家 M1 公司 + 11 条技能字典（dji 休眠 is_active=false） |
| CLI | app/workers/collect_cli.py | ✅ init/seed/list/run（同步手动触发，与 Celery beat 并存） |
| 调度 | app/workers/celery_app.py | ✅ Celery beat：每 30min tick collect_jobs_task（间隔守卫在 collect_all 内生效）+ 每日 09:00 催进扫描 + **每日 09:30 面试催进（§12.6 P5 ②，与投递催进分时）** + 每日 03:00 全局 TTL 下架 + **每周一 06:00 权重自动采纳（§12.5，WEIGHTS_AUTO_TUNE=true 才注册，样本/增益双门槛）**；resume_parse_task / run_match_task 按 `resume_id` 异步解析与重算（API 侧 `resume_parse_async` / `match_async` 开关投递，投递失败降级请求内同步）；broker=settings.redis_url，默认 redis://localhost:6379/0 |
| CI | .github/workflows/ci.yml | ✅ push/PR 触发：Python 3.13 + pytest（SQLite 内存库，不触网） |
| 迁移 | alembic/ | ✅ 0001_init（以 Base.metadata 建全 9 表）+ **0002_interview_at（增量加 `applications.interview_at`，`op.add_column` 手写不 autogenerate）** + **0003_apply_consent（增量加 `authorized_at` / `consent_version` 同意留痕）** + **0004_match_weights（§12.5 权重版本表）** + 存量库 `alembic stamp head`（dev 阶段仍可 create_all） |
| 测试 | tests/ | ✅ 398 用例（38 文件）+ 3 条 PG 集成（设 `JDA_TEST_PG_URL` 才跑，CI 自动跳过）：适配器（含 ashby/sr、greenhouse `content=true` 请求参数与全文解析）、限流/去重/TTL/fetch_log（含"轮次内刷新不被误杀""重新出现即复活"两条回归）、**入库抽标签（词典优先/LLM 兜底/cap/失败容错/无 key 不发请求/端到端接入）**、city 过滤（含 city_keys 多值）、Celery 调度（含 §9 三项异步任务的 .run 直调用例）、简历解析（规则/防幻觉/管道/API/管理/三块经历）、匹配推荐（含语义融合/全量重算/region 分区与交错/**跨区城市中性/中英 role 双向/术语桥语义**）、投递闭环（状态机/合规/催进）、邮件/IM 通知、OCR、companies admin、质量报告、权重调参（网格/NDCG/搜索结论/入参校验）、令牌桶/别名缓存、异步投递与降级、**市场洞察（四维聚合/k-匿名截断/region 过滤与 §4.2 同源/入参越界）**、**面试陪伴（清单/缺口/背景包 active 口径/英文项/终态拒登记/催进窗口与 overdue/路由 404·400·422/beat 直调）**、**简历优化（五口径差距与"满足/未知不列"、跨区跳过、关键词覆盖与 missing、诚实口径反向断言、LLM 默认关/未配 key/失败降级/成功路径、路由 200·404·422）**、**PWA 资产契约（manifest 可安装字段与 standalone/图标 SVG/SW 三钩子与 `/api/` 不缓存/视口与响应式媒体查询）**、**招聘季（只认 publish_date 不回退 created_at/旺淡季阈值/样本不足不给结论/月份与季度分布/当前季节/年份跨度提示/region 过滤与标签/中英标签与语言回退/路由 200·422）**、**多语言看板契约（中英词典 key 一致/前端语言码与后端 `SUPPORTED_LANGS` 一致/读 cookie 输出 `<html lang>`/开关写 cookie 后刷新/看板消费 seasonality 接口）**、**原件加密（AES-GCM 往返与幂等/明文字节不入密文/同明文两次不同/篡改·截断·错密钥拒收/存量明文透传/上传密文落盘与删除联动）**、**可观测性（JSON 日志字段与异常不外泄、request_id 回显/生成、按路由模板计数与 unmatched、直方图分桶、/metrics 令牌三态、业务口径仪表与 Redis 不可用不冒充）**、**官网兜底（三模式解析、JSON-LD 全链路、{page} 与 next_page 翻页与 max_pages 封顶、robots 拒绝即停手且不抓页面、未配规则/页面无职位的 NotConfigured）**、**反馈回灌闭环（校验 8 组反例、设置回退、采纳→全量重算按新权重生效、两级回滚、自动采纳全链路与三种不换参数、接口生命周期、beat 条目随开关注册）**、**上线合规（政策载荷与前后端版本一致/投递告知顺序与旧文案已删/`consent_version` 不符 400 与留痕/删简历连带删盘与越界拒删/审计行结构自检与覆盖缺口/Markdown 表/运维看板口径与告警静默/生产配置审计分级与"不回显密钥原文"/仓库 `.env.example`·`.gitignore` 契约/**政策正文中英小节与段落一一对应、强制三节中英齐备、英文不残留中文、无占位词、效力声明与 `authoritative_lang` 同口径**）** |

**剩余零散优化项（独立排期，均非阻塞；完整清单见 §12.7）：**
- **§12.7 #9 残留已销号（第十九轮）**：海外语义分均值 0.0020 → 嵌入口径 **0.4318**（50 条切片实测：同一中文简历查询，25 英文海外岗 + 25 中文岗），英文岗最高名次 #12 → #7，且中文岗 top5 不劣化；要再上台阶可换 BGE-M3（默认目标模型，多语最强）并做嵌入分校准（无关分地板 0.37 的问题，见 §7）；
- `job_tag_llm` 默认关（成本开关）：不开启时只有词典扫描生效，**非技术岗与长尾技术词仍无标签**（dev.db 实测 active 4617 条中 908 条有 skills），要覆盖需开启 LLM 兜底或扩大 `skill_tags` 字典；
- 线上 PG 侧待办（Docker 起来后）：重跑 greenhouse 等源采集以补 description → 重跑 `scripts/extract_job_skills.py` 回填存量 skills → 重算 match_scores。

---

## 1. 技术栈与环境

| 项 | 选型 | 说明 |
|---|---|---|
| 后端框架 | Python 3.13 + FastAPI | 单体，统一 BFF 与匹配网关（CI 与本地 venv 均 3.13） |
| 异步任务 | Celery + Redis broker | 采集/解析/催进等后台任务 |
| 调度 | celery beat / APScheduler | 定时抓取与状态轮询 |
| 数据库 | PostgreSQL 15 + PGVector | 业务数据 + 向量检索（二期） |
| 缓存/限流 | Redis 7 | 去重、限流、会话 |
| 前端 | Next.js 14 / React / TypeScript | 以后可加 PWA、小程序 |
| ORM | SQLAlchemy 2.0 + Alembic | 迁移管理 |
| LLM | OpenAI 兼容接口 | 简历结构化抽取、语义打分（二期） |
| 基建 | Docker Compose（本地）/ CI: GitHub Actions | |

**关键决策：** 不做多后端拆分。FastAPI 单体承载 API；采集/匹配逻辑落在 Celery worker。采集规模变大需隔离主服务时，再抽出独立 worker 集群。

---

## 2. 系统架构

```
                           ┌────────────────────────────────────────────┐
    Next.js 前端 ──HTTPS──▶│                  FastAPI                    │
 (推荐页/看板/简历上传)     │  /api/resumes  /api/jobs  /api/applications│
                           │  /api/auth*    /api/insights               │
                           └───────────────┬────────────────────────────┘
                                           │
                          ┌────────────────┴────────────────┐
                          │           Celery Worker          │
                          │  ATS 采集器  简历解析器  匹配引擎  │
                          │  投递执行器  反馈回灌              │
                          └────────┬─────────────────────────┘
                                   │
         ┌───────────────┬─────────┴────────────┬────────────────┐
         ▼               ▼                      ▼                ▼
   PostgreSQL        Redis              外部 ATS(公开接口)      LLM 服务
  (业务+向量)      (去重/限流/队列)      Workday/Gh/Lever        解析/语义
                                            邮件/表单
```

> **图注（2026-09-24）：** `*` 标记的 `/api/auth` 为规划位（JWT 未实现，见 §4.3），已实现路由以 §4.1 为准；采集、催进扫描、每日 TTL 下架由 Celery 承载（beat 常开）；框内"简历解析器 / 匹配引擎"已接 Celery（`resume_parse_task` / `run_match_task`），但 API 侧开关 `resume_parse_async` / `match_async` **默认关**——开发期无 worker 时仍走请求内同步，投递失败亦自动降级（§9）。

---

## 3. 数据模型（PostgreSQL）

### 3.1 companies（公司 → ATS 站点映射，冷启动核心资产）

```sql
CREATE TABLE companies (
  id          BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  slug        TEXT UNIQUE NOT NULL,        -- company_slug，全局唯一
  name        TEXT NOT NULL,
  ats_type    TEXT NOT NULL,               -- workday|greenhouse|lever|smartrecruiters|official_site|bes|moka|dayi
  site_url    TEXT,                        -- 职位列表/站点根地址
  feed_url    TEXT,                        -- 公开 JSON API 地址（海外）
  locale      TEXT DEFAULT 'zh-CN',
  auth_type   TEXT DEFAULT 'public',       -- public|oauth|none
  fetch_policy JSONB DEFAULT '{"interval_min": 360}',
  is_active   BOOLEAN DEFAULT TRUE,
  created_at  TIMESTAMPTZ DEFAULT now(),
  updated_at  TIMESTAMPTZ DEFAULT now()
);
```

### 3.2 jobs（标准化职位）

```sql
CREATE TABLE jobs (
  id            BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  company_id    BIGINT REFERENCES companies(id),
  external_id   TEXT NOT NULL,             -- ATS 侧原始 job id
  title         TEXT NOT NULL,
  city          TEXT,
  city_keys     TEXT,                      -- 多城市规范化索引串（分隔符拆多值、别名已展开、去"市"后缀；查询层 city_keys+city 双列 LIKE）
  skills        JSONB DEFAULT '[]',        -- ["fastapi","docker",...]
  experience_min INTEGER,
  degree_req    TEXT,                      -- bachelor|master|phd|na
  salary_min    NUMERIC, salary_max NUMERIC, salary_currency TEXT,
  description   TEXT,
  apply_url     TEXT NOT NULL,
  can_auto_apply BOOLEAN DEFAULT FALSE,
  source        TEXT,                      -- workday|greenhouse|...|official_site
  publish_date  DATE,
  status        TEXT DEFAULT 'active',     -- active|closed|expired
  vector        vector,                    -- §7 二期已接入：不定维（维度门禁在应用层 EMBEDDINGS_DIM，见 vector_store）
  created_at    TIMESTAMPTZ DEFAULT now(),
  updated_at    TIMESTAMPTZ DEFAULT now(),
  UNIQUE (company_id, external_id)         -- 去重主键
);
CREATE INDEX idx_jobs_source ON jobs(source);
CREATE INDEX idx_jobs_city_status ON jobs(city, status);
```

### 3.3 resumes / user_profiles

```sql
CREATE TABLE resumes (
  id         BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  user_id    BIGINT NOT NULL,
  file_path  TEXT,                         -- 源文件路径（当前明文落盘，加密未实现，见 §10）
  raw_text   TEXT,                         -- OCR/解析文本
  profile    JSONB,                        -- 结构化画像（见 §6）
  lang       TEXT DEFAULT 'zh',            -- zh|en|mix
  is_active  BOOLEAN DEFAULT TRUE,         -- 同用户仅一条生效（上传/activate 互斥）
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### 3.4 match_scores（结果持久化，供反馈回灌与离线评测）

```sql
CREATE TABLE match_scores (
  id          BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  resume_id   BIGINT REFERENCES resumes(id),
  job_id      BIGINT REFERENCES jobs(id),
  rule_score  NUMERIC(5,2),                -- 一期规则分
  vec_score   NUMERIC(5,2),                -- 二期向量分
  llm_score   NUMERIC(5,2),
  final_score NUMERIC(5,2),
  explain     JSONB,                       -- 可解释标签命中明细
  created_at  TIMESTAMPTZ DEFAULT now(),
  UNIQUE (resume_id, job_id)
);
```

### 3.5 applications（投递 + 反馈闭环）

```sql
CREATE TABLE applications (
  id           BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  user_id      BIGINT NOT NULL,
  resume_id    BIGINT REFERENCES resumes(id),
  job_id       BIGINT REFERENCES jobs(id),
  mode         TEXT,                       -- direct_link|semi_auto
  status       TEXT NOT NULL DEFAULT 'submitted',  -- submitted|under_review|interview|rejected|no_feedback|closed
  apply_url    TEXT,
  external_ref TEXT,                       -- ATS 侧回执/申请编号
  authorized   BOOLEAN DEFAULT FALSE,      -- 是否已用户显式授权代填
  supplier_new TEXT,                       -- 半自动帮填后的供应商侧单据
  interview_at TIMESTAMPTZ,                -- §12.6 P5 ② 用户登记的面试时间（可空；登记后才进定时催进）
  created_at   TIMESTAMPTZ DEFAULT now(),
  updated_at   TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_apps_user ON applications(user_id);
CREATE INDEX idx_apps_status ON applications(status);
```

> **`interview_at` 口径（§12.6 P5 ②，2026-09-24）：** 由**用户自己登记**（`PUT /api/applications/{id}/interview-at`），平台不去 ATS 猜；仅 `submitted`/`under_review`/`interview` 三个非终态可登记（其余返回 400），清空传 `null`。增量迁移见 `alembic/versions/0002_interview_at.py`。

### 3.6 feedback_log（结果回灌匹配引擎）

```sql
CREATE TABLE feedback_log (
  id            BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  application_id BIGINT REFERENCES applications(id),
  outcome        TEXT,                    -- interview|rejected|no_feedback|offer
  note           TEXT,
  created_at     TIMESTAMPTZ DEFAULT now()
);
```

### 3.7 users（账号体系）

> 各业务表中的 `user_id`（resumes/applications 等）均引用本表。

```sql
CREATE TABLE users (
  id            BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  email         TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,             -- bcrypt/argon2
  role          TEXT NOT NULL DEFAULT 'user',  -- user|admin
  created_at    TIMESTAMPTZ DEFAULT now(),
  updated_at    TIMESTAMPTZ DEFAULT now()
);
```

### 3.8 fetch_log（采集审计）

> 对应 §5.2 采集流程与 §10 合规审计要求，每次采集执行落一条记录。

```sql
CREATE TABLE fetch_log (
  id          BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  company_id  BIGINT REFERENCES companies(id),
  status      TEXT NOT NULL,               -- success|failed|rate_limited|robots_blocked
  job_count   INTEGER DEFAULT 0,           -- 本次新增/更新职位数
  error_msg   TEXT,
  started_at  TIMESTAMPTZ NOT NULL,
  finished_at TIMESTAMPTZ
);
CREATE INDEX idx_fetch_log_company ON fetch_log(company_id, started_at DESC);
```

### 3.9 skill_tags（标签字典，技能/工种归一化）

> 匹配求交与搜索召回的统一口径：所有"同义不同名"的词（K8s/Kubernetes、后端/backend）抽取后统一映射到 `canonical` 标准名。

```sql
CREATE TABLE skill_tags (
  id          BIGINT PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  canonical   TEXT UNIQUE NOT NULL,        -- 标准名，如 "kubernetes"
  category    TEXT NOT NULL,               -- skill|role（技能/工种）
  aliases     JSONB DEFAULT '[]',          -- ["k8s","kube"]，抽取命中别名即归一
  is_active   BOOLEAN DEFAULT TRUE,
  created_at  TIMESTAMPTZ DEFAULT now()
);
```

---

## 4. 后端 API（REST，前缀 `/api`）

统一响应 `{ code, data, message }`。

### 4.1 已实现路由（与 `app/main.py` 一致，2026-09-24 核对）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | /health | 存活探针（响应头带 `X-Request-ID`，全站统一） |
| GET | /metrics | Prometheus 抓取点（§10）：文本格式 `text/plain; version=0.0.4`，含 HTTP 指标与业务口径仪表；`METRICS_ENABLED=false` → 404；配了 `METRICS_TOKEN` 则需同值令牌（`?token=` 或 `Bearer`，不符 401） |
| POST | /auth/register | 注册（body `{email, password}`）：邮箱唯一 409、口令 ≥8 位/邮箱格式错 422；**角色不接受客户端传入（恒 user）**；返回 `{id, email, role, access_token, refresh_token, token_type, expires_in}` |
| POST | /auth/login | 登录：失败一律 401（文案不区分"未注册/口令错"，响应时间不泄露注册状态） |
| POST | /auth/refresh | 用 refresh 令牌换新令牌对；**access 当 refresh 用 → 401**；账号已删 → 401 |
| GET | /auth/me | 当前用户（**恒需令牌**，与 `auth_required` 开关无关） |
| GET | /jobs | 职位列表（`?city=&source=&limit=&offset=`；city 走 city_keys+city 双列 LIKE。**`query` 全文检索未实现**） |
| POST | /resumes | 上传/解析简历（multipart `file` 或 `raw_text`；返回 profile + source + parse_status；新简历自动置 active。`settings.resume_parse_async=true` 时先入库返回 `parse_status=pending`，解析交 worker） |
| GET | /resumes | 简历列表（`?user_id=`，含 is_active） |
| GET | /resumes/{id} | 简历详情（含 profile，前端解析详情面板与异步解析轮询的数据源） |
| POST | /resumes/{id}/activate | 切换生效简历（active 互斥） |
| DELETE | /resumes/{id} | 删除简历（**连带 match_scores + 磁盘原件**，§14 ① 个人信息删除口径；归属校验不通过一律不删，响应含 `file_removed`） |
| PUT | /resumes/{id}/profile | 人工修正画像（白名单字段，改后触发重匹配；`settings.match_async=true` 时改投递 worker，返回 `{matches_refreshed: 0, async: true}`） |
| POST | /resumes/{id}/optimize | AI 简历优化（body `{job_id, use_llm=false}`）：对照目标 JD 逐条差距（skill/experience/education/city/keyword 五口径，只列未满足项）+ 改写建议；默认纯规则离线可算，LLM 兜底默认关；简历/职位不存在 404，缺 job_id 422 |
| GET | /recommend | 推荐列表（`?resume_id=&limit=&offset=&refresh=&region=`；懒刷新，按 final/rule/updated 排序；`parse_status=pending` 的简历跳过重算） |
| POST | /applications | 发起投递（body: job_id, resume_id, authorized, consent_version；authorized=false → 403，重复投递 409，职位失效 404；**`consent_version` 与当前政策版本不符 → 400**，未带则补记当前版本；落 `authorized_at` + `consent_version` 同意留痕，§14 ②） |
| GET | /applications | 投递看板（`?user_id=&status=`） |
| POST | /applications/{id}/status | 状态机迁移（非法迁移 400，变更回灌 feedback_log） |
| POST | /applications/{id}/autofill | 半自动帮填（playwright 有头预填，绝不自动提交） |
| GET | /reminders | 催进扫描（`?user_id=`，T=settings.reminder_after_days，默认 3 天） |
| GET/POST/PUT/DELETE | /companies, /companies/{id} | 公司→ATS 映射增删改查（slug 冲突 409、ats_type 未注册 400、有职位时删除需 `force=true`；列表带 job_count） |
| GET | /insights/quality | 质量基线报告（反馈覆盖率/命中率/outcome 组均分/NDCG@k/当前权重快照） |
| GET | /insights/tuning | 权重回归调参建议（`?k=10&step=0.1&top=5`，step 越界 [0.05,0.5] → 422；只读，采纳走 /match-weights） |
| GET | /match-weights | 当前生效权重 + 采纳历史（历史即可回滚的审计轨迹；`active_weights` 单一口径：打分/报告/调参同源） |
| POST | /match-weights/apply | 采纳一组权重（管理动作；body `{weights, note, rematch=true}`；权重和 ≠1/越界/beta 不自洽 → 400；采纳后默认全量重算 match_scores） |
| POST | /match-weights/rollback | 回滚到上一版（管理动作；无采纳记录 400） |
| POST | /match-weights/auto-tune | 跑一次"反馈→调参→可选采纳"（管理动作；body `{k, step, top, min_sample, margin, apply=false, rematch}`；**默认 dry-run**；样本/增益双门槛不过就不换参数，原因如实返回） |
| GET | /insights/market | 市场洞察报告（`?region=all|cn|overseas&top=10&min_sample=3&months=12`；城市/技能/薪资/趋势四维聚合，**k-匿名脱敏：分桶计数 < min_sample 不单独输出**；region 非法 422、top/min_sample/months 越界 422） |
| GET | /insights/seasonality | 招聘季画像（`?region=all|cn|overseas&lang=zh-CN|en&min_sample=3`；月份/季度分布 + 旺淡季判定 + 当前季节与建议，**只认 `publish_date` 不回退 `created_at`**，有发布日期样本 < 12 不给结论；region/lang 非法 422、min_sample 越界 422） |
| GET | /applications/{id}/interview-kit | 面试陪伴包（准备清单 + 技能考察点 + 公司背景包 + 反问问题；规则推导离线可算；申请不存在 404） |
| PUT | /applications/{id}/interview-at | 登记/清空面试时间（body `interview_at`，ISO 字符串或 `null`；格式非法 422，终态申请 400） |
| GET | /interview-reminders | 面试催进扫描（`?user_id=&within_days=`，默认 2 天；窗口 `[now-7天, now+within_days]`，已过标 `overdue`；within_days 越界 [0,30] → 422） |
| GET | /legal/policies | 政策版本/生效日 + 政策清单 + 投递前告知文案 + **强制披露三节的机器可读 id**（`app/services/legal.py` 单一事实源；前端 `/legal/*` 与投递确认面板的数据源，§14 ①②） |
| GET | /compliance/audit | 采集 robots/terms 合规审计表（逐公司结论 + 证据 + 复核日 + **覆盖缺口 `unreviewed`**，§14 ③） |
| GET | /insights/ops | 运维监控看板（`?hours=24&k=10`；采集成功率 / 任务积压 / 命中率基线 + `alerts`；Redis 不可用时队列口径 `available=false`，越界 422，§14 ④） |

> **异步开关（§9，2026-09-24）：** `settings.resume_parse_async` / `match_async` 默认 **false**（开发期无 Redis/worker，走请求内同步，行为与旧版完全一致）；置 true 后上述两个路由改投递 Celery 任务，`main._enqueue` 投递失败（broker 不可达）**自动降级为同步执行**，不会静默丢任务。任务幂等与失败态见 §9。

> **质量看板（§12.7 #3，2026-09-24）：** 前端新增 `/insights` 页面消费上述两接口——上半区为反馈质量（指标卡 + 结果分布 + 分数对照 + 当前权重），下半区可「运行调参」并按 `step` 粒度展示基线/建议/候选/提示。调参接口**不写 .env**，只回 `env_snippet` 供人工确认后采纳（详见 §7 与 tuning.py docstring）。

> **市场洞察（§12.6 P5 ①，2026-09-24）：** `GET /api/insights/market` 只读 `jobs` active 快照做四维聚合，**脱敏口径是硬约束**——分桶计数 < `min_sample`（默认 3）不单独输出（只累计进 `anonymity.suppressed_buckets`），且报告不含公司名/职位标题/申请链接，可直接作为 B 端脱敏数据交付。城市归属取 `city_keys` 首段（采集侧已规范化的多城市与别名），region 口径与 §4.2 同源（`market.region_of` 懒导入 `main.DOMESTIC_SOURCES`，`source IS NULL` 归海外）。口径细节见 `app/services/market.py` docstring；C 端订阅推送与 B 端交付合同属运营事项（非代码）。

> **面试陪伴（§12.6 P5 ②，2026-09-24）：** `interview_at` 由用户自己登记（不猜 ATS）；陪伴包全部规则推导，**不调 LLM**，且技能缺口给的是「可迁移经验 + 正在学习」的**诚实口径**（不教谎报）。公司背景包只用库内公开招聘信息（在库 active 职位数 / ATS 来源 / 官网 / 城市 / 发布日期），不含外部未公开情报。催进窗口 `[now-7天, now+within_days]`：已过面试时间的标 `overdue` 提示补记结果，**超过 7 天宽限即静默**（避免用户不更新终态导致永久打扰）；beat 每日 09:30 推送，邮件/IM 通道复用 `notify.py`，均未配置则降级打日志。详见 `app/services/interview.py` docstring。

> **简历优化（§12.6 P5 ③，2026-09-24）：** `POST /api/resumes/{id}/optimize` 把「简历画像 vs 目标 JD」拆成五个可比口径（skill/experience/education/city/keyword），**只列未被满足的项**，每项带 `kind/status/title/detail/advice`。experience/education/city 口径与 `match.py`、`city.py` **同源**（跨区城市按 §12.7 #9 C 中性口径跳过，不误报）。关键词只取 **ASCII 硬词**（去虚词停用词）、**不用 CJK 2-gram**（中文 n-gram 是噪音）；`missing` 只提示不诱导堆砌。默认 `use_llm=false` 走纯规则离线路径；开启且配了 `llm_api_key` 才复用 `llm_extract._build_client` 调 LLM，**未配置/失败都只在 `llm.status` 标注，不抛异常**。诚实口径是硬约束：`notes` 声明不替用户编造经历，缺口技能明确"未接触过就不要写进简历"。详见 `app/services/optimize.py` docstring。

> **招聘季适配 + 全球多语言看板（§12.6 P5 ⑤，2026-09-24）：** `GET /api/insights/seasonality` 把多年 `publish_date` 投影到 1~12 月叠加，给月份/季度分布、旺淡季判定（月均 × 1.2 / × 0.8 为阈值）与当前季节建议。**口径硬约束：只认 `publish_date`，绝不回退 `created_at`**——采集时间会聚集到采集当月，跨年叠加等于把"我们什么时候采的"说成"市场什么时候招的"（`market.py` 趋势块回退 `created_at` 看的是"库内新增节奏"，与此不同，故这里刻意不回退）。有发布日期样本 < `MIN_DATED_JOBS`(12) 时不给季节结论（`season` 全 `null`），`years_covered < 2` 时另加"仅供参考"提示。多语言走 `app/services/i18n.py`（`SUPPORTED_LANGS = ("zh-CN","en")`，**只翻译枚举与固定提示语**，报告数值结构语言无关；服务层未知语言回退、API 层 422）——前端 `app/i18n.js` 词典 + `lang-switch.js`（写 cookie 后整页刷新，`<html lang>` 由 `layout.js` 按 cookie 服务端渲染），看板 `/insights` 新增招聘季卡片（区域切换 + 柱状分布）。**剩余（非代码）**：行业细分季节需行业标签、旺季订阅提醒属运营配置、第三语言翻译需人工校对。

> **上线合规（§14 ①②③④⑤，2026-09-24；① 中英对照版 2026-09-24 补）：** 政策版本由 `app/services/legal.py` 单一事实源下发（`GET /api/legal/policies`，另回强制披露三节的机器可读 id `required_section_ids`），**前端硬编码的版本号与后端常量一致由用例断言**——同意版本对不上是"可核验"的问题，不能靠人记。`POST /api/applications` 的 `consent_version` 不符即 400，并把 `authorized_at`/`consent_version` 落库留痕；`DELETE /api/resumes/{id}` 连带删磁盘原件（越界不删）。**政策正文（中英逐条对照）是前端静态资产 `frontend/app/legal/legal-text.json`**——页面渲染、送审稿（`scripts/export_legal_text.py`）、契约断言读同一份字符串；`meta.authoritative_lang` 声明中文为准、英文为参考译文（`disclaimer` 与页面文案同源，脚本校验二者不得互相打脸），`meta.legal_review` 记签署版本/复核人/复核日，**政策改版会让原签署失效**。采集合规审计表在 `app/services/compliance.py`（`GET /api/compliance/audit` + `scripts/audit_compliance.py --markdown`），**库内公司无审计行会出现在 `unreviewed`**——这条是"新增公司先补审计行再开采集"的机器兜底。运维看板 `GET /api/insights/ops` 复用既有数据（`fetch_log` / Redis 队列 / `insights`），**限流与被拒计入采集成功率分母、Redis 不可用不拿 0 冒充健康**。生产配置审计 `app/services/prodcheck.py` + `scripts/audit_production_config.py`（有 `fail` 则 exit 1），只判密钥长度与占位词、**不回显原文**。剩余条件（法务终稿签署与运营主体信息、密钥实际轮换、加密存储、JWT、Prometheus）见 §14 逐项标注。

### 4.2 推荐地区分区（region，2026-09-23 新增；交错展示 2026-09-24 补）

`GET /api/recommend?region=all|cn|overseas`（默认 `all`，非法值 422）：

- 国内 = `Job.source ∈ DOMESTIC_SOURCES`（当前仅 `("netease",)`）；
- 海外 = 非国内 **∪ `source IS NULL`**（`NOT IN` 遇 NULL 会整条件落空，故显式补 `OR source IS NULL` 把脏数据归海外）；
- 选 `source` 而非城市正则判定的原因：实测 netease 2629 条全为中文城市、其余 ATS 源 1918 条全为非中文城市，两者信号一致，而 `source` 不依赖 PG 专有正则、SQLite 下同样可用。新增国内源时在 `DOMESTIC_SOURCES` 登记。
- **`region=all` 为国内/海外 1:1 交错**（方案 A，§12.7 #9，`main._mix_regions`）：两路各自按 final/rule/updated 排序，再严格交替合并，首个位置给分数更高的一路——海外岗位此前被纯分数排序整体挤出默认列表（实测根因是跨区 `city` 恒 0 + `role` 跨语言难命中，**不是**缺 `experience_min`/`degree_req`；该缺口经方案 C 收窄，交错作为稳定曝光位兜底）。实现上两路各取前 `offset+window` 条后交错再切片，与一次性取全的交错序一致（分页稳定），`total` 仍为两区之和。
- **路内再做公司级轮转**（2026-09-25 补，`main._route_rows` + `main._round_robin`）：方案 A 只解决了"国内 vs 海外"，没管"海外内部是谁"——实测海外前 50 名 100% 来自 ashby 上的 3 家公司（elevenlabs / linear / ashby），而 stripe(647)、equinox(736)、nvidia(40) 一条都进不来。故每路内部先做公司级轮转再交出去：公司顺序按"该公司在本简历下的最高分"降序（高分先出），公司内仍按 final/rule/updated 排序，逐轮各取一条，取空的公司自动退出轮转。实测海外前 50 名覆盖 **8 家公司**、首轮 8 个位子来自 8 家不同公司；国内只有 netease 一家，行为不变。公司数是冷启动资产、个位数（当前库内 11 家），逐家取数的开销可忽略。
- `region=cn` / `overseas` 走同一套路内轮转（仅 `all` 再叠跨区交错）；前端标题显示为"国内/国外交错展示，各路内按公司轮流曝光"（单区为"各公司轮流曝光，公司内按匹配度排序"），每项仍展示 `来源`。

### 4.3 账号体系（JWT，2026-09-25 落地）与设计位

**已实现：** `POST /api/auth/register`、`POST /api/auth/login`、`POST /api/auth/refresh`、`GET /api/auth/me`（HS256 JWT，**零依赖实现**，见 `app/services/auth.py`）。用户态接口通过 `current_user` 依赖接收 `Authorization: Bearer <token>`：

- `settings.auth_required=false`（**默认**）：未带令牌时行为与旧版一致（`user_id` 仍可由表单/查询参数传入，老客户端不被打死）；
- `settings.auth_required=true`：用户态接口缺令牌一律 **401**；
- **两种模式下都生效的安全口径**：带令牌时普通用户的操作主体恒为令牌本人（`_scope_user_id`：自述型 `user_id` 被**静默覆盖**，冒名不成立；admin 尊重显式传值，可代操作），用户态资源做归属校验（`_owned`：按 id 取他人简历/投递 → **403**），公司映射写操作需 admin（`_require_admin`）——**越权拒绝在服务端，不靠前端不传参**。

> 设计取舍（冒烟实测撞出来的）：自述型 `user_id` 不硬判 403。前端目前仍带默认 `user_id=1`，若“传了别人的 id”就 403，登录后所有上传会突然失败；而该字段只是“我以谁的身份提交”，静默归到令牌主体既绝了冒名，又不打新老客户端过渡——**真正的越权拦截落在资源 id 路径上**。

> **前端接入（第二十轮，2026-09-25）：** 令牌与带鉴权请求收在 [auth.js](file:///d:/AIProjects/job-direct-apply/frontend/app/auth.js)（`access_token` / `refresh_token` 存 localStorage，`authFetch` 附 `Authorization: Bearer`；**401 → 用 refresh 换一次令牌 → 重试一次**，换不到清令牌跳 `/login?next=<当前路径>`；并发 401 由 `refreshing` 单例合并为一次刷新）。[登录页 /login](file:///d:/AIProjects/job-direct-apply/frontend/app/login/page.js) 用一张表单切登录/注册两个 Tab（打 `/api/auth/login` 与 `/api/auth/register`，字段与后端同名），成功后存令牌整页跳 `next`（**只接受站内相对路径**，挡开放重定向）；[auth-bar.js](file:///d:/AIProjects/job-direct-apply/frontend/app/auth-bar.js) 挂在 `layout.js`，未登录给登录入口、已登录显示邮箱 + 退出。**客户端自认登录、后端却拒绝**这条只剩"换令牌"一个可自愈动作，故不做静默降级、也不吞 401；`/api/auth/me` 这类登录态自检显式传 `redirectOnFail: false`（未登录 401 是正常答案）。契约断言见 `tests/test_frontend_auth.py`（7 条）。

| 方法 | 路径 | 说明 |
|---|---|---|
| PATCH | /applications/{id} | 与已实现的 `POST /applications/{id}/status` 同义，保留设计位 |

**关键鉴权/合规钩子：** `POST /applications` 必须携带 `authorized=true` 才允许半自动模式；拒绝带空授权。

---

## 5. 数据采集：ATS 适配器层

### 5.1 统一接口（抽象基类）

```python
class AtsAdapter(ABC):
    ats_type: str
    @abstractmethod
    def discover(self, company: Company) -> list[RawJob]: ...
    @abstractmethod
    def normalize_raw(self, raw: RawJob) -> NormalizedJob: ...  # 输出 §3.2 schema
```

**首批实现（对齐 2026-09-16 实际代码，app/adapters/）：**

| Adapter | 站点/接口 | 状态 |
|---|---|---|
| `WorkdayAdapter` | `POST {tenant}.wd{N}.myworkdayjobs.com/wday/cxs/{tenant}/{site}/jobs` | ✅ 已实现（limit≤20 / 2000 封顶 / site 大小写敏感，见 §5.3 踩坑要点） |
| `GreenhouseAdapter` | `boards-api.greenhouse.io/v1/boards/{token}/jobs?content=true` | ✅ 已实现（`content=true` 让列表直接带职位全文，实体转义 HTML 需先 unescape 再剥标签） |
| `LeverAdapter` | `api.lever.co/v0/postings/{slug}?mode=json` | ✅ 已实现（生态萎缩，暂无 M1 代表公司，见 §5.3） |
| `NeteaseAdapter` | `POST hr.163.com/api/hr163/position/queryPage`（pageSize=50 分页） | ✅ 已实现（OfficialSiteAdapter 家族首站，见 §5.3 网易实录） |
| `OfficialSiteAdapter` | 官网自有接口/页面，规则化解析（§5.1 三模式） | ✅ **已实现（2026-09-25 第十七轮）**：JSON-LD `JobPosting`（自动，含 `@graph`）/ 内嵌 JSON（`script_id` + `json_path` + `fields`，SPA 常见）/ 逐字段正则；规则放 `fetch_policy["html_rules"]`（纯数据，不执行代码）；**robots 前置**（不允许 → `RobotsDisallowed`，不抓页面）；未配规则 → `OfficialSiteNotConfigured`；官方站默认间隔 720 分钟；大疆（Moka）评估后休眠 |
| `SmartRecruitersAdapter` | 公开职位 JSON API（v1 分页 limit=100） | ✅ 已实现（列表无 description，详情 enrich 开关 `sr_fetch_details` 默认关，cap=200/次、限速 0.1s） |
| `AshbyAdapter` | posting-api（单页全量带 `descriptionPlain`，零 N+1） | ✅ 已实现（P4 扩容新增） |

### 5.2 采集流程

```
触发(定时/TTL) → 读取 companies(fetch 策略) → adapter.discover()
            → normalize → LLM 标签抽取（工种/技能 → skill_tags 标准标签）
            → 入库(upsert by UNIQUE company_id,external_id；skills 存标准标签)
            → 关键词→字段/标签映射写 Redis（搜索意图判定用，见 §7）
            → 失效扫：active 超过 N 个周期无更新的标记 expired/closed
            → Redis 限流 + 抓取策略卡片更新
```

**抽标签要点（发布即抽取，一次抽取全链路复用，2026-09-24 已实现）：**
- **实现顺序（关键）**：词典扫描优先（`app/pipelines/job_tags.tag_from_dictionary`，零成本、离线可跑），命不中的空标签职位再走 LLM（`llm_extract_job_skills`）——反过来会让 LLM 处理本来免费可命中的职位，成本白翻一倍；
- 标签在采集入库阶段一次性抽出并归一到 `skill_tags.canonical`（与简历侧共用 `build_alias_map` + `canonicalize_skills`），推荐/搜索/二期向量共享，避免各环节重复理解同一份 JD；
- 千级数据量按采集周期**增量**抽取：只处理"本轮采集到 + skills 为空 + description 非空"的职位，已有标签不重复花钱；`job_tag_llm`（默认关，需 `llm_api_key`）+ `job_tag_llm_cap=50` 限每轮 LLM 条数，超出的计入 `llm_skipped` 留到下一轮；
- 抽取失败降级：**不阻塞入库**——抽标签在职位与 fetch_log 提交之后执行，单条 LLM 失败只计 `llm_failed`、skills 留空等下一轮或离线脚本补（`scripts/extract_job_skills.py` 用于存量回填）；
- 关键词→字段映射缓存：抽取产出的标签别名同步写入 Redis（key=关键词，value=字段/标准标签），供搜索端"先判字段再检索"。（设计借鉴《黑马直聘》2026-09 公开技术分享）

**限流与保鲜守卫（强制）：**
- 每公司独立 `fetch_policy`（默认 `interval_min=360`，官网兜底更保守）；
- Redis 令牌桶限流，超阈值熔断，写 `fetch_log` 审计；
- **TTL 的语义是"源里已消失了"，不是"曾经过期"**：本轮出现在结果集里的职位一律强制 `status=active`（含此前被误杀的），只有"本轮未见且超过 `interval_min × ttl_multiplier`"才置 `expired`。配套两点实现约束——upsert 的 `else` 分支必须显式复位 `status`（`NormalizedJob.to_row()` 不含 status，否则复活无门）；`cleanup_stale` 前必须先 `session.flush()`（会话是 `autoflush=False`，否则 SELECT 读不到本轮刚写的 `updated_at`，会把**刚刷新过**的职位整批误判过期）；
- 走 `OfficialSiteAdapter` 前必须校验 robots 白名单。

### 5.3 首批站点情报与候选清单（M1 输入，2026-09 核实）

**ATS 归属判定法（10 秒判定）：** 打开目标公司任一职位详情，看"申请"跳转域名——`*.myworkdayjobs.com` 即 Workday、`boards.greenhouse.io` / `job-boards.greenhouse.io` / apply 链接带 `gh_jid` 参数即 Greenhouse、`jobs.lever.co/{slug}` 即 Lever。只信 apply 链接，不信品牌名气（反例：Netflix 已迁离 Lever；Robinhood 实为 Greenhouse 而非 Lever）。

**已核实站点（真实可用）：**

| 公司 | ATS | 站点参数 / 入口 | 核实结果 |
|---|---|---|---|
| NVIDIA | Workday | tenant=`nvidia`, shard=`wd5`, site=`NVIDIAExternalCareerSite` | 约 2000 在招职位；实测 2 页 40 条入库 |
| Salesforce | Workday | tenant=`salesforce`, shard=`wd12`, site=`External_Career_Site` | 约 1477 在招职位 |
| Adobe | Workday | tenant=`adobe`, shard=`wd5`, site=`external_experienced` | 约 800 在招职位 |
| Stripe | Greenhouse | board token=`stripe`（boards-api.greenhouse.io/v1/boards/stripe/jobs） | 实测 647 条入库 |
| Robinhood | Greenhouse | board token=`robinhood` | 实测 153 条入库（2026-09-16 终核） |
| Anyscale | Lever | slug=`anyscale` | 实测唯一活板，仅 1 条（搬家公告）；Lever 适配器经此验证 |
| 网易 | official_site | 根=`https://hr.163.com`，`POST /api/hr163/position/queryPage` | 实测 2641 条（53 页 × 50），免鉴权无加密；NeteaseAdapter 已落地，2629 条入库（12 条跨页重复被批内去重） |
| 大疆 | official_site | 实为 Moka ATS（apply.careers.dji.com，siteId=170070） | 公开接口响应整体加密，暂缓（见下评估实录） |

**网易招聘评估实录（2026-09-16，OfficialSiteAdapter 首个落地依据）：**
- hr.163.com 为 React SPA，`/robots.txt` 返回 HTML 回退页（无真实 robots）→ 按惯例视为允许，仍遵守低频（720 分钟/轮）；
- 公开 JSON 接口：`POST {base}/api/hr163/position/queryPage`，body `{"currentPage":N,"pageSize":50,"keyword":"","workPlaceList":[],"jobTypeId":""}`，响应 `data.pages/data.total/data.list`；pageSize=50 实测可用，keyword 过滤有效；
- 列表项自带 `description`（职位描述）与 `requirement`（任职要求）全文 → **无需逐条调详情接口**；字段含 `id/name/workPlaceNameList[]/reqEducationName/reqWorkYearsName/firstPostTypeName/firstDepName/productName/updateTime(ms)/beeUrl`；
- 详情页 URL：`https://hr.163.com/job-detail.html?id={id}&lang=zh`（beeUrl 实测多为 None，兜底自建）。

**官网兜底规则化解析（2026-09-25 第十七轮）：** `OfficialSiteAdapter` 已从骨架补为可用实现（三模式 + robots 前置，见 §5.1 表），新增评估工具 `scripts/probe_official_site.py`（输入官网职位页 URL → 输出 robots 判定 / JSON-LD 块与 JobPosting 数 / 带 id 的 script / 首职位规范化字段；无结构化数据时直接给 `html_rules` 骨架）。**实测两条样本**：`hr.163.com/job-list.html` → robots 允许但 0 个 JSON-LD（SPA + 自有 JSON 接口，故走 NeteaseAdapter，工具如实建议补规则或休眠）；`job-boards.greenhouse.io/stripe/jobs/6606581` → robots 允许、1 个 JobPosting，title/url/datePosted/city/description 逐个对得上——JSON-LD 路径在真实标记上成立。**接入新站点的流程**：先跑评估工具取证据 → 写 `fetch_policy.html_rules`（或确认可直用）→ 入库 `companies` 并补 §14 ③ 审计行。

**大疆评估实录（2026-09-16，结论：暂缓）：**
- `we.dji.com` 仅是品牌门面：Next.js SSG 营销页（`/zh-CN/list`、`/zh-CN/position/*` 全部落回主页），无 SSR 职位数据、无 sitemap.xml；
- 真实招聘入口为 `apply.careers.dji.com/social-recruitment/dji/170070?hash=#/jobs`，特征（`/api/outer/ats-*` 命名空间、`moka-unified-account`、`mage/captcha`）确认为 **Moka ATS**；
- 公开列表接口 `POST /api/outer/ats-apply/website/jobs/v2`（body 含 `orgId:"dji"/siteId:170070/currentPage/pageSize`）无论参数对错均返回 `{"data":"<密文>","necromancer":"<混淆>"}`；`jobs/recent`、`group-by-job` 同样加密，`settings/jobs` 明文但需登录；
- 解密需复刻其前端混淆加密逻辑：脆弱（Moka 可随时轮换）且属**主动绕过反爬措施**，违反 §5.2 合规原则 → 大疆休眠（seed 中 `is_active: false`，评估证据保留），国内 M1 暂由网易代表；若需国内第二家，候选（腾讯/米哈游等）需另行评估。

**Lever 生态萎缩（2026-09-16 实测结论）：** 对 20 个历史 Lever slug 批量探测，17 个 404（Twitch/Loom/Mistral/Pinterest 等均已迁移）、2 个 200 但空板、仅 Anyscale 残留 1 条无实质内容的公告。**Lever 已不宜作为 M1 数据源代表**；适配器代码保留（接口仍可用），P4 扩容时优先 Greenhouse/Workday，并将 Ashby（同为开放 JSON ATS）列入评估。

**M1 首批名单（2026-09-16 定稿：3 海外 + 2 国内）：**

| 序号 | 公司 | ATS | 入选状态 | 备注 |
|---|---|---|---|---|
| 1 | NVIDIA | Workday | ✅ 入选 | 2000 级职位，画像匹配度最高；实测入库 |
| 2 | Stripe | Greenhouse | ✅ 入选 | 公开 API 字段完整；实测 647 条入库 |
| 3 | Robinhood | Greenhouse | ✅ 入选 | 终核实测 153 条入库；Greenhouse 第二家验证自定义域名板 |
| 4 | 网易 | netease（official_site 家族） | ✅ 入选 | 实测 2641 条、免鉴权无加密；NeteaseAdapter 已落地，2629 条入库 |
| 5 | 大疆 DJI | Moka | ⚠️ 暂缓 | 实为 Moka ATS 且接口整体加密，解密属绕过反爬；seed 休眠，补位候选待评估 |
| 6 | Anyscale | Lever | 适配器验证样本 | 非 M1 名单成员，仅用于验证 LeverAdapter |
| 7 | Salesforce / Adobe / Dell | Workday | 备选 | 已核实可用 |
| 8 | Twitch / Loom | Lever | ✖ 剔除 | 实测 404，板已迁移 |

**定稿说明：** 海外 3 家 = NVIDIA（Workday）+ Stripe（Greenhouse）+ Robinhood（Greenhouse），覆盖两类主流 ATS 与自定义域名板两种形态；Lever 因生态萎缩暂无代表公司，适配器已实测就绪，待 P4 扩容再评估是否引入 Ashby 等替代源。国内 = 网易（netease 适配器，已入库 2629 条）；大疆经实测为 Moka 加密体系暂缓休眠，国内第二家补位候选（腾讯/米哈游等）待补评估。备选替换顺序：海外 Salesforce / Adobe / Dell。

**Workday 适配器实现要点（踩坑实录，实现时必须遵守）：**
- 真实接口：`POST https://{tenant}.wd{N}.myworkdayjobs.com/wday/cxs/{tenant}/{site}/jobs`，JSON body，无鉴权；GET 无有效响应；
- ① `limit` 上限 20，传 21 会**静默返回空数组**（HTTP 200、total 不变）——不是报错，极易误判为"该公司无职位"；
- ② 单站点单次搜索 2000 条封顶（部分租户连 total 都封在 2000），超出需按 `searchText` / location 关键词切分多次拉取；
- ③ `{site}` 名大小写敏感，拼错返回 404（表现为"公司不存在"，实际是参数错）；`{site}` 无法从域名推断，须从任一职位 URL 中解析（路径含 `/en-US/` 等 locale 段，需剔除）；
- tenant/shard/site 三元组从任一 job URL 一次性分解后即可永久复用，存入 `companies.feed_url`。

---

## 6. 简历解析管道

```
上传/OCR → 文本清洗 → LLM 结构化抽取 → profile JSONB（可人工修正）→ 触发匹配
```

`profile` 结构：

```json
{
  "skills": ["fastapi", "kafka"],
  "experience_years": 5,
  "target_role": "后端工程师",
  "cities": ["上海", "远程"],
  "salary_min": 30,
  "salary_max": 45,
  "uta_city": null,
  "industry": ["互联网"],
  "edu_degree": "bachelor",
  "education": [
    {"school": "某某大学", "major": "计算机科学与技术", "degree": "bachelor",
     "start": "2020/09", "end": "2024/06", "highlights": ["主修课程：...", "GPA 3.8"]}
  ],
  "projects": [
    {"name": "简历直达", "role": "后端负责人", "tech": ["fastapi", "postgresql"],
     "links": ["https://github.com/..."], "description": "项目描述与亮点（≤200 字）"}
  ],
  "internships": [
    {"company": "某某科技", "title": "后端开发实习生",
     "start": "2023/07", "end": "2023/10", "description": "工作内容与成果（≤200 字）"}
  ],
  "lang": "zh"
}
```

**教育/项目/实习抽取（2026-09-23 新增）：** LLM 路径由 `llm_extract.SYSTEM_PROMPT` 的 schema 定义，简历中没有对应内容时列表字段整体省略（禁止编造）；LLM 不可用或该字段缺失时走 `rules.py` 兜底——先按标题白名单切章节（教育经历/项目经历/实习经历等，其它标题仅作边界终止），再正则抽取（院校、`专业 · 学历`、时间区间、技术栈锚点切项目条目），`validate_profile` 逐字段回填并记 `{key}_from_rules`。

**规则：** 结构化字段以 LLM 抽取为主，关键数值（经验年限、学历、薪资）叠加规则校验（正则/关键词兜底），避免 LLM 幻觉影响匹配。

**口径统一：** 简历 `profile.skills` 同样映射到 `skill_tags` 字典（§3.9），与职位侧使用同一套标准标签，保证匹配求交口径一致；`projects[].tech` 也与 LLM 技能、词典扫描命中取并集后一并归一，避免"项目里用到的技术"漏出技能集导致匹配得分偏低。

---

## 7. 匹配引擎

**一期（规则加权，可解释优先）：**

```
score = w_skill * skill_hit + w_exp  * exp_fit
      + w_city * city_fit + w_role * role_fit
```

- `skill_hit`：简历技能 ∩ 职位技能 / 职位技能（Jaccard 加权热门技能）；两侧均基于 `skill_tags` 标准标签求交，"K8s" 与 "Kubernetes" 归一后可命中；
- `exp_fit`：`experience_min <= resume exp <= rest_max` 给满分，超出递减；**职位无经验要求 → 1.0**（`min_exp is None`，注意海外源普遍缺该字段，实际是"不惩罚"而非"扣分"）；
- `city_fit`：命中所投城市/远程 → 1，否则 0 或低分；**跨区（国内城市简历 vs 海外职位，或反之）→ 0.5 中性 + `cross_region_neutral`**（§12.7 #9 C：简历里的城市列表只表达国内意向，对海外属"不可比"，与"city 未知 0.5"同一口径；同区不同城市仍是 0）；
- `role_fit`：`target_role` 剥修饰词/通用后缀后与 title contains，命中 1、无 target_role 0.5、否则 0；候选词走**中英双向同义词表**（后端 ↔ backend/server；英文核心词反查中文核心词），ASCII 后缀（engineer/developer…）同样剥净，解决中文简历 vs 英文 title 的 role 分恒为 0；
- 输出 `explain` 数组，供前端展示"匹配上/差在哪"。

**二期（融合向量 + LLM）：**
`final = α*rule + β*vec + γ*llm`；α/β/γ 由反馈数据回归调参。**vec 分双路（第十九轮，§7 二期落地，`build_vec_map` 统一出口）**：默认 TF-IDF（CJK 2-gram、零依赖、离线可测）；`EMBEDDINGS_PROVIDER=sentence_transformers` 且 PGVector 就绪时 → pgvector `<=>` 多语嵌入余弦（`services/embeddings.py` 懒加载本地模型 + `vector_store.py` raw SQL 存取，列不定维、换模型不动表；缺向量职位现场补嵌，`scripts/backfill_vectors.py` 预热）。**跨语言**零依赖侧另有中英岗位术语桥（`semantic._CROSS_LINGUAL`）兜底；实测嵌入余弦：中文查询 vs 同类中文 JD 0.73（TF-IDF 0.29）、vs 同类英文 JD 0.59（TF-IDF 0.21）——海外英文 JD 语义分低的 §12.7 #9 残留由此解决。已知特性：嵌入的无关分地板更高（0.37 vs 0），排序够用，需更陡区分度可再做校准（留作后续）。`γ*llm` 留位。落库字段：`rule_score/vec_score/final_score`（`llm_score` 预留），explain 的 semantic 项标注 vec 来源（tfidf_cosine / embedding_cosine）。

**反馈回灌：** `feedback_log.outcome` 聚合出"约面职位"正样本，反推优化权重与简历优化建议。

**权重回归调参（§12.7 #3，2026-09-24 落地）：** 目标函数 = NDCG@k（推荐序 vs 反馈相关度 offer=3 > interview=2 > 0），在四分项权重 simplex 网格（每维为 `step` 倍数且 sum=1）× α∈{0.6,0.75,0.9} 上搜索最优（`app/services/tuning.py`）。**打分成本只付一次**：`match_parts` 返回的四分项原始分与 TF-IDF 语义分都与权重无关，预计算后每个候选仅做算术合成 + 排序。入口：`GET /api/insights/tuning`、`scripts/tune_weights.py`、前端 `/insights` 页；样本 <3 份有反馈简历不给结论，最优不优于当前基线也返回 `best=None`。**调参结果不自动写配置**——需人工确认后改 `MATCH_W_*` / `MATCH_ALPHA` 并重算 match_scores（`refresh_matches_all` 或 `PUT /api/resumes/{id}/profile`）。

> 与线上公式同源：`compute_match` 内部调用同一组 `match_parts` + `weighted_rule`，调参结果可直接套用，不存在"离线算一套、线上算另一套"的口径漂移。

**搜索意图判定（先猜字段，再检索）：** `GET /jobs?query=` 的查询词先查 Redis 字段映射缓存（§5.2 抽取时写入）：命中则按对应字段/标准标签精确检索（解决"腌菜厨师 vs 厨师"字面不一致漏召）；未命中再走 ES 全文兜底。

**地区分区与海外得分问题（§12.7 #9，2026-09-24 三方案全落地）：** 推荐列表支持 `region=all|cn|overseas` 分区（口径见 §4.2）。海外岗位在默认排序中系统性靠后，**实测根因（dev.db 同一份国内简历 × 4617 条 active，按区分项均值）**：

| 区域 | n | skill | city | exp | role | rule 均值 |
|---|---|---|---|---|---|---|
| 海外 | 1988 | 0.473 | **0.073**（0 分占比 0.90） | **1.000** | 0.011 | 0.4027 |
| 国内 | 2629 | 0.467 | 0.643（0 分占比 0.36） | 0.993 | 0.013 | 0.5131 |

- **失分几乎全部来自 `city`**（0.2 权重 × 0.57 差距 ≈ 0.114，与 rule 总差 0.110 吻合）；`role` 两侧都趋近 0（title contains 在跨语言与同语言下都难命中）；
- **修正此前误判**：`experience_min`/`degree_req` 缺失**不是**原因——`exp_fit` 在 `min_exp is None` 时返回 1.0（海外均值 1.000、0 分占比 0.00），`degree_req` 从未参与打分；
- **方案 A（交错展示）**：`main._mix_regions` 让 `region=all` 的默认列表国内/海外 1:1 严格交错；**2026-09-25 补路内公司级轮转**（`main._route_rows` + `main._round_robin`）——交错只保证"海外有位置"，不保证"海外内部是谁"，实测海外前 50 名被 ashby 上的 3 家公司吃光，故每路内部再按公司逐轮各取一条（详见 §4.2）；
- **方案 B（补采 description）**：greenhouse `content=true` 一次请求补齐 800 条全文（§12.7 #6）；smartrecruiters 仍缺 733 条，靠 `sr_fetch_details` 详情 enrich 补（默认关）；
- **方案 C（跨语言/跨区匹配口径）**：跨区城市改 0.5 中性 + role 中英双向同义词 + semantic 术语桥。效果：海外 `city` 0.073 → 0.525、rule 0.4027 → 0.4932（国内 0.5131，齐平），**海外最高名次 25 → 7** 且第一名为真正相关的 `Backend Engineer, Payments Experiences`。**未引入神经多语模型**（零依赖约束），语义分绝对值仍偏低，是 §7 二期的后续升级项。

---

## 8. 投递闭环与状态机

状态迁移（`applications.status`）：

```
submitted → under_review → interview → offer
     │           │
     └───────────┴──> rejected / no_feedback / closed
```

**三档模式落地：**
1. `can_auto_apply=true` → 半自动帮填表单 → 跳转由用户人工确认提交；
2. 需登录/验证 → 生成直达链接 + 模板投递备注，用户一键带入；
3. 一律留人工兜底，不硬刚 ATS 登录/验证码。

**催进提醒（Celery beat，定期扫描）：**
状态卡在 `submitted`/`under_review` 超 T 个自然日 → 生成"催进"待办并通知用户（不自动发第三方邮件，仅在平台提醒）。

---

## 9. 调度与后台任务

| 任务 | 触发方式 | 状态 | 说明 |
|---|---|---|---|
| `collect_jobs_task` | celery beat `crontab(minute="*/30")` | ✅ 已接入 | 见 §5.2；公司级 fetch_policy 间隔守卫在 collect_all 内生效 |
| `collect_company_task` | 手动调用（Celery 入参 `slug`，无 HTTP 入口） | ✅ 已接入 | 采集后触发受影响 match_scores 全量重算 |
| `scan_reminders_task` | celery beat 每日 09:00 | ✅ 已接入 | 投递催进扫描，结果写日志；平台内查询式提醒见 `GET /api/reminders` |
| `idle_jobs_cleanup` | celery beat 每日 03:00 | ✅ 已接入 | 全局 TTL 下架（`collect.cleanup_idle_jobs`，`idle_jobs_ttl_days` 默认 14 天）；与采集侧公司级 TTL 互补：**停用/删除公司留下的孤儿职位**只能靠它下架；有下架时全量重算 match_scores；幂等 |
| `resume_parse_task` | 上传后异步（`settings.resume_parse_async`，默认关） | ✅ 已接入 | 上传即入库占位 `parse_status=pending` 并投递 `resume_id`；worker 内解析 + 重算匹配分；`done` 幂等 skip、失败置 `failed`+`parse_error`；**投递失败自动降级请求内同步**（无 Redis/worker 时行为与旧版一致） |
| `run_match_task` | 画像修改后异步（`settings.match_async`，默认关） | ✅ 已接入 | `PUT /api/resumes/{id}/profile` 改为投递 `resume_id`，返回 `{matches_refreshed: 0, async: true}`；投递失败同样降级同步重算 |
| `interview_reminder_task` | celery beat 每日 09:30（§12.6 P5 ②） | ✅ 已接入 | 面试催进：扫 `status=interview` 且 `interview_at` 落在 `[now-7天, now+2天]` 的申请 → 邮件/IM，均未配置则逐条打日志；**入参 `within_days` 可覆盖默认前瞻窗口**；与投递催进 09:00 分时，避免挤同一封/同一条 |

启动 worker/beat（开启异步开关时才需要；开发期默认关，靠请求内同步）：

```
celery -A app.workers.celery_app worker --loglevel=info     # backend/ 下执行
celery -A app.workers.celery_app beat --loglevel=info
```

**幂等：** 采集按公司级 `fetch_policy` 间隔守卫 + Redis 令牌桶（§5.2）防重复抓取，入库按 UNIQUE `(company_id, external_id)` upsert；`resume_parse_task` 以 `profile.parse_status=done` 自锁（acks_late 重投递不重复调 LLM），`run_match_task` / `idle_jobs_cleanup` 天然幂等（先删后插 / 只动 active）；`{action}:{key}` 形式的 Redis 任务锁未实现（任务本身无并发冲突场景）。

---

## 10. 安全、合规与可观测

- **个人信息：** 简历源文件**加密存储 ✅（2026-09-25：应用层 AES-GCM，配 `UPLOADS_KEY` 即密文落盘，见 `app/services/crypto.py`；未配密钥则为明文，prodcheck 会 warn `uploads_plaintext`；托管密钥 KMS 属部署侧后续）**；删除联动 ✅（`DELETE /api/resumes/{id}` 连带删库行、match_scores **与磁盘原件**，归属校验不通过一律不删，§14 ①）；授权记录落库 ✅（`applications.authorized` + `authorized_at` 同意时间 + `consent_version` 所同意政策版本，§14 ②）；跳转第三方 ATS 前的告知文案 ✅（`GET /api/legal/policies` 下发告知要点 + 前端**投递前勾选确认**，用户协议/隐私政策 `/legal/terms`、`/legal/privacy`，§14 ①②）；**政策正文中英对照 ✅（单一事实源 `frontend/app/legal/legal-text.json`，英文版已落地，效力口径 = 中文为准、英文为参考译文；送审稿由 `scripts/export_legal_text.py` 出）**，仅剩**法务复核签署与运营主体信息（非代码）**。
- **采集合规：** 只走公开接口 + 严格限流 + robots 白名单；不做反爬绕过；留版本合规审计 `fetch_log`；**逐公司 robots/terms 审计表 ✅（§14 ③，`GET /api/compliance/audit`，库内公司无审计行会进 `unreviewed` 缺口）**，结论的法务复核签署属非代码事项。
- **鉴权：** JWT + 角色（user/admin）**已实现（§4.3，2026-09-25）**：HS256 零依赖实现（`app/services/auth.py`），`POST /api/auth/register|login|refresh` + `GET /api/auth/me`；`settings.auth_required` 默认 false（旧客户端不被打死），置 true 后用户态接口缺令牌 401；**带令牌时以令牌主体为准**，越权 403（`_scope_user_id`/`_owned`/`_require_admin`）。投递接口仍强制 authorized。（仍未做：前端登录页与令牌存储、口令重置/邮箱验证）
- **日志与监控：** **已实现（2026-09-25，第十六轮，`app/services/obs.py`）**：结构化日志 `LOG_JSON=true` 时一行一个 JSON（含 `request_id`，异常只留类型不留堆栈正文）；`GET /metrics` 暴露 Prometheus 文本格式（HTTP 计数/耗时直方图 + 业务口径仪表：采集成功率/队列积压/命中率，**复用 `ops` 口径不另算一套**；HTTP 的 `path` 标签用路由模板防基数爆炸；`METRICS_TOKEN` 可选门禁）；Sentry 配 `SENTRY_DSN` 且装了 `sentry-sdk` 才初始化（**没装也不报错**）；告警阈值仍由 `ops.build_alerts` 给判定，**外部告警通道（Alertmanager/钉钉）与 Prometheus 服务端部署属非代码项**。
- **配置管理：** 环境变量 + `.env.example`，密钥不入库、不入 Git；**生产配置审计 ✅（§14 ⑤，`app/services/prodcheck.py` + `scripts/audit_production_config.py`：密钥占位/过短、通知通道半配置、`.env` 被 git 跟踪均判 `fail`，只判长度与占位词、不回显原文）+ 密钥轮换清单（服务商侧人工执行，非代码）**。

---

## 11. 工程规范与目录结构

```
job-direct-apply/
├── backend/            # FastAPI 单体
│   ├── app/
│   │   ├── main.py            # 应用入口 ✅ 全部 38 条路由（/health、/metrics + /api/*，含 §4.3 鉴权与 §12.5 权重采纳/回滚/自动调参，见 §4.1；路由全在 main.py，api/ 仍为空包）
│   │   ├── api/               # ✅ 仅 __init__.py 占位（路由变多后再拆分）
│   │   ├── models/            # SQLAlchemy 模型 ✅（10 张表，含 match_weight_versions）
│   │   ├── schemas/           # ✖ 未建（Pydantic 模型内联在 main.py）
│   │   ├── adapters/          # ATS 适配器 ✅（base + workday/greenhouse/lever/netease/ashby/smartrecruiters/official_site + registry）
│   │   ├── services/          # ✅ collect / seed / applications / autofill / notify / **auth（JWT 零依赖）** / **crypto（原件 AES-GCM）** / **obs（日志·指标·Sentry）** / **weights（权重版本与自动采纳）** / insights / tuning / market / seasonality / i18n / interview / optimize / ratelimit / city / legal / compliance / ops / prodcheck
│   │   ├── pipelines/         # ✅ text_extract / rules / llm_extract / job_tags / parse / match / semantic / ocr（解析 §6 + 抽标签 §5.2 + 匹配 §7；semantic 含中英术语桥）
│   │   ├── workers/           # Celery 任务 ✅ collect_cli.py（手动触发）+ celery_app.py（8 个任务：采集/单公司/投递催进/每日 TTL 下架/异步解析/异步重算/面试催进/**权重自动采纳（开关注册）**）
│   │   └── core/              # 配置/安全/日志/限流 ✅ config.py（含 §9 三开关 + 抽标签开关 + §4.3 auth_* 四项）+ db.py
│   ├── alembic/               # 迁移 ✅ 0001_init（9 表基线）+ 0002_interview_at + 0003_apply_consent + 0004_match_weights（增量）
│   └── tests/                 # ✅ 39 个文件 / 405 用例 + 3 条 PG 集成（可选容器）
├── scripts/            # ✅ 站点探测与运维脚本（probe_*.py（含 **probe_official_site.py：官网逐站评估**）、backfill_city_keys.py、extract_job_skills.py、tune_weights.py、migrate_sqlite_to_pg.py、audit_compliance.py、audit_production_config.py、export_legal_text.py、**encrypt_uploads.py（存量加密/追溯解密）**）
├── frontend/           # ✅ Next.js 推荐页 + 看板 /insights（中英双语 + 招聘季 + 运维监控卡片）+ 协议页 /legal/terms·/legal/privacy（正文中英对照，单一事实源 app/legal/legal-text.json）+ 账号（登录/注册页 /login、令牌存取与 401 自愈 app/auth.js、状态条 auth-bar.js）+ PWA（manifest / service worker / 响应式）（App Router；/api 代理到后端 8001，proxyTimeout=180s）
├── start-all.ps1 / .cmd # ✅ 一键启动（Docker 引擎 + PG/Redis 容器 + uvicorn + Next.js，带可见日志窗口）
├── 简历直达方案.md      # 产品与策略基线
└── 开发文档.md          # 本文件
```

**规范：** 后端全量类型标注（Pydantic 校验）；DB 变更走 Alembic migration；接口 commit 遵循约定；所有对外写操作用户可见、可撤销。

---

## 12. 分阶段开发计划

### 12.1 阶段总览

开发按 **P1 → P4 串行主线推进，P5 为储备轨道**。每个阶段产出"可运行版本 + 验收通过"后才进入下一阶段；前置依赖不满足的阶段不放行。

```
P1 基建+数据接入 ──▶ P2 简历解析+推荐 ──▶ P3 投递闭环 ──▶ P4 扩容+语义+报告
                                                          │
   P5 储备（市场洞察/面试陪伴/AI 简历优化/小程序/招聘季） ─┘（并行储备）
```

| 阶段 | 主题 | 核心产出物 | 依赖 |
|---|---|---|---|
| P1 | 基建 + 数据接入 | 数据库、适配器、采集 worker | 无（冷启动） |
| P2 | 简历解析 + 推荐 | 解析管道、规则匹配、推荐页 | P1（需职位数据） |
| P3 | 投递闭环 | 投递 API、状态机、看板、催进 | P2（需简历画像） |
| P4 | 扩容 + 语义 + 报告 | 更多适配器、向量/LLM、命中率看板 | P3（需反馈数据） |
| P5 | 储备轨道 | 洞察/陪伴/优化/小程序 | 主线立稳后切入 |

**并行原则：** P1 内适配器与建库可并行；P2 内解析与匹配可并行；P3 内看板与投递执行可并行。跨阶段串行。

---

### 12.2 P1 — 基建与数据接入（对应方案 M1）

**目标：** 打通"公司→ATS 站点映射 → 采集 → 入库 → 保鲜"的地基，产出首批真实在招职位。

- [x] 建库 + Alembic 迁移（companies/jobs/match_scores 等，见 §3）✅ 2026-09-16 完成；Alembic 0001_init 基线已于 2026-09-19 补齐（§0）
- [x] 适配器框架（抽象基类 `discover/normalize`，见 §5）✅ 2026-09-16 完成
- [x] `Workday` / `Greenhouse` / `Lever` 三适配器实现，按 §5.3 定稿名单（NVIDIA/Stripe/Robinhood）产出首批 3 家海外站点映射（Workday 按 §5.3 踩坑要点实现）✅ 2026-09-16 完成：841 条真实职位入库
- [x] `OfficialSiteAdapter` 低频兜底 + robots 校验 ✅ 2026-09-16 完成：网易/大疆评估定稿（§5.3 评估实录）；NeteaseAdapter 落地并实测入库 2629 条，大疆（Moka 加密）休眠（**规则化解析引擎与逐站评估工具已于 2026-09-25 第十七轮补齐，见 §0/§5.3**）
- [x] 采集 worker + 限流 + TTL 失效清理 + `fetch_log` 审计 ✅ 2026-09-16 完成（同步 CLI 版；2026-09-17 Celery beat 接入完成）
- [x] 单元/集成测试：normalize 幂等、限流、去重 ✅ 2026-09-16 完成（2026-09-17 扩至 37/37：city 过滤 16 例 + Celery 调度 4 例）
- [x] P1 收尾：CI（GitHub Actions）+ Celery beat 定时调度 ✅ 2026-09-17 完成（ci.yml：Python 3.13 + pytest，推到 GitHub 后自动生效；beat 每 30min tick）
- **DoD（退出验收）：** 能拉取真实在招职位并入库；映射表覆盖首批清单；无采集错误报警；CI 冒烟通过。P1 全部退出，后续进入 P2（简历解析 → 匹配引擎 → /recommend → 前端）。

---

### 12.3 P2 — 简历解析与推荐（对应方案 M2）

**目标：** 上传简历 → 结构化画像 → 可解释的排序推荐。

- [x] 简历上传/OCR + LLM 结构化抽取 + 规则校验（防幻觉，见 §6）✅ 2026-09-17 完成：app/pipelines/ 四模块 + /api/resumes 上传/查询/人工修正；pypdf 纯文本型抽取（扫描件 OCR 于 2026-09-18 补齐）；LLM 走 DeepSeek 兼容客户端，key 空自动规则兜底（55/55 测试离线通过，LLM 实测待配 key）
- [x] 规则匹配引擎 + `explain` 命中明细（见 §7）✅ 2026-09-17 完成：app/pipelines/match.py 四分项加权，explain 输出 hit/missing/note
- [x] `/recommend` API + 推荐列表页（Next.js）✅ 2026-09-17 完成：API（score+explain 排序、缓存懒刷新）+ frontend/ Next.js 最小页（上传→解析→profile→可展开 explain 排序列表，/api 代理后端 8001）
- [x] `profile` 人工修正接口 + 修改后触发重匹配 ✅ 2026-09-17 完成：PUT profile 白名单修正，改后同步全量重算 match_scores
- [x] 解析增强：院校 / 项目 / 实习抽取 + 前端解析详情面板（见 §6）✅ 2026-09-23 完成：LLM schema 扩三类经历 + rules 章节切分/正则兜底 + projects[].tech 并入 skills
- [x] 推荐地区分区 `region=all|cn|overseas` + 前端筛选按钮（见 §4.2）✅ 2026-09-23 完成
- [x] `region=all` 国内/海外 1:1 交错展示（方案 A，解决海外被整体挤出默认列表，见 §4.2 / §12.7 #9）✅ 2026-09-24 完成
- **DoD（退出验收）：** 上传简历可出带解释的排序推荐；命中率可人工核验；测试覆盖打分与状态。

---

### 12.4 P3 — 投递闭环（对应方案 M3）

**目标：** 从"看到推荐"到"投得出去、追得回来、反馈回来"。

- [x] `/applications` 投递 API + `authorized` 合规钩子（见 §4/§8）✅ 2026-09-18 完成：authorized=false 403，重复投递 409，职位失效 404
- [x] 投递状态机（submitted→…→offer / rejected，见 §8）✅ 2026-09-18 完成：非法迁移 400，每次变更回灌 feedback_log
- [x] 半自动帮填（公开表单）+ 直达链接两种模式 ✅ 2026-09-18 直达链接（mode=direct_link）；✅ 半自动帮填 POST /api/applications/{id}/autofill（playwright 有头预填，绝不自动提交，用户人工核对）
- [x] 投递看板（Next.js）+ 催进提醒 + 反馈回填 ✅ 2026-09-18 完成：推荐页"标记已投递"+ 投递列表/催进提醒区块；/api/reminders 查询式扫描（T=3 天可调）；反馈经状态接口回填
- **DoD（退出验收）：** 完整闭环可用；授权与信任条款流程上线后即可投递；越权/未授权投递被拒。✅ 2026-09-18 P3 主体退出（P4 通知渠道增强亦已完成：SMTP 邮件 + 钉钉/企微群机器人，见 §0）

---

### 12.5 P4 — 扩容与语义（对应方案 M4）

**目标：** 扩大覆盖、引入语义匹配、让"效果可量化"。

- [x] 扩充公司→ATS 映射表 + 更多适配器（如 SmartRecruiters）✅ 2026-09-18：新增 Ashby + SmartRecruiters（+1077 条），总量 4547、6 个 ATS 源
- [x] PGVector 向量检索 + LLM 语义打分（融合 `α/β/γ` 权重，见 §7）✅ 2026-09-18 部分完成：TF-IDF 融合（final=α*rule+β*vec，α=0.75 可调）已落地，γ*llm 留位；PG+pgvector 容器与 vector 扩展就绪，BGE/PGVector 升级接口已留
- [x] 命中率/NDCG 报告看板 + 权重回归调参 ✅ 2026-09-24：报告（2026-09-19）+ Next.js 质量看板 `/insights` + 权重网格搜索调参（`GET /api/insights/tuning`、`scripts/tune_weights.py`）全部完成（§12.7 #3）
- [x] 反馈回灌闭环上线（`feedback_log` → 排序优化）✅ 2026-09-25 完成（第十八轮）：反馈（状态机回灌）→ 调参（`GET /api/insights/tuning`，只读）→ **采纳**（`POST /api/match-weights/apply`，版本化落库 `match_weight_versions` + 全量重算，`/rollback` 可回滚）→ 生效权重供打分/报告/调参三处同源（`active_weights` 单一口径）；自动采纳默认关（`WEIGHTS_AUTO_TUNE=true` 时 beat 每周一跑，样本 ≥10 且 NDCG 增益 ≥0.02 才换参数）
- **DoD（退出验收）：** 约面率/NDCG 达到预设基线；价值可量化评估；新增站点持续稳定采集。（采集/融合/基建部分已达成，量化评估待反馈数据积累）

---

### 12.6 P5 — 储备轨道（roadmap，不阻塞主线）

> 主线 P1–P4 立稳后再切入；以下任一项均独立评估、独立立项，不挤占主线资源。

- [x] 市场洞察报告（城市/技能/薪资趋势，C 端订阅 + B 端脱敏数据）✅ 2026-09-24：`app/services/market.py` + `GET /api/insights/market`——四维聚合（城市按区 / 技能计数与占比 / 薪资按币种中位数与四分位 / 发布趋势零值补齐）+ **k-匿名脱敏**（分桶 < `min_sample` 不单独输出，报告不含公司名与链接，满足 B 端交付形态）；region 口径与 §4.2 同源；7 条用例，测试 186 → 193。**dev.db 实测**：城市 top「杭州 1364 / 广州 638 / 上海 327」、技能 top「python 379 / sql 215」、`with_salary=0`（现有 ATS 源不提供薪资字段，薪资块待薪资源接入）、趋势近月聚集（`publish_date` 缺失回退 `created_at`，已在 `notes` 声明）。**剩余条件（非代码）**：C 端订阅推送渠道/频次（可复用 `notify.py`）、B 端交付合同与导出格式
- [x] 面试陪伴闭环（面试准备清单、公司背景包、定时催进）✅ 2026-09-24：新列 `applications.interview_at`（增量迁移 `0002_interview_at`）+ `app/services/interview.py`——`GET /api/applications/{id}/interview-kit`（准备清单 / 技能考察点 / 公司背景包，**全部规则推导、离线可算**）、`PUT /{id}/interview-at`（用户登记，终态 400）、`GET /api/interview-reminders`（窗口 `[now-7天, now+2天]`，`overdue` 标注，超 7 天宽限静默）+ beat 每日 09:30 `interview_reminder_task`（与投递催进 09:00 分时，邮件/IM 复用 `notify.py`）；缺口给**诚实口径**（不教谎报），背景包只用库内公开信息；9 条用例，测试 193 → 202。**剩余条件（非代码）**：公司深度资料需外部数据源、日程双向同步需日历 OAuth
- [x] AI 简历优化（对照目标 JD 逐条差距与改写建议）✅ 2026-09-24：`app/services/optimize.py` + `POST /api/resumes/{id}/optimize`——五口径差距（skill/experience/education/city/keyword，**只列未满足项**，experience/education/city 口径与 `match.py`/`city.py` 同源、跨区城市按 §12.7 #9 C 中性跳过）+ 逐条改写建议；关键词只取 **ASCII 硬词**（去停用词、不用 CJK 2-gram）；**规则为主**，LLM 兜底开关默认关（`use_llm`，未配置/失败只在 `llm.status` 标注不抛异常）；诚实口径硬约束（不替用户编造经历，缺口技能明确"未接触过就不要写进简历"）；14 条用例，测试 202 → 216。**剩余条件（非代码）**：LLM 模型选择/配额属配置项（`llm_*`）、简历版本管理与 PDF 导出属前端工程
- [x] 移动端 / PWA / 微信小程序（解决手机求职 + 账号冷启动）✅ 2026-09-24（**PWA 部分**）：`frontend/app/manifest.js`（Next 元数据路由 → `/manifest.webmanifest`，`standalone` + theme/background + 图标）+ `frontend/public/sw.js`（外壳预缓存 / 导航 network-first / 静态 stale-while-revalidate / **`/api/` 绝不缓存**）+ `app/sw-register.js`（能力探测 + 静默失败）+ `layout.js` 的 `viewport`/`appleWebApp` + `globals.css` 响应式（≤640px、触控 ≥44px、iPhone 安全区）；6 条内容契约用例，测试 216 → 222；`next build` 通过（新增 `/manifest.webmanifest` 路由）。**剩余条件（非代码）**：位图 192/512 安装图标（含 maskable）需设计产出、真机安装/离线行为需浏览器验证、**微信小程序为独立工程**（账号 / 备案 / 独立前端，非本仓库改造项）
- [x] 招聘季适配 + 全球多语言看板 ✅ 2026-09-24：新模块 `app/services/seasonality.py` + `GET /api/insights/seasonality`——多年 `publish_date` 投影到 1~12 月叠加，给月份/季度分布 + 旺淡季判定（月均 × 1.2 / × 0.8）+ 当前季节与行动建议；**口径硬约束：只认 `publish_date`，绝不回退 `created_at`**（采集时间会聚集到采集当月，跨年叠加等于把"我们什么时候采的"说成"市场什么时候招的"——与 `market.py` 趋势块看"库内新增节奏"的取舍刻意不同）；有发布日期样本 < 12 不给季节结论、`years_covered < 2` 加"仅供参考"提示；多语言走 `app/services/i18n.py`（`SUPPORTED_LANGS = ("zh-CN","en")`，只翻译枚举与固定提示语，报告数值结构语言无关；服务层未知语言回退 / API 层 422）+ 前端 `app/i18n.js` 词典与 `lang-switch.js` 语言开关（写 cookie 后整页刷新，`<html lang>` 由 `layout.js` 按 cookie 服务端渲染）+ 看板 `/insights` 招聘季卡片（区域切换 `all|cn|overseas` + 柱状分布旺季绿/淡季灰）；15 条用例（招聘季 9 + 前端多语言契约 6），测试 222 → 237；`next build` 通过。**剩余条件（非代码）**：行业细分季节需行业标签、旺季到点订阅提醒属运营配置（可用 `notify.py`）、第三语言翻译需人工校对

---

### 12.7 剩余零散优化项（P1~P4 已退出，均非阻塞）

> 独立排期、逐项销号；与 P5 储备轨道的区别：这些是现功能的收尾打磨，不涉新赛道。

| # | 项目 | 状态（2026-09-24） | 目标 / 备注 |
|---|---|---|---|
| 1 | companies admin API | ✅ 2026-09-19 完成 | 增删改查接口（前端管理页可选，未做） |
| 2 | 职位更新后 match_score 失效 | ✅ 2026-09-19 完成 | refresh_matches_all + collect_company 后自动全量重算（rematch=False 可关） |
| 3 | 质量基线 | ✅ 2026-09-24 完成 | 报告（2026-09-19）+ 看板前端化（`frontend/app/insights/page.js`）+ 权重回归调参（`GET /api/insights/tuning`、`scripts/tune_weights.py`）；调参只读不写配置，回 `env_snippet` 供人工采纳 |
| 4 | Alembic 迁移补齐 | ✅ 2026-09-19 完成 | 0001_init + 存量库 `alembic stamp head` |
| 5 | jobs.city 多值 | ✅ 2026-09-19 完成 | city_keys 多值列 + 查询双列 LIKE + 回填脚本 |
| 6 | greenhouse 无 description | ✅ 2026-09-24 完成（168 → 171） | **修根因而非压制**：`GreenhouseAdapter.discover` 带 `content=true`，boards-api 列表一次请求返回全量职位全文（实体转义 HTML，`content_to_text` 先 unescape 再剥标签）→ greenhouse 800 条补齐 description，语义分不再恒为 0；skills 由 `scripts/extract_job_skills.py` 词典扫描免费补齐。**顺带修 3 个真实缺陷**：`cleanup_stale` 前缺 `session.flush()`（`autoflush=False` 致本轮刚刷新的职位被整批误判过期）、upsert 未复位 `status`（职位重新出现在源里也无法从 expired 复活）、`\bgo\b` 在英文 JD 散文里的误命中（853 条中 307 条被误标，改首字母大写判定后降到 ~94） |
| 7 | LLM 标签抽取 + Redis 字段映射缓存 | ✅ 2026-09-24 完成（171 → 182） | 别名映射缓存（2026-09-19）+ **采集入库抽标签**：`app/pipelines/job_tags.py`——**词典优先（零成本）→ LLM 兜底**，接入 `collect_company`（提交后执行，异常/单条失败都不阻塞入库），只处理本轮"skills 为空 + description 非空"的职位，`job_tag_llm`（默认关，需 `llm_api_key`）+ `job_tag_llm_cap=50` 限成本，结果带 `tagged` 计数并触发重算；职位侧 prompt 与简历侧分离（显式排除福利/软性词/公司名）。**未开启 LLM 时仍只有词典覆盖**（dev.db active 4617 条中 908 条有 skills），长尾词与非技术岗需开启开关或扩字典 |
| 8 | Redis 令牌桶限流 | ✅ 2026-09-19 完成 | Lua 原子令牌桶（DB 间隔守卫第二层，Redis 不可用自动降级） |
| 9 | 海外岗位默认排序缺位 | ✅ 2026-09-24 完成（A/B/C 三方案全落地，182 → 186） | **实测根因（4617 条 active 按区分项均值）：失分几乎全来自 `city`（海外 0.073、0 分占比 0.90 vs 国内 0.643；0.2 权重 × 0.57 ≈ 0.114，与 rule 总差 0.110 吻合），`role` 两侧都趋近 0。此前文档"海外 100% 缺 `experience_min`/`degree_req` → 规则分偏低"是误判——`exp_fit` 在 `min_exp is None` 时返回 1.0（海外均值 1.000、0 分占比 0.00），`degree_req` 从未参与打分**。方案 A：`region=all` 国内/海外 1:1 交错（`main._mix_regions`，见 §4.2）；方案 B：greenhouse `content=true` 补齐 800 条全文（见 #6）；方案 C（跨语言/跨区匹配口径）：跨区城市 0.5 中性（`match._city_region`/`_cross_region`，同区不同城市仍 0）+ role 中英双向同义词（`_ROLE_SYNONYMS` + `_role_candidates` 反查，ASCII 后缀剥净）+ `semantic` 中英术语桥（`_CROSS_LINGUAL`，零依赖双向投影 + 词边界匹配）。效果：海外 rule 0.4027 → **0.4932**（国内 0.5131，齐平）、**海外最高名次 25 → 7** 且第一名是真正相关的 `Backend Engineer, Payments Experiences`。**未引入神经多语模型**（零依赖约束），海外语义分绝对值仍偏低 → §7 二期 PGVector + 多语嵌入是后续升级项（**已于 2026-09-25 第十九轮销号**：嵌入口径海外语义均分 0.0020 → 0.4318、最高名次 #12 → #7，见 §7 与剩余零散优化项） |
| 10 | 新增解析字段 / region 缺专项用例 | ✅ 2026-09-24 完成（138 → 146） | `test_recommend_region.py`：分区计数、`source IS NULL` 归海外、非法 422、交错模式、单区不受影响、分页拼接一致；`test_resume_parse.py`：三块经历规则抽取、LLM 缺字段规则回填、LLM 路径保留三块且项目技术栈并入 skills |
| 11 | §9 三项后台任务异步化 | ✅ 2026-09-24 完成（146 → 159） | `idle_jobs_cleanup`（每日 03:00 全局 TTL 下架，兜底孤儿职位）+ `resume_parse_task`（上传后异步解析，pending 占位 + done 幂等 + 失败置 failed）+ `run_match_task`（画像修改后异步重算）；API 侧 `resume_parse_async` / `match_async` 开关默认关（开发期无 worker），**投递失败自动降级请求内同步**；前端 pending 轮询 |
| 12 | 质量看板前端化 + 权重回归调参（#3 收尾） | ✅ 2026-09-24 完成（159 → 168） | `app/services/tuning.py`（权重 simplex 网格 × α 搜 NDCG@k 最优；打分与权重无关故只算一次）+ `GET /api/insights/tuning`（step 越界 422）+ `scripts/tune_weights.py` + `frontend/app/insights/page.js`；`match_parts`/`weighted_rule` 抽出保证与线上公式同源；样本 <3 不给结论，只读不改配置 |

---

## 13. 测试策略

- **单测：** 适配器 normalize 幂等、打分公式、状态机迁移、限流逻辑；
- **集成：** 真实公开接口(sample tenant) 拉取冒烟用例 + fake ATS 模拟器；
- **契约：** API 用 pytest + httpx TestClient 全量回归；
- **安全：** 未授权投递被拒、越权读取他人简历被拒的用例（**2026-09-25 起由 `tests/test_auth.py` 真正覆盖**——JWT 校验六分支 + 跨用户 403 + 管理动作分级）；
- **数据质量：** "上传→解析→匹配→投递→反馈" 端到端 smoke 在 CI 跑通。
- **现状（2026-09-25 实跑）：** 405 用例全绿（约 5s，SQLite 内存库、不触网；另 3 条 PG 集成测试在真容器全过，CI 无 PG 自动跳过）；含教育/项目/实习抽取（规则 + LLM 两条路径）、region 分区/交错、§9 三项后台任务（`.run()` 直调 + API 投递与降级）、权重调参（网格 sum=1、NDCG 对正确权重的偏好、搜索结论与 step 越界 422）、采集 TTL 回归（"轮次内刷新不被误杀""重新出现即复活"——两条都在 `autoflush=False` 会话下才会暴露）、greenhouse `content=true` 请求参数与全文解析、入库抽标签（词典优先/LLM 兜底/cap 上限/失败容错/无 key 不发请求/端到端接入）、跨区城市中性（含反向对称与"同区不同城市仍 0"）、role 中英双向（含 ASCII 后缀剥净）、中英术语桥（双向补词 + 词边界 + 中文简历 vs 英文 JD 语义分不再为 0）、**市场洞察（四维聚合与趋势零值补齐、k-匿名截断、薪资小样本组丢弃、region 过滤与 §4.2 同源、入参越界 422）**、**面试陪伴（清单/缺口/背景包 active 口径/英文项/终态拒登记/催进窗口与 overdue/路由 404·400·422/beat 直调）**、**简历优化（五口径差距与"满足/未知不列"、跨区跳过、关键词覆盖与 missing、诚实口径反向断言、LLM 默认关/未配 key/失败降级/成功路径、路由 200·404·422）**、**PWA 资产契约（manifest 可安装字段与 standalone、图标 SVG、SW 三钩子与 `/api/` 不缓存、视口与响应式媒体查询）**、**招聘季（只认 `publish_date` 不回退 `created_at`、旺淡季阈值、样本不足不给结论、月份/季度分布与零值补齐、当前季节与中英标签、年份跨度提示、region 过滤、路由 200·422）**、**多语言看板契约（中英词典 key 完全一致、前端语言码与后端 `SUPPORTED_LANGS` 一致、未知语言回退且不引用 `navigator.language`、layout 读 cookie 输出 `<html lang>`、开关写 cookie 后刷新、看板消费 seasonality 接口）**、**上线合规（§14：政策载荷与必含章节、**前后端政策版本三点一线一致**、首页链接协议页、投递前告知顺序与旧文案"点击即视为同意"已删、`consent_version` 不符 400、同意留痕落库、删简历连带删盘与越界拒删、审计行结构自检/dji 休眠原因/netease no_robots 回退/覆盖缺口单列/Markdown 表、运维看板"限流计入分母""Redis 不可用不冒充 0"与全绿静默、生产配置审计 fail·warn 分级与"报告不回显密钥原文"、仓库 `.env.example`/`.gitignore` 契约、**政策正文中英小节/段落/列表一一对应、强制三节中英齐备且归位正确、英文不残留中文且每节 ≥40 字、无占位词、效力声明与 `authoritative_lang` 同口径、两页共用正文模块与语言码映射**）**、**鉴权（JWT 散列往返与畸形存储、令牌往返与 claims、过期·篡改·错类型·结构错误·alg=none·非法 sub 六类拒收、注册/登录/刷新/me、开关两态、跨用户越权 403、管理动作 401/403/200）**、**原件加密（密钥与非法格式、往返、篡改·截断·错密钥拒收、存量明文透传、上传密文落盘与删除联动）**、**可观测性（JSON 日志字段与异常不外泄、日志仅在开启时输出、request_id、按路由模板计数与 unmatched、直方图、/metrics 令牌三态、业务口径仪表、不泄露密钥）**、**官网兜底（JSON-LD 含 @graph、内嵌 JSON 字段映射、正则模式、相对链接与 external_id、翻页两种、robots 拒绝即停手、两类 NotConfigured）** 专项用例。
- **前端测试口径（§12.6 P5 ④/⑤、§14 ①②④、§4.3）：** 前端无自动化测试框架，改走"**后端 pytest 校验前端静态资产内容契约**"（`test_pwa_assets.py` / `test_web_i18n.py` / `test_legal.py` / `test_ops.py` / `test_frontend_auth.py`）——漏翻译、SW 误缓存 `/api/`、语言码前后端漂移、**政策版本前后端不一致**、**政策英文版漏译或结构漂移（页面照样渲染、`next build` 照样过）**、投递前"默认勾选即同意"、**登录态 401 自愈链路被改断（不换令牌 / 换了不重试 / 换不到不跳登录）或用户态接口漏接 `authFetch`（裸 fetch 不带令牌，登录后仍拿匿名视角的数据）**这类问题 `next build` 不会报错（只会静默降级或直接违规），正是静态断言的价值区；**法务正文的渲染另有实跑验证（`npm run build` + `next start` 后按 `lang` cookie 取页，确认中英两页正文与效力声明正确）**，`scripts/export_legal_text.py` 用同一套规则出送审稿（结构问题 exit 2 / 法务签署未闭环 exit 1）；真机安装、离线行为、法律文本终稿、第三方语言校对仍需浏览器/法务/人工验证。

---

## 14. 上线前 checklist（运营/合规）

**状态（2026-09-24 第十三轮）：五项均已完成"可离线验证的代码部分"（§14 ① 的英文版已于第十三轮补齐）；剩余条件逐项标注为（非代码），不靠人记——每一项都有接口/脚本/用例兜住（详见本节表末「机器兜底」列）。**

- [x] **① 用户协议 + 隐私政策（含个人信息授权、删除、最小必要）上线**——政策版本单一事实源 `app/services/legal.py`（`POLICY_VERSION="1.0"`，必含章节给人看的中文名 `REQUIRED_SECTIONS = 授权范围 / 个人信息删除 / 最小必要` + **机器可读 id `REQUIRED_SECTION_IDS = authorization / minimal / deletion`**）；**正文（中英逐条对照）是前端静态资产 [legal-text.json](file:///d:/AIProjects/job-direct-apply/frontend/app/legal/legal-text.json)，页面渲染、送审稿、契约断言读同一份字符串**，两页 [terms/page.js](file:///d:/AIProjects/job-direct-apply/frontend/app/legal/terms/page.js)、[privacy/page.js](file:///d:/AIProjects/job-direct-apply/frontend/app/legal/privacy/page.js) 渲染 `<LegalSections>`，外壳 [legal-doc.js](file:///d:/AIProjects/job-direct-apply/frontend/app/legal/legal-doc.js) 管版本行与效力声明（隐私政策含「一、最小必要的信息收集 / 1. Data Minimization」「三、个人信息的删除与撤回 / 3. Deletion of Personal Information…」含"原件文件同时删除 / the original uploaded resume file is deleted at the same time"）；**英文版已落地**（原剩余项之一），效力口径 = 中文为解释依据、英文为参考译文；删除联动 ✅ `DELETE /api/resumes/{id}` 连带删库行 + match_scores + **磁盘原件**（越界一律不删）。**剩余（非代码）：** 法务复核签署（填 `meta.legal_review.signed_version/reviewer/reviewed_at`，改版即失效需重签）、运营主体名称与联系方式落终稿、适用法律与个人信息保存期限条款、英文版是否改为中英同等效力；**简历原件加密已落地（2026-09-25，应用层 AES-GCM；政策已升 v1.1 并如实描述，KMS 托管密钥属部署侧后续）**，法务复核签署现针对 v1.1（改版即让原签署失效）。
- [x] **② 投递授权钩子 + 告知文案产品的 PRD 对齐**——`GET /api/legal/policies` 下发投递前告知（要点：跳第三方 / 不会代填密码不代提交 / 最小必要不留存售卖 / 可撤回；`consent_label` + `withdraw_hint`）；`POST /api/applications` 落 `authorized_at` + `consent_version` 留痕（`0003_apply_consent` 迁移），**版本不符 400**；前端两段式：先弹告知面板并**要求显式勾选**，勾选后才提交（旧"点击即视为用户确认"已删除，`FALLBACK_NOTICE` 保证告知永不缺席）。**剩余（非代码）：** 告知文案与产品 PRD 的正式对齐（本节已把要点固定为代码常量，对齐时改 `legal.APPLY_NOTICE` 一处）。
- [x] **③ 采集 robots/terms 合规审计表（逐公司）**——审计行在 `app/services/compliance.py`，`GET /api/compliance/audit` 做覆盖比对，`scripts/audit_compliance.py --markdown` 生成下表（**复核日 = 该结论最后一次实测/复核日**；证据来自 §5.3 评估实录与各适配器 docstring）。**剩余（非代码）：** 逐公司结论的法务复核签署。
- [x] **④ 监控看板（采集成功率、任务积压、命中率基线）就绪**——`GET /api/insights/ops`（+ `/insights` 运维卡片）。口径：成功率**分母含限流与被拒**（剔除会虚高）；队列积压取 Redis `llen("celery")`，**不可用回 `available:false` 而非 0**，另给 pending/超期投递业务口径；命中率基线复用 `insights.build_quality_report`。阈值判定收敛为 `alerts`（全绿为空）。**剩余（非代码）：** 外部告警通道（Alertmanager / 钉钉）与 Prometheus 服务端部署；**Prometheus 指标暴露与结构化日志 / Sentry 接入已于 2026-09-25 第十六轮落地（`GET /metrics` 文本格式、`LOG_JSON=true`、`SENTRY_DSN` + `pip install sentry-sdk`）**。
- [x] **⑤ `.env` 密钥轮换与生产配置审计**——`app/services/prodcheck.py`（纯函数，`fail`：密钥占位/短于 16 位、SMTP·IM 半配置、`.env` 被 git 跟踪；`warn`：SQLite、未启用 Redis、默认示例 UA、示例文件漏列密钥项）+ `scripts/audit_production_config.py`（有 `fail` 则 exit 1）+ `.env.example` 密钥段；审计**只判长度与占位词、不回显原文**。**剩余（非代码）：** 8 项密钥在服务商侧的**实际轮换操作**（清单见脚本输出与 `prodcheck.ROTATION_KEYS`，含 §4.3 的 `AUTH_SECRET` 与 §10 的 `UPLOADS_KEY`——前者泄露可伪造任意用户令牌，后者泄露则存量原件全部可解、轮换需先重新加密存量）与生产置 `AUTH_REQUIRED=true`（否则 `auth_not_required` 会持续 warn）。

**协议/隐私政策送审出口（§14 ①，2026-09-24 第十三轮）：**

| 事项 | 怎么做 |
|---|---|
| 出中英对照送审稿 | `python scripts/export_legal_text.py --lang both --out 送审稿.txt`（默认打印到 stdout；`--lang zh|en` 出单语） |
| 结构校验（阻断） | 中英小节 id/顺序一致、块类型与列表条数一一对应、每节字数下限、英文**不得残留中文**、无 TODO/待补 占位、效力声明与 `meta.authoritative_lang` 一致、前端版本 = 后端 `POLICY_VERSION`。有问题 **exit 2** 并逐条列出问题（定位到"文档/小节 id/语种"） |
| 签署状态（非阻断） | `meta.legal_review.signed_version / reviewer / reviewed_at` 三者齐备**且签署版本 = 当前政策版本**才算闭环，否则 **exit 1** 并列出缺口——**政策改版会让原签署失效**，避免"签过一版、后面偷偷改正文" |
| 改正文的唯一入口 | `frontend/app/legal/legal-text.json`（页面、送审稿、用例读的都是它）；改完递增 `legal.POLICY_VERSION` 与 `legal-doc.js` 的版本号（用例会拦住漂移） |
| 送审时必须法务/运营拍板 | ① 运营主体名称与联系方式（终稿「六、联系方式」需落到具体主体与邮箱）② 适用法律与争议解决条款 ③ 个人信息保存期限与到期处理 ④ 英文版效力（现为"中文为准、英文参考"） ⑤ 加密存储落地后删掉隐私政策第四节的披露句（脚本固定打印这 5 条） |

**逐公司 robots/terms 审计表（§14 ③，`scripts/audit_compliance.py --markdown` 生成，覆盖 11/11 = 100%）：**

| 公司 | ATS | robots | terms | 结论 | 复核日 |
|---|---|---|---|---|---|
| NVIDIA（nvidia） | workday | allow | public_api_ok | collect | 2026-09-16 |
| Stripe（stripe） | greenhouse | not_applicable | public_api_ok | collect | 2026-09-16 |
| Twitch（twitch） | lever | not_applicable | public_api_ok | dormant | 2026-09-16 |
| 网易（netease） | netease | no_robots_fallback | public_api_ok | collect | 2026-09-16 |
| 大疆 DJI（dji） | official_site | halted | halted | dormant | 2026-09-16 |
| Anyscale（anyscale） | lever | not_applicable | public_api_ok | dormant | 2026-09-16 |
| Robinhood（robinhood） | greenhouse | not_applicable | public_api_ok | collect | 2026-09-16 |
| ElevenLabs（elevenlabs） | ashby | not_applicable | public_api_ok | collect | 2026-09-18 |
| Linear（linear） | ashby | not_applicable | public_api_ok | collect | 2026-09-18 |
| Equinox（equinox） | smartrecruiters | not_applicable | public_api_ok | collect | 2026-09-18 |
| Ashby（ashby） | ashby | not_applicable | public_api_ok | collect | 2026-09-18 |

**读表口径：** `robots` ∈ `allow`（robots 允许）/ `no_robots_fallback`（无 robots 时的保守回退，限速更严）/ `not_applicable`（公开 API，不适用 robots）/ `halted`（已停采）；`terms` ∈ `public_api_ok`（公开接口允许）/ `terms_silent`（条款未明示，按最保守口径）/ `halted`；`decision` ∈ `collect`（在采）/ `dormant`（休眠，**保留在表中并记录原因，不做反爬绕过**——dji 因 robots/terms 均不允许而停采，twitch/anyscale 是 P1 探测期入库、已迁出采集名单）。

**机器兜底（不靠人记的部分）：**

| 事项 | 兜底机制 |
|---|---|
| 同意版本前后端漂移 | `test_legal.py` 断言前端硬编码版本 = 后端 `POLICY_VERSION`（三点一线） |
| 政策英文版漏译 / 结构漂移 | `test_legal.py` 断言中英小节 id 与段落/列表一一对应、**英文不得残留中文且每节 ≥40 字**；`export_legal_text.py` 同规则拒收（exit 2） |
| 政策改版后拿旧签署当有效 | `meta.legal_review.signed_version` 与当前版本比对，不一致即报"须重新复核"（exit 1） |
| 效力声明自相矛盾 | `disclaimer` 与 `meta.authoritative_lang` 双向校验（说"中文为准"就不能反过来） |
| 新增公司漏做审计 | `GET /api/compliance/audit` 把无审计行的公司单列 `unreviewed`；`test_compliance.py` 断言 seed 公司必有审计行 |
| 审计行写错枚举/缺字段 | `validate_audit_rows()` + `scripts/audit_compliance.py` 非零退出 |
| 看板"美化"口径 | 用例断言限流计入成功率分母、Redis 不可用不得回 0 |
| 密钥进版本库 / 占位值上线 | `scripts/audit_production_config.py` 有 `fail` 即 exit 1（含 `.env` 被 git 跟踪、占位词、过短） |