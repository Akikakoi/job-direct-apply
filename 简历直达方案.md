# 简历直达：基于企业官方 ATS 的职位推荐与投递平台 · 方案文档

> 版本：v2.0 ｜ 状态：方案完善中 ｜ 面向市场：中国大陆 + 海外 ｜ 数据策略：公开结构化 ATS 优先，官网爬虫兜底

---

## 1. 产品定位

**一句话：** 上传简历 → 从"企业官方招聘系统（ATS）"实时匹配在招职位 → 给出排序推荐 + 直达官方申请入口 + 投递进度追踪。

**差异化不是"多一个职位搜索"，而是三件事：**

1. **数据源是官方 ATS**，而非聚合搬运站 → 真实、及时、有"官方申请"的正规感；
2. **一键直达官方入口**，投递方式更正式，候选人心理上命中率更高；
3. **投递闭环**，从投递 → 催进 → 反馈全程可追踪。

**必须坦诚的边界：** 平台的核心价值建立在"简历匹配质量"和"投递陪伴体验"上，而非"多一个搜索入口"。若只会把用户导去官网，用户直接在官网搜即可，产品没有存在意义。因此匹配引擎与反馈闭环是真正的护城河，后续所有投入向这里倾斜。

**目标用户：** 主动求职者（应届生、社招、海归/海外求职者），对"投得正规、看得真实"有强需求的群体。

---

## 2. 目标市场与用户场景

### 2.1 双市场覆盖

| 市场 | 特点 | 数据源侧重 |
|---|---|---|
| 中国大陆 | ATS 供应商集中，但公开开放接口稀缺 | 官网 + 低频结构化兜底 |
| 海外 / 全球 | Workday/Greenhouse 等公开 API 成熟 | Workday、Greenhouse、Lever 为主 |

### 2.2 典型用户旅程

1. 上传/填写简历（支持中英双语）；
2. 系统解析画像并匹配企业官方在招职位；
3. 查看按匹配度排序的推荐列表，附"直达申请链接/半自动投递"；
4. 发起投递并进入投递看板；
5. 查看反馈与命中率分析，获得简历/匹配优化建议。

---

## 3. 数据源策略（结构化优先、官网兜底）

### 3.1 现实前提（必须正视）

> **海外**与**国内**的 ATS 开放程度完全不同，方案按此分层设计。

- **海外（Workday / Greenhouse / Lever / SmartRecruiters）**：三者均提供**按公司独立部署的公开职位 JSON API**。特点是稳定、反爬少、数据质量高，但没有"全局统一索引"，**必须逐公司配置站点**。
- **国内（北森 / Moka / 大易）**：多数**没有合规的对外开放接口**；嵌入页面的"结构化 JSON"往往带签名/加密并与反爬耦合。直接高频抓取既不稳定也有合规风险。因此国内数据源策略调整为：**公开稀疏抓取 + 官网列表页兜底 + 高频退化为人工引导**。宁可少而稳，不做高危采集。

> 结构化为第一优先的根本原因：数据稳定、可保鲜、可追溯。国内若客户本身是北森/Moka 的 SaaS 用户，长期理想路径是通过**企业侧授权/合作**拿到数据，而非爬。

### 3.2 公司 → ATS 站点映射表（冷启动核心资产）

海外按公司独立部署，因此**"公司名 → 所属 ATS + 站点配置 + 抓取策略"的映射表是本项目最关键的基建资产**，没有它 M1 无法落地。示例结构：

```
company_slug | company_name | ats_type | site_url/feed_url | locale | auth_type | fetch_policy
```

首批映射清单（配合 M1 一起确认）：

| 数据源 | 公司/站点形态 | 优先级 | 备注 |
|---|---|---|---|
| Workday | `api.myworkdayjobs.com/<tenant>`，按公司独立租户 | 高 | 需逐租户配置 |
| Greenhouse | `<company>.greenhouse.io` 公开 JSON API | 高 | 逐公司配置 |
| Lever | `<company>.jobs.lever.co` 公开 JSON API | 高 | 逐公司配置 |
| SmartRecruiters | 公开职位列表 JSON API | 中 | 逐公司配置 |
| 北森 / Moka / 大易 | 官网嵌入页（受限） | 低→中 | 低频稀疏抓取，遵守 robots，走官网兜底 |
| Boss 直聘官方企业号 | 公开企业页补充 | 低 | 仅补充，不做主力 |

### 3.3 统一连接器层

每类 ATS 一个适配器，输出统一**标准化职位 Schema**：

```
job_id | company | title | city | skills[] | experience_req | degree_req
salary_range | publish_date | apply_url | can_auto_apply | source | ats_tenant
```

同时在适配器层完成：

- **去重归一**：同一 JD 在多数据源撞库时以"官方源优先 + job_id 主键"合并；
- **失效清理**：标记长期不可达 / 下线的职位，启动 TTL 下架策略；
- **抓取策略卡片**：每家站点记录限流、频率、robots/terms 遵守状态，供巡检审计。

---

## 4. 核心流程与系统模块

```
简历(上传) → [简历解析] → [匹配引擎] → [推荐排序页] → [一键投递/直达链接]
                                                        ↓
                          [投递看板 ← 状态回写 ← 半自动提交]
                                                        ↓
                          [命中率分析 → 简历/匹配优化]（反馈闭环）
```

| 模块 | 职责 | 关键技术点 |
|---|---|---|
| 简历解析 | 中英双语抽取技能/经验年限/意向岗位/城市/薪资/行业 | 规则 + LLM 结构化抽取 |
| 职位采集 | 对接各 ATS 适配器，去重、结构化、保鲜（含 TTL 下架） | 官方结构化接口优先，HTML 兜底 |
| 匹配引擎 | 简历画像 ↔ 职位打分排序 | 一期标签+规则；二期向量+LLM |
| 公司→ATS 映射 | 站点配置与抓取策略管理 | 冷启动基建，随接入持续扩充 |
| 推荐页面 | 按匹配度排序，标注投递方式 | 直达链接 / 需登录自动投 |
| 一键投递 | 直达官方入口，记录投递状态 | 能自动则自动，不能则给直达链接 |
| 追踪反馈 | 进度看板、催进、命中率分析、简历优化 | 结果回灌匹配引擎调优 |

### 4.1 投递三档模式（合规 + 信任优先）

1. **公开表单可投** → 半自动帮填，跳转后由用户人工确认提交；
2. **需登录/验证** → 生成直达链接 + 按企业模板生成投递备注，用户一键带入；
3. **一律保留人工确认兜底**，不硬刚 ATS 登录与验证码风控。

**信任与责任条款（新增）：**

- 所有半自动投递**启动前必须让用户知情**"将跳转到企业官方系统、并提供其简历信息"，并有显式授权记录；
- 因代填导致的 ATS 判定风险、用户企业账号受害的**责任归属在用户协议中明确**；
- 平台**只在本站存储投递行为日志**，被投递的简历内容以用户显式授权为准。

---

## 5. 匹配逻辑与质量度量

### 5.1 匹配方法

- **一期（保证可解释）：** 技能标签命中、经验年限符合、城市匹配、岗位关键词匹配 → 加权打分排序；
- **二期（提升召回质量）：** 向量语义匹配（PostgreSQL PGVector）+ LLM 语义打分，并与一期规则分融合；
- **反馈闭环：** 投递结果（约面/拒绝/无反馈）回写，用于调整排序与简历优化建议。

### 5.2 质量指标（新增，作为验收底线）

- **线上：** 简历→职位点击率、投递率、约面率、offer 率；
- **排序离线评测：** 用历史"真实约面职位"反推，计算 NDGC/召回率，随反馈数据持续累积；
- **定位锚点：** "约面率/offer 率"取代"点击量"作为产品价值标尺，避免只做流量不做效果。

---

## 6. 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | Next.js / React | SPA + 服务端渲染，后续可扩展 PWA |
| 后端 | Python FastAPI（单体） | 统一承载 BFF、匹配网关 |
| 采集/匹配 worker | Celery / APScheduler | 挂在同一 FastAPI 后端下，减少独立部署 |
| 存储 | PostgreSQL（数据 + PGVector）+ Redis（去重/缓存/限流） | |
| 向量检索 | PostgreSQL + PGVector（二期） | |

> **修正 v1：** 不再拆分"独立 Python 采集/匹配服务"，统一为一个 FastAPI 单体 + Celery worker，降低部署与维护成本。若后续采集规模逼停主服务，再拆独立 worker 集群。

---

## 7. 里程碑

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| M0 方案定稿 | 本文档确认 | 方案 approve |
| M1 数据接入（PoC） | 建立"公司→ATS 映射表"，接 3 家海外 + 低频覆盖 2 家国内官网兜底 | 能拉取真实在招职位并入库，映射表覆盖首批公司清单 |
| M2 简历 → 推荐 | 双语简历解析 + 规则打分 + 推荐列表页 | 上传简历可出排序推荐 |
| M3 投递闭环 | 直达链接/半自动投递 + 状态看板 + 反馈日志 | 完整闭环可用，信任/授权流程上线 |
| M4 扩容与语义 | 扩大 ATS 覆盖 + 向量/LLM 匹配 + 命中率报告 | 约面率/NDCG 达到预设基线，可评估价值 |

---

## 8. 关键风险与合规

1. **数据源可用性（最高风险）：** 国内 ATS 公开接口稀缺、海外按公司隔离无全局索引 → 用"公司→ATS 映射表"对冲冷启动，国内转低频兜底与未来企业合作路径。
2. **自动化投递风控：** ATS 登录与验证码会封禁批量操作 → 始终保持"人工确认兜底"，以半自动姿态推进。
3. **个人信息 / 个保法：** 平台存储用户简历（含手机、学历、经历）需满足授权、最小必要、可删除；跳转第三方 ATS 前需显式告知并记录授权。采集侧仅用公开接口、遵守 robots，杜绝突破登录/反爬。
4. **用户信任与责任：** 半自动代填可能影响用户企业账号，责任归属在协议与授权流程中明确，避免产品背锅。
5. **抗规模化失真：** 匹配质量是关键护城河，用约面率/NDCG 持续度量，避免"只做流量、不做效果"。

---

## 9. 商业化方向（收敛为主线）

**收敛为一条主线：求职者付费的"效果型工具"。** 期初不依赖导流向雇主收费，避免雇主付费与引流混为一谈、两败俱伤。

- **主线（C 端为主）：** 免费基础推荐 + 增值订阅（深度投递分析、简历逐条差距优化、催进提醒、面试陪伴、招聘机会提醒）；
- **顺延 B 端（数据成熟后）：** 将脱敏聚合的**职位趋势/薪酬数据**及**简历匹配能力 API** 输出给 HR/招聘 SaaS，聚焦"数据与能力"而非"卖简历"，回避与企业招聘岗位的直接冲突；
- **明确不做/缓做：** 导流按有效简历向企业收费，因其与 C 端定位和目标客户重叠冲突，暂缓。

---

## 10. 可拓展方向（主线立稳后的延伸）

> 以下项目当前**不进入本轮里程碑**，仅作 roadmap 储备，避免过早铺开。

1. **求职洞察 / 市场情报：** 基于职位库产出城市/行业/技能薪资趋势、岗位竞争度报告 → C 端订阅 + B 端脱敏数据（变现路径最清晰）。
2. **面试陪伴闭环：** 投递后生成面试准备清单、公司背景包、定时"催进"提醒 → 高频、强留存场景。
3. **AI 简历优化：** 对照目标 JD 给逐条差距与改写建议，与匹配引擎共享解析/打分底座，边际成本低、付费意愿高。
4. **移动端 / PWA / 微信小程序：** 求职路径以手机为主，小程序生态可顺带解决部分账号冷启动。
5. **招聘季与多语言：** 海外端接季节性校招，投递看板做全球化多语言。

---

## 11. 下一步待确认

- [x] M1 首批接入公司名单（含每家公司"所属 ATS + 站点配置"）确认——2026-09-16 定稿：NVIDIA/Stripe/Robinhood（海外，Workday×1 + Greenhouse×2；Lever 经实测生态萎缩已剔除）+ 网易（国内 official_site，NeteaseAdapter 已落地入库 2629 条）；大疆经实测为 Moka 加密体系、暂缓休眠，国内第二家补位候选待评估，详见开发文档 §5.3；
- [ ] 数据源接口调研（重点核实 Workday / Greenhouse / 北森 / Moka 的真实开放形态与可用性，产出一份"可用性评估表"）；
- [ ] 商业化主线（C 端效果订阅）是否认可，B 端与导流是否按"缓做/暂缓"处理；
- [ ] 匹配质量基线目标（约面率/NDCG 预设值）与首批取数口径。